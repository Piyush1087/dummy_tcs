# Corrected Campaign Page attachment manifest

## Codex-only authority

- This corrected package and registry.
- Frontend reconciliation authority at `1048e486fa6b7178e34c636aeaa47bda881e2cb1`.
- Phase G documents `00`, `04`–`13`, and `11A` at that SHA.
- Accepted Campaign Page source, tests, Aurora, AppShell, and Create Campaign visual references.
- Backend contracts remain read-only supporting authority.

Never upload repositories, Git mechanics, engineering standards, or source to Stitch.

## Stitch packet

Use retained global context, or attach `global/01A`–`01D` only if missing/contradicted. Attach:

1. `modules/campaign/campaign-page-reconciliation/02-grounding.md`.
2. Exactly one brief from `modules/campaign/screen-briefs/campaign-page-reconciliation/`.

Do not attach the old Campaign Page package/briefs as corrected lineage authority. Approved Create Campaign screenshots may be optional visual references only.

## Per-attachment record

Record path, library commit, blob SHA, stage, connector attachment ID, version, and retained-context reuse. Preserve every generated prompt, screenshot, HTML/code resource, stable ID, and review classification under `artifacts/campaign-page-reconciliation/`.
