# Raw Gatekeeper Stitch export

These files are preserved reference evidence. Do not normalize, refactor, restyle or execute the generated HTML as production code.

- `stitch_gatekeeper_scan_codex/` is the unchanged downloaded Stitch project bundle from `C:\Users\piyus\Desktop\stitch_gatekeeper_scan_codex (5)\stitch_gatekeeper_scan_codex`.
- `family-b-export-final/` is the unchanged separate download containing final D02 `17ee893f03e64a92aef0ab128482678a` and M02 `f0663d2e37b14f0a9f227e142d5438ae`.

The project bundle contains noncanonical candidates and contextual project assets. Only entries allowlisted in `../registry.md` are canonical.
