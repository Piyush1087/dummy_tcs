# Brand Preview Stitch → frontend handoff

Status: `READY FOR FRONTEND RECONCILIATION`

## Authority boundary

The six screens in `registry.md` are approved visual composition references only. They do not replace existing Creator Shop pages, the surrounding shell, routing, frontend state architecture, Product contracts, canonical copy, executable Aurora components or backend authority. Generated HTML must not be imported as production source.

The Frontend AI Worker must reconcile the existing implementation selectively using:

1. frozen Brand Preview Product, copy, frontend-state and interaction authority;
2. canonical Aurora 5.0 documentation and executable primitives;
3. the exclusive canonical allowlist in `registry.md` for composition;
4. the GO / NO-GO / IMPLEMENTATION boundaries in `reconciliation-ledger.md`;
5. the implementation corrections in `final-audit.md`;
6. relevant existing frontend architecture.

## Canonical family authority

- Fast Brand Analysis: D01 `84ebbbb4b00b445cb809bfe2a6c8e1af`, M01 `282e8f6479db47e5aa470a86d04c0560`.
- Analysis Recovery: D02 `4dc22ef43d444597bcc028b2efe31fde`, M02 `64b9512885224728b0f5369c914ea1dd`.
- Brand Preview: D03 `b3f88fe29d754ff4a6ea67bad8c323a2`, M03 `cc9d84750fdc4124a66cdab9f768c46c`.

D03 Side-by-Side Insight is the only canonical desktop Brand Preview reference. Do not use any earlier desktop candidate, color variant, corrected/refined-density candidate, staggered narrative or Layout Variant 2 as authority.

## Runtime requirements

- Runtime activity controls analysis state; do not implement fake timers, percentages or scripted completion.
- Use `Building your Brand Preview…` only as the generic fallback.
- Keep slow analysis in the same visual family with frozen reassurance and no warning/error treatment.
- Treat Audience, Opportunity and Archetype collections as runtime-variable for 1, 2 or 3 items.
- Render Recovery actions only from canonical state authority; M02 shell `Help` is not Recovery authority.

## Entry criteria for the next stage

- Six of six PNG/HTML pairs are registered and `PNG_HTML_CONSISTENT`.
- D03 Side-by-Side Insight is the exclusive canonical desktop Brand Preview.
- Superseded D03 candidates are excluded from the registry.
- Aurora implementation corrections and the reconciliation ledger are frozen.
- Raw export is preserved byte-for-byte.
- No production frontend or backend code changed during artifact registration.

The next authorized stage is `FRONTEND_RECONCILIATION`. This handoff does not itself start that stage.
