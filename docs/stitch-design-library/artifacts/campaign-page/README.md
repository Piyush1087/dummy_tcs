# Campaign Page Stitch artifacts

The [`registry.md`](registry.md) file is the exclusive 36-screen canonical allowlist for the `Campaign page codex` Stitch project.

Each numbered directory under `canonical/` contains exactly:

- `screen.png` — preserved visual evidence;
- `code.html` — preserved, untrusted design evidence and component reference.

Generated HTML must not be executed, imported into production, or treated as frontend implementation authority. Rejected, superseded, AppShell, design-system, and auxiliary artifacts are intentionally absent.
