# WINDOWS_DISK_CAPACITY_RECOVERY_PARENT — Post-Canonical Preservation and Aggressive Cleanup Relay V1

## Assignment

You are `WINDOWS_DISK_CAPACITY_RECOVERY_PARENT`.

Canonical reconciliation is complete. The mandatory read-only post-reconciliation delta preflight for Applicant AI Match and Campaign Performance/Reporting has also completed. This prompt activates only the preservation, targeted disk audit, and aggressive local/Docker cleanup stages of the post-canonical program.

Do not begin Git repository demarcation, repository retirement, AWS work, deployment, provider activation, deferred-product implementation, or either paused intelligence lane.

Your outcome is:

> Preserve the exact immutable Git authority needed to resume later, prove that no unique unpublished work will be lost, remove reconstructible Codex and Docker residue aggressively, compact Docker storage when safe, and return a precise cleanup handoff for the later Git-governance stage.

This is destructive-action authority only within the gates and allowlists below. It is not broad permission to delete unknown data.

## Controlling program authority

Repository:

`Piyush1087/dummy_tcs`

Program charter:

`docs/organization/charters/post_canonical_operational_readiness_repository_governance_and_development_deployment_program_charter.md`

Charter commit:

`003c108532b75516ded4c0818e396c820bf22bfe`

Charter tree:

`e6245ceadfdb8826e7b012956b6255c1f589d1dd`

Charter blob:

`166f3952c6b3b14224cbe26a492838b5650b1e98`

This relay records Parent review of the returned delta preflight for cleanup purposes only. It does not accept a future intelligence plan or authorize resumption.

## Canonical application authority — always protect

Authority repository:

- Branch: `canonical/mvp-canonical-application-v1-authority`
- Checkpoint commit: `73d9cf8ecd0dd477d0b2d3d04fa1a3b17bfe427b`
- Tree: `16300ee47c241c3717a24603c02114275ee4b4bc`
- Record path: `docs/ai-collaboration/core-platform-centres/canonical-reconciliation/CANONICAL_RECONCILIATION_FINAL_ACCEPTANCE_AND_CHECKPOINT_V1.md`
- Record blob: `43c48758a32290783f49991d9277408c0e951a9b`

Canonical backend:

- Repository: `Piyush1087/creator-commerce-backend-v2-clone`
- Branch: `canonical/mvp-canonical-application-v1-backend`
- Commit: `1a99cf3f54df849408c8775b20e78c036ee637c3`
- Tree: `9679ed60f186220716e948779d3d70dbbf89b39a`
- Schema blob: `1d9b3a8bf3145d47d326fae88493f7816eef9140`
- Migration count: `108`
- Migration subtree: `ae9c3e8036461260ad6f0963dffb7d5d9d010139`

Canonical frontend:

- Repository: `Piyush1087/creator-commerce-frontend-v2-clone`
- Branch: `canonical/mvp-canonical-application-v1-frontend`
- Commit: `edc6ff991a43ce3a93d2bd242d148a6fcbd104dc`
- Tree: `8b8a9c515afb04a69a962e4d786d7f398854ea9c`

Final validation evidence that must remain remotely reachable:

- Backend validation support: `9b48025a1aacfe00a8ea0451ca250f6a1bd853e9`
- Frontend validation support: `9685d1ef0b3c3eaa9f68ea7d60eb29e92a2a0fca`
- Final whole-application evidence: `d09457951ffd04cfbd259e3ffcb6b61a6c88ccde`

Validation-support commits are evidence only. They do not require retained local dependency trees, worktrees, browser artifacts, databases, containers, images, networks, or caches after remote reachability is proven.

## Reviewed intelligence-preflight disposition

The read-only delta preflight returned:

```text
CANONICAL_CHECKPOINT_INTEGRITY = PASS

APPLICANT_MATCH_P0_DISPOSITION = CARRY_FORWARD_WITH_BOUNDED_AMENDMENTS
APPLICANT_MATCH_PLANNING_CAN_RESUME = NO
APPLICANT_MATCH_P1_IMPLEMENTATION_CAN_BEGIN = NO

CAMPAIGN_REPORTING_PLAN_DISPOSITION = CARRY_FORWARD_WITH_BOUNDED_AMENDMENTS
CAMPAIGN_REPORTING_PLANNING_CAN_RESUME = NO
CAMPAIGN_REPORTING_IMPLEMENTATION_CAN_BEGIN = NO

PARALLEL_IMPLEMENTATION_SAFE = CONDITIONAL
```

The preflight performed no mutation or resumption. No branch was created. No install, test, build, migration, database, browser, provider, or model execution occurred.

Several old validation-harness worktrees were already dirty before the inspection. They are not accepted intelligence implementation checkpoints. Nevertheless, do not delete any dirty worktree until its exact diff, HEAD/ref condition, ownership, and remote reconstructibility have been classified.

## Paused-lane preservation capsule

Treat this section as the minimum durable resumption capsule. Verify the objects remotely before destructive cleanup and include the verified result in your protection manifest.

### Lane A — Applicant AI Match

- Pause authority commit: `2358578de79a99cef1aae88f9c5059e19c0e9ef5`
- Pause tree: `a80dd781b7c151511f971caa65d163829370ebf5`
- Pause register: `docs/ai-collaboration/campaign-page/campaign-applicant-ai-match-v1/CAMPAIGN_APPLICANT_AI_MATCH_PAUSE_AND_RESUMPTION_REGISTER_V1.md`
- Last accepted P0 authority: `3b62be76a4412f7f657e66291b253bf9918b76f9`
- Accepted P0 tree: `c207976e475570b5b5f20dc863fc1a23b6f694d1`
- Accepted implementation checkpoint: `NONE`
- Backend implementation branch: `NONE`
- Frontend implementation branch: `NONE`
- P1–P4: `NOT_STARTED / NOT_AUTHORIZED`
- Future backend base: canonical backend `1a99cf3f54df849408c8775b20e78c036ee637c3`
- Future frontend base: canonical frontend `edc6ff991a43ce3a93d2bd242d148a6fcbd104dc`
- Historical backend evidence/donor only: `3c1c523a178c452023add9745628ecff8a6e5e55`
- Historical frontend evidence/donor only: `0662cd6963859d9e46abe53910b174d4826adaa3`
- Restart boundary: a separately authorized bounded planning amendment after owner prerequisites are accepted; never restart P0 automatically.
- Missing prerequisites: `CampaignTargetingHandoffV1`; `C03_APPLICATION_SNAPSHOT_V2`; durable Match-ready signal; APPLICATION subject support in the shared Intelligence runtime; a converged owner checkpoint; Parent planning and P1 authorities.
- Status: `NO_FURTHER_EXECUTION_UNTIL_RESUMED`

### Lane B — Campaign Performance/Reporting

- Pause authority commit: `ab31144a40c921af448fb745745d1df122d8211a`
- Pause tree: `221e9845384e583dc19b4eabf21216fe2544d6e0`
- Pause register: `docs/ai-collaboration/campaign-page/campaign-reporting-v1/CAMPAIGN_REPORTING_PAUSE_AND_RESUMPTION_REGISTER_V1.md`
- Last accepted pre-pause authority: `cf1d09bdcc37012e1dc6076caf2db8bb28a7b8b5`
- Accepted pre-pause tree: `617817e2` prefix as recorded by the preflight; resolve and record the full immutable tree before deletion.
- Accepted implementation checkpoint: `NONE`
- Future backend base: canonical backend `1a99cf3f54df849408c8775b20e78c036ee637c3`
- Future frontend base: canonical frontend `edc6ff991a43ce3a93d2bd242d148a6fcbd104dc`
- Historical backend convergence evidence only: `ebe1b4a8faa6dab59fb924b4c42be8f9e3f1e6f2`
- Historical frontend convergence evidence only: `3d04cd9c84585218fcdc0f1412662a165c38400c`
- Restart boundary: separately authorized Reporting planning amendment, followed by separate implementation authority.
- Missing prerequisites: CAMPAIGN subject/root current-generation support; typed C04 publication-to-provider lineage; post-108 migration plan; accepted Reporting route/DTO/compatibility disposition; provider-backed versus ASSETS-first Product decision; Parent planning and implementation authorities.
- Status: `NO_FURTHER_EXECUTION_UNTIL_RESUMED`

### Cleanup conclusion from the preflight

Neither paused lane has an accepted implementation checkpoint, resumable implementation branch, or unique accepted database state.

Therefore, after exact verification:

- local paused-lane worktrees and clones are not required for resumption;
- their `node_modules`, build outputs, test/browser outputs and caches are reconstructible;
- task-owned stopped containers, images and networks are reconstructible;
- task-owned disposable or paused-lane database volumes are not authority and may be removed when ownership and non-use are proven;
- historical heads must remain reachable in Git as evidence, but must not be kept as active local worktrees or selected as future bases;
- future resumption starts from the canonical application commits and the immutable pause/preflight authority, not from a local runtime snapshot.

Do not convert that conclusion into a wildcard deletion. Each actual path or Docker object still requires exact identity and ownership classification.

## Scope

Primary filesystem scope:

`C:\Users\piyus`

Prioritize targeted high-yield roots instead of repeating an unbounded whole-profile recursive scan:

- `C:\Users\piyus\Documents\Codex`
- known canonical-reconciliation and campaign-intelligence runner roots;
- known backend, frontend, and authority clones/worktrees;
- user-local npm, Playwright, package-manager, test, browser and temporary caches;
- Docker Desktop data under the user profile;
- Docker containers, images, networks, volumes and build cache;
- task-owned PostgreSQL runtimes and volumes;
- large VHDX files after Docker-object cleanup.

Expand beyond these targets only when a measured parent directory indicates material recoverable capacity. Do not launch a slow whole-profile content/hash scan before targeted cleanup. Use metadata and path-scoped inspection first.

## Required execution model

Proceed in one controlled program unless a mandatory stop condition is reached:

1. Verify immutable Git authority.
2. Inventory and classify protected state.
3. Produce an exact deletion allowlist.
4. Execute allowlisted cleanup in bounded waves.
5. Re-measure and compact Docker VHDX if useful and safe.
6. Re-verify Git, Docker, preserved paths, and resumability.
7. Return one consolidated result.

Do not report back merely to ask whether an item already proven reconstructible can be deleted. This prompt is that authority. Do not continue past an unknown database, secret store, uncommitted source delta, unreachable Git object, or ownership ambiguity.

If a command fails for a clearly local and recoverable reason, inspect and make up to two bounded corrections before declaring a circuit breaker. Never broaden a deletion target to work around a failure.

## Phase 0 — Host and safety preflight

Record without exposing secret values:

- system-drive total and free bytes;
- Docker client/server health;
- Docker Desktop/WSL storage locations and VHDX sizes;
- active processes using known worktrees or Docker resources;
- active listeners relevant to project runtimes;
- current Git worktree registrations for the three repositories;
- exact locations of `.env*`, credential/config, SSH, cloud, GitHub and provider stores by path and size only;
- reparse points/junctions in targeted cleanup roots.

Rules:

- Do not follow reparse points recursively without proving the resolved target.
- Do not print, hash through output, copy into evidence, or expose secret contents.
- Do not kill unrelated processes.
- You may stop a task-owned disposable runtime only after proving its ownership.
- Do not factory-reset Docker Desktop.
- Do not use broad `docker system prune --volumes` or filesystem wildcards as the deletion mechanism.

## Phase 1 — Remote reachability and local-state protection

Using direct repository URLs and isolated verification refs, verify every canonical, validation-evidence, paused-lane, and historical-evidence commit listed above.

For every Git clone/worktree under the targeted roots record:

- absolute path;
- repository remote URL;
- resolved HEAD or dangling/missing-ref condition;
- branch/ref;
- clean/dirty/untracked status;
- staged, unstaged and untracked path counts;
- whether every required commit/object is reachable remotely;
- whether the directory contains `.env*` or other protected configuration;
- approximate reclaimable bytes, especially `node_modules`, build and artifact subtrees;
- classification.

Classifications:

```text
CANONICAL_PROTECTED
AUTHORITY_EVIDENCE_PROTECTED
PAUSED_LANE_GIT_EVIDENCE_REMOTE_ONLY
SECRET_OR_ENVIRONMENT_PROTECTED
RECONSTRUCTIBLE_FROM_GIT
RECONSTRUCTIBLE_FROM_LOCKFILE
HISTORICAL_RUN_SAFE_TO_DELETE_EXACT
DATABASE_REQUIRES_EXACT_CLASSIFICATION
REQUIRES_HUMAN_REVIEW
UNKNOWN_STOP
```

Dirty historical validation/candidate worktrees may be deleted only when all of the following are true:

1. the dirty paths are validation/support/generated residue rather than unaccepted product source;
2. the accepted/canonical or evidence commits are remotely reachable;
3. no unique untracked authority document, patch, database dump, environment file, or credential exists there;
4. any still-useful small non-secret evidence is already committed remotely or explicitly preserved outside the deletion target;
5. the exact worktree and directory are allowlisted.

If a dirty path cannot meet all five conditions, retain it and continue cleaning other exact targets.

## Phase 2 — Docker ownership and database classification

Inventory every container, image, network, volume and build-cache object with exact IDs/names, labels, status, creation time, size where available, mounts, and referencing containers.

Classify each object as:

```text
ACTIVE_PROTECTED
CANONICAL_OR_AWS_FUTURE_NEUTRAL
PAUSED_INTELLIGENCE_RECONSTRUCTIBLE
HISTORICAL_RECONCILIATION_RECONSTRUCTIBLE
DISPOSABLE_TEST_OR_VALIDATION
DATABASE_RECONSTRUCTIBLE_EXACT
DATABASE_UNKNOWN_STOP
SAFE_TO_DELETE_EXACT
```

The following known historical objects require fresh inspection rather than automatic preservation:

- any exited Campaign Intelligence container;
- its task-owned database volume;
- historical C03 dependency/dist volumes and networks;
- canonical-reconciliation/final-gate containers, images, networks and volumes;
- `creator-shop-acceptance-postgres` and its mounts;
- stopped PostgreSQL runtimes from prior acceptance or validation runs.

Paused intelligence resources may be removed when exact ownership is proven because neither paused lane has an accepted implementation checkpoint and future execution must start from canonical Git.

Database volumes may be removed only when all are true:

1. exact owning task/lane is known;
2. no active container, environment, or later stage references the volume;
3. it contains only disposable validation/test/paused-lane runtime state;
4. schema and application state are reconstructible from the protected Git commits, migrations and accepted fixtures;
5. no unique provider credential, production-like data, required dump, or accepted implementation checkpoint depends on it;
6. the exact volume name is in the deletion allowlist.

If `creator-shop-acceptance-postgres` satisfies all six conditions, its container and exact task-owned volume may be removed. If any condition cannot be proven, retain it as `DATABASE_UNKNOWN_STOP`; that does not block deletion of other objects.

Never delete an unidentified database volume.

## Phase 3 — Exact deletion allowlist

Before mutation, create an in-memory or local evidence table containing for every proposed target:

- exact absolute path or Docker object ID/name;
- type;
- measured/estimated bytes;
- owner task/lane;
- why it is reconstructible;
- remote Git/lockfile/migration proof;
- protected exclusions inside or adjacent to the target;
- deletion method;
- expected post-delete verification.

Reject any entry expressed only as:

- a user-profile root;
- a workspace root containing mixed ownership;
- an unresolved environment variable;
- a wildcard/glob;
- “all unused volumes”;
- “all old Codex files”;
- another broad description.

Resolve exact targets first.

## Phase 4 — Aggressive cleanup waves

Execute from lowest-risk/highest-confidence to higher-risk targets. Recheck protected state between waves.

### Wave A — Historical Codex execution residue

Delete exact allowlisted:

- stale reconciliation and validation worktrees/clones;
- paused-intelligence worktrees/clones proven reconstructible;
- old run directories;
- task-local `node_modules`;
- build/dist/coverage/test outputs;
- Playwright browsers and artifacts that are not the user’s only shared installation;
- traces, videos, screenshots and sanitized evidence already committed remotely;
- disposable `.env` files only when they contain synthetic run credentials and are not needed; retain real environment/credential files.

Remove registered Git worktrees with Git-aware operations where possible before deleting directories. Do not run destructive Git reset/checkout to make a dirty worktree appear safe.

### Wave B — Docker task residue

Delete exact allowlisted:

- exited task-owned containers;
- unused task-owned networks;
- reconstructible paused-intelligence and validation database volumes that passed the database gate;
- dependency/dist volumes;
- images used only by deleted task runtimes;
- build cache not referenced by a protected active build.

Do not touch unknown volumes or credential helpers.

### Wave C — Reconstructible caches

Delete exact allowlisted:

- npm/pnpm/yarn caches;
- temporary package extraction directories;
- reconstructible Playwright/browser caches;
- project-specific temporary files;
- stale logs;
- Windows temporary files proven task-owned.

Prefer caches with high measured yield. Preserve configuration and credential files.

### Wave D — Docker VHDX reclamation

After Docker objects are removed:

1. measure the VHDX logical/file size;
2. ensure Docker/WSL filesystems are clean and no container/database write is active;
3. stop Docker Desktop/WSL through supported mechanisms;
4. compact using a supported safe route appropriate to the installed Docker/WSL version;
5. restart Docker only if required for verification;
6. verify Docker client/server health and protected objects;
7. measure final VHDX and host free space.

No factory reset. Do not delete or replace the VHDX file.

## Mandatory protections

Never delete or alter:

- the Windows user-profile root;
- any canonical or authority remote branch/ref;
- any Git object not proven remotely reachable;
- any uncommitted product/source change not proven disposable;
- `.ssh`, Git credential stores, GitHub authentication, AWS profiles/credentials, provider credentials, signing keys, password stores, or browser credential databases;
- environment files containing real configuration or secrets merely because their worktree is old;
- personal documents or unrelated projects;
- unknown database volumes;
- Docker Desktop itself;
- Git repositories as a substitute for the later canonical-versus-legacy governance stage.

Do not rewrite Git history, prune remote branches, change default branches, archive/delete repositories, or create the clean canonical repository in this task.

## Final verification

After cleanup verify:

1. the canonical authority, backend and frontend commits/trees still resolve remotely;
2. both pause authorities and both last accepted technical authorities still resolve;
3. the historical evidence heads still resolve remotely even though their local worktrees may be gone;
4. remaining local worktrees have documented status;
5. every protected environment/credential path still exists unless explicitly classified synthetic and allowlisted;
6. no unknown database volume was deleted;
7. Docker health is known, and any retained protected object is present;
8. the system-drive free bytes and Docker VHDX size are recorded before and after;
9. no secret value appears in logs or evidence;
10. Applicant Match and Campaign Reporting remain paused.

## Required result

Return one concise but complete result in this structure:

```text
WINDOWS_POST_CANONICAL_AGGRESSIVE_CLEANUP_RESULT = RESOLVED | PARTIAL | CIRCUIT_BREAKER

CANONICAL_AUTHORITY_INTEGRITY = PASS | FAIL
PAUSED_LANE_AUTHORITY_INTEGRITY = PASS | FAIL
APPLICANT_MATCH_ACCEPTED_IMPLEMENTATION_CHECKPOINT = NONE
REPORTING_ACCEPTED_IMPLEMENTATION_CHECKPOINT = NONE
PAUSED_LANE_RESUMABILITY_WITHOUT_LOCAL_RUNTIME = PASS | FAIL

PROTECTED_GIT_PATHS = <exact list or count plus evidence table>
PROTECTED_ENVIRONMENT_OR_SECRET_PATHS = <paths only, values never shown>
PROTECTED_DATABASE_OBJECTS = <exact names and reasons>

DELETED_FILESYSTEM_TARGETS = <exact targets grouped by wave>
DELETED_DOCKER_CONTAINERS = <exact IDs/names>
DELETED_DOCKER_IMAGES = <exact IDs/tags>
DELETED_DOCKER_NETWORKS = <exact IDs/names>
DELETED_DOCKER_VOLUMES = <exact names and classification>
DELETED_CACHE_TARGETS = <exact paths>

HOST_FREE_BYTES_BEFORE = <integer>
HOST_FREE_BYTES_AFTER = <integer>
OBSERVED_NET_FREE_SPACE_GAIN = <integer>
DOCKER_VHDX_BYTES_BEFORE = <integer or NOT_APPLICABLE>
DOCKER_VHDX_BYTES_AFTER = <integer or NOT_APPLICABLE>

DOCKER_HEALTH_AFTER = PASS | FAIL | NOT_REQUIRED
CANONICAL_REPOSITORY_CONTINUITY = PASS | FAIL
PAUSED_LANES_REMAIN_PAUSED = YES
GIT_GOVERNANCE_OR_AWS_WORK_STARTED = NO

RETAINED_REVIEW_ITEMS = <exact items and reason>
BLOCKERS = NONE | <exact blockers>
NEXT_BOUNDARY = RETURN_TO_POST_CANONICAL_PARENT_FOR_GIT_DEMARCATION_STAGE
```

Also provide:

- a classification table for every retained high-volume item;
- a deletion evidence table with exact targets and recovered capacity where measurable;
- any database object retained because the six-condition deletion gate could not be proven;
- any dirty worktree retained and its exact paths/status;
- any further cleanup opportunity requiring separate destructive authority.

Do not include execution-time telemetry unless Parent specifically asks for it.

## Stop conditions

Stop the affected target and report a circuit breaker if:

- a canonical or paused authority object is unreachable;
- a proposed deletion target contains unique uncommitted application/source work;
- a required `.env`, credential, key, dump or authority file would be lost;
- a database volume’s ownership or reconstructibility is unknown;
- an exact target resolves outside the intended path because of a junction/reparse point;
- Docker storage cannot be safely detached/compacted without factory reset or broad destructive action;
- the filesystem or Docker state changes unexpectedly during verification;
- the task would require repository retirement, history rewrite, AWS/provider action, or intelligence resumption.

A circuit breaker for one target does not require abandoning independent, already-proven safe cleanup targets. Isolate and retain the blocked target, continue with the remaining exact allowlist, and return `PARTIAL` unless canonical integrity itself is at risk.

