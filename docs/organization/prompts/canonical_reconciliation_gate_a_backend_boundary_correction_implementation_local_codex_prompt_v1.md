# Local Codex Prompt — Gate A Backend Boundary Correction Implementation V1

## Role and authorization

Act as the bounded implementation worker for the corrective backend checkpoint required before canonical reconciliation Gate A can be rerun.

The Parent has reviewed the read-only diagnostic and authorizes one correction candidate on top of the immutable accepted backend target. The correction consists of:

1. strengthening ten stale or incorrectly scoped test contracts; and
2. correcting one real Nest dependency-injection composition leak by separating narrow Instagram account-lifecycle clients from Instagram Intelligence/media acquisition providers.

This is a technical corrective checkpoint under existing accepted Product and architecture authority.

This prompt does **not** authorize:

- rewriting or force-moving the accepted target;
- modifying the freeze, seven-module donor history, or Gate A0 fixture;
- Product or authorization-policy changes;
- Prisma schema or migration changes;
- frontend implementation changes;
- C06 or Campaign Objective convergence;
- main Gate A reconciliation branch creation;
- final canonical publication;
- Campaign Performance, Applicant AI Match, or Campaign Intelligence resumption;
- deployment, provider credentials, or live provider calls.

Publish a correction candidate and implementation evidence, then stop for Parent review. Do not begin Gate A V4.

## Controlling repositories and immutable identities

### Authority repository

```text
REPOSITORY =
Piyush1087/dummy_tcs

CONTROLLING_REGISTER_COMMIT =
346ae2a5ec58506870036db832223c996311fc29

CONTROLLING_REGISTER_TREE =
215528f9b1e077f576ef29e5b61119b08d398107

CONTROLLING_REGISTER_PATH =
docs/ai-collaboration/core-platform-centres/CORE_PLATFORM_CENTRES_CONSOLIDATED_ACCEPTANCE_REGISTER_V2.md

CONTROLLING_REGISTER_BLOB =
c9e0343794aad4a46e27882b58b98381dd4c9dcf
```

### Backend correction base

```text
REPOSITORY =
Piyush1087/creator-commerce-backend-v2-clone

IMMUTABLE_CORRECTION_BASE =
da1d19d15198699465040c32ce075b6be1c85faf

BASE_TREE =
560e2b90e95c3456ac7eef7ce9351861ab04a576

BASE_MIGRATION_COUNT =
106

CORRECTION_BRANCH =
correction/canonical-reconciliation-gate-a-backend-boundaries-v1
```

The base is the accepted Creator Media Kit target previously selected for Gate A. It remains immutable even though its checked-in suite contains the diagnosed assertion regressions.

### Frontend validation target — read-only

```text
REPOSITORY =
Piyush1087/creator-commerce-frontend-v2-clone

IMMUTABLE_TARGET =
95058a5dd6d4e89e8c65172da4d71cfe63c03018

TARGET_TREE =
4e0c9906d5de1b6d03bfa74a7b5683c920d67a61
```

No frontend file or branch mutation is authorized.

### Gate A0 migration fixture

```text
BACKEND_FIXTURE_COMMIT =
7a70d074eb2f3109b697c953bfa8a722bf69caf4

BACKEND_FIXTURE_TREE =
efaa2f0f635788e862b2c39752802c520c3f3b06

SQL_PATH =
test/fixtures/canonical-reconciliation/migration-94/canonical_migration_94_fixture_v1.sql

SQL_BLOB =
2dc29cfed740ddd2ad4780081f68296f98c20065

SQL_SHA256 =
fe27a4fed67a5cb02b507efc7dc86aaf4a17c2b281ec660f95a92f16bc488706

NORMALIZED_MIGRATION_94_SNAPSHOT_SHA256 =
e606dde5add59ab5863725390ccffc4fad15545988ffba91062971b234265d99

GATE_A0_EVIDENCE_COMMIT =
5826c82606c77383810a0dfdc5757ca4a4ece006

GATE_A0_EVIDENCE_TREE =
f27ac6a294c3fe5a4746f5a542645b0dd5adb3ee

GATE_A0_EVIDENCE_BLOB =
928f42b2dc8655747abafa6f57ca4e7f9641b52a
```

### Prompt lineage

```text
GATE_A_V3_PROMPT_COMMIT =
8b1cb66c06d62b4ee035bb2eaebd8f69d72766a4

GATE_A_V3_PROMPT_BLOB =
1ae7cdc7a88c624dc6787cd8811fc97a66296976

READ_ONLY_DIAGNOSTIC_PROMPT_COMMIT =
f640da433966192375f5d2ca0322803b9d0d1ade

READ_ONLY_DIAGNOSTIC_PROMPT_BLOB =
92b4870af312cecc792872c40d92d631bd57c098
```

Verify all relevant objects before mutation.

## Accepted diagnostic result

```text
DIAGNOSTIC_RESULT =
MIXED_CORRECTION_REQUIRED

PRODUCT_CHANGE_REQUIRED =
NO

FRONTEND_CORRECTION_REQUIRED =
NO

MIGRATION_OR_SCHEMA_CHANGE_REQUIRED =
NO

ARCHITECTURE_CHANGE_REQUIRED =
YES — bounded Nest module-composition correction under existing authority

CORRECTIVE_BACKEND_CHECKPOINT_REQUIRED =
YES

FULL_GATE_A_RERUN_REQUIRED =
YES
```

The diagnostic reproduced all 11 failures after a clean install and Prisma generation:

- five duplicated application-global Intelligence registry expectations;
- one Creator Home global migration-count assertion;
- one Instagram provider-module overexposure;
- one stale C04 full-enum assertion;
- three Creator role/action count assertions.

Ten failures require stronger test semantics. The Instagram module failure requires a bounded runtime composition correction.

## Publication targets

Create these only after collision checks:

```text
BACKEND_CORRECTION_BRANCH =
correction/canonical-reconciliation-gate-a-backend-boundaries-v1

BACKEND_CORRECTION_BASE =
da1d19d15198699465040c32ce075b6be1c85faf

AUTHORITY_EVIDENCE_BRANCH =
reconcile/mvp-canonical-application-v1-gate-a-correction-authority

AUTHORITY_EVIDENCE_BASE =
346ae2a5ec58506870036db832223c996311fc29

AUTHORITY_EVIDENCE_PATH =
docs/ai-collaboration/core-platform-centres/canonical-reconciliation/CANONICAL_RECONCILIATION_GATE_A_BACKEND_BOUNDARY_CORRECTION_IMPLEMENTATION_EVIDENCE_V1.md
```

If either remote branch already exists, do not delete, force-update, or reuse it. Inspect and stop with `TARGET_BRANCH_COLLISION` unless its exact state is already the output of this prompt and is independently verifiable.

The authority document is implementation evidence with status `READY_FOR_PARENT_REVIEW`; it must not self-declare Parent acceptance or alter the controlling register.

## Exact allowed file boundary

The base-to-correction diff may contain exactly these 18 backend paths and no others.

### Eleven test files

```text
src/features/brand-intelligence/audience-persona.architecture.test.ts
src/features/brand-intelligence/brand-character.architecture.test.ts
src/features/brand-intelligence/brand-differentiation.architecture.test.ts
src/features/brand-intelligence/brand-meaning.architecture.test.ts
src/features/brand-intelligence/visual-style.architecture.test.ts
src/features/brand-intelligence/contracts/registry/contract-runtime.registry.test.ts
src/features/creator-home/creator-home.architecture.test.ts
src/features/notifications/notifications-module-wiring.architecture.test.ts
src/features/collaboration/schemas/provision-collaboration.schema.test.ts
src/features/creator-settings/team/creator-workspace-actor.service.test.ts
src/features/creator-settings/team/creator-team.policy.test.ts
```

### Seven runtime module-composition files

```text
src/features/instagram/instagram-provider-client.module.ts
src/features/instagram/instagram-intelligence-provider.module.ts
src/features/brand-settings/brand-settings-consumer.module.ts
src/features/creator-audience/creator-audience.module.ts
src/features/creator-content/creator-content.module.ts
src/features/data-extraction/data-extraction.module.ts
src/features/instagram-intelligence/instagram-intelligence.module.ts
```

No formatter, generator, dependency installer, or test run may leave another tracked change. Generated and ignored local files must not be committed.

If a necessary correction cannot be implemented inside this boundary, stop and report the exact additional path and reason. Do not silently expand scope.

## Mandatory timing telemetry

Record:

```text
RUN_STARTED_AT_UTC =
RUN_COMPLETED_AT_UTC =
WALL_CLOCK_SECONDS =
ACTIVE_EXECUTION_SECONDS =
COMMAND_COUNT =
LONGEST_COMMAND =
LONGEST_COMMAND_SECONDS =
DEPENDENCY_INSTALL_SECONDS =
IMPLEMENTATION_SECONDS =
FOCUSED_VALIDATION_SECONDS =
FULL_BACKEND_VALIDATION_SECONDS =
DATABASE_VALIDATION_SECONDS =
FRONTEND_VALIDATION_SECONDS =
PUBLICATION_SECONDS =
WAITING_OR_BLOCKED_SECONDS =
```

Use measured values when available and `NOT_MEASURABLE` rather than guessing.

## Execution protocol

### Phase 0 — immutable integrity and collision gate

Before editing:

1. Fetch all three repositories and required immutable objects.
2. Verify the controlling register commit/tree/path/blob.
3. Verify the backend base commit/tree and 106-migration inventory.
4. Verify the frontend target commit/tree.
5. Verify the Gate A0 fixture and evidence tuples.
6. Verify both prompt commit/blob tuples.
7. Confirm the correction and authority branch names are absent remotely.
8. Require clean tracked and staged worktrees.
9. Record Node, npm, Git, Docker, PostgreSQL, Prisma, and FFmpeg versions.
10. Reproduce the 9-file/11-test failure family at the exact backend base.

Stop if reproduction materially differs.

### Phase 1 — create the local correction branch

Create the local backend correction branch from exactly `da1d19d15198699465040c32ce075b6be1c85faf`.

Do not push it yet. Do not rewrite, merge, rebase, squash, or modify the base history.

### Phase 2 — shared Intelligence registry test corrections

The application-global contract runtime intentionally contains 18 accepted registrations, including:

```text
creator_audience_v0
creator_audience_v1
creator_brand_suggestions_v0
creator_content_v0
```

Registration does not grant arbitrary execution authority: lookups use exact compiled keys, no route accepts a user-supplied processor key, and duplicate keys/conflicting path ownership fail closed.

Implement these rules:

1. Keep one centralized exact application-global registry inventory in `contract-runtime.registry.test.ts`.
2. Strengthen that central test to assert:
   - the complete exact accepted registration set;
   - uniqueness of keys;
   - no conflicting object/path ownership;
   - expected execution-enabled status;
   - explicit Brand/Product/Instagram/Creator domain ownership or classification.
3. Remove duplicated global inventory assertions from the five processor-specific architecture tests.
4. In each processor-specific test, replace the removed global list with exact assertions for:
   - its own registry key and accepted bundle;
   - its exact object semantic IDs and owned paths;
   - required shared infrastructure;
   - non-ownership of Creator paths/objects;
   - inability to select a Creator registration through the Brand processor’s compiled key.
5. Preserve all existing dependency, no-write, no-transport, no-cross-processor-current, identity, prompt, evidence, and persistence invariants.
6. Do not weaken an exact architecture assertion into a mere `contains` smoke test without an exact centralized invariant replacing it.

No registry runtime implementation change is authorized.

### Phase 3 — Creator Home migration-ownership test

The Creator Home acceptance owns no persistence or migration. The repository-wide migration total belongs to reconciliation validation, not the Creator Home unit boundary.

Replace the hard-coded global count with durable checks that:

- no migration directory/name is owned by or named for Creator Home;
- no migration SQL creates a Creator Home table or persistence artifact;
- Prisma defines no `CreatorHome`, `HomeActivity`, `HomeEvent`, `HomeCache`, or equivalent Home-owned model/field/table;
- existing Home consumer ports remain read-only;
- the test does not hard-code 94 or 106 as a global count.

Do not change Creator Home runtime code.

### Phase 4 — Instagram lifecycle/Intelligence module separation

#### 4.1 Narrow lifecycle module

Restore `InstagramProviderClientModule` as the narrow account-lifecycle boundary:

- providers: exactly `InstagramOAuthClient` and `InstagramGraphClient`;
- exports: exactly those two clients;
- no provider-read token;
- no image/video/audio acquisition, storage, download, transport, decoder, or extractor provider/export;
- no runtime behavior changes inside either client.

Brand Settings, Creator Settings, and `InstagramModule` must remain on this narrow module.

#### 4.2 New Intelligence provider module

Create `InstagramIntelligenceProviderModule` for the accepted provider-read and contained media infrastructure.

Move the non-lifecycle bindings from the former broad module into it:

- `InstagramIntelligenceProviderClient`;
- `INSTAGRAM_INTELLIGENCE_PROVIDER_READ_CLIENT` binding;
- image locator, temporary store, DNS resolver, pinned HTTPS transport, secure downloader, and contained acquisition;
- video locator, temporary store, secure downloader, contained acquisition, decoder implementation and port binding;
- audio extractor implementation and port binding.

The new module may import the narrow lifecycle module when its internal providers require Graph/OAuth clients. It must not re-export the narrow module or OAuth/Graph clients.

Export only the accepted ports/services required by authorized consumers. Minimize exports; do not export internal DNS, transport, downloader, locator, or implementation classes unless an existing accepted consumer demonstrably requires them.

#### 4.3 Authorized consumer updates

Update only these five consumers to import the new Intelligence module:

- Brand Settings consumer module — accepted read/media wrapper only;
- Creator Audience module — provider read;
- Creator Content module — provider read/media;
- Data Extraction module — contained acquisition;
- Instagram Intelligence module — complete accepted Intelligence acquisition.

Inspect actual injection needs and preserve exact provider behavior. Do not move Settings lifecycle providers into the broad module and do not introduce circular imports or `forwardRef` merely to silence compilation.

#### 4.4 Required DI boundary tests

Strengthen `notifications-module-wiring.architecture.test.ts` and related allowed tests to prove:

- narrow lifecycle module provider/export arrays are exact;
- Settings lifecycle modules import only the narrow module;
- lifecycle/Settings module contexts cannot resolve the provider-read token or image/video/audio acquisition ports;
- the new Intelligence module does not export OAuth/Graph;
- each authorized consumer can resolve only the required accepted ports;
- internal transport/downloader/locator implementations are not leaked as public exports;
- no provider is instantiated with usable external credentials;
- no real network/provider call occurs.

Use Nest compilation tests where practical, with deny-by-default fakes/configuration. Metadata-only checks are not sufficient for the negative resolution boundary, but may supplement compilation tests.

### Phase 5 — C04 Collaboration vocabulary test

Existing authority distinguishes:

- six active operational C04 securement states; and
- three retained legacy compatibility enum values:
  `AWAITING_BRAND_PAYMENT`, `AWAITING_CREATOR_CONFIRMATION`, and `PAYMENT_DISPUTED`.

Strengthen the test to assert:

1. the exact complete nine-value Prisma enum;
2. the exact six-value active operational vocabulary used by C04 commands/transitions;
3. active provisioning/commands cannot emit the three legacy states;
4. legacy values confer no payout/provider authority;
5. `MANUAL_PAYMENT_DISABLED`, provider-disabled behavior, and zero payout/provider writes remain fail-closed.

Do not change Prisma schema, migrations, Collaboration runtime, or payout behavior.

### Phase 6 — Creator role/action contract tests

Do not assert counts.

Strengthen the policy test as the canonical exact role/action matrix:

- Owner: exactly all 29 accepted actions;
- Manager: exactly all 29 accepted actions;
- Assistant: exactly these eight actions:
  - `PORTFOLIO_READ`;
  - `CAMPAIGN_OPPORTUNITY_VIEW`;
  - `CAMPAIGN_APPLICATION_APPLY`;
  - `INSIGHTS_AUDIENCE_READ`;
  - `INSIGHTS_CONTENT_READ`;
  - `CREATOR_BRAND_READ`;
  - `COMMERCIAL_SETUP_READ`;
  - `MEDIA_KIT_READ`.

Assert explicit Assistant denial for:

- workspace/contact/team mutations and protected reads;
- Instagram Settings manage/read authority;
- Creator Brand edit/confirm;
- payout and legal read/manage;
- Work Preferences and Rate Card edit;
- Portfolio curate;
- Media Kit manage/publish/PDF download;
- campaign withdrawal;
- owner-only, financial, private-projection, cross-tenant, and non-C03 mutation authority.

The Assistant’s accepted C03 application action remains unchanged.

In the actor-service test:

- compare returned actions against the exact policy result for each role;
- retain actor, subject Creator, Owner, workspace, organization, active-membership, ambiguity, and legacy-Owner reconciliation assertions;
- do not duplicate brittle numeric counts.

No authorization policy/runtime change is authorized.

### Phase 7 — diff and static boundary gate

Before broad validation:

1. Require the base-to-working-tree path set to be a subset of the exact 18 paths.
2. Confirm no Prisma schema/migration, dependency, lockfile, generated client, provider implementation, financial, frontend, or authority file changed.
3. Confirm the 106 migration directories and all checksums are byte-identical to the base.
4. Confirm no live endpoint, credential, provider call, payout write, or new persistence was added.
5. Run formatter only on the authorized paths if required, then repeat the diff gate.

### Phase 8 — focused validation

Run the repository-defined focused Vitest commands for all 11 changed test files and all directly affected module/policy suites.

Mandatory focused outcomes:

- the prior 9-file/11-test failure family is zero failures;
- the centralized registry inventory and cross-domain exclusion tests pass;
- all five Brand processor boundary tests pass;
- Creator Home zero-owned-persistence test passes;
- narrow/wide Instagram module metadata and compilation/negative-resolution tests pass;
- C04 active-versus-legacy and provider-disabled tests pass;
- exact Creator role/action and Assistant forbidden-set tests pass;
- existing Creator actor/subject/cross-tenant tests pass;
- no test makes a network/provider call.

Record files, tests, passes, failures, skips, and duration. Do not use unsupported Jest-only flags such as `--runInBand` unless the repository runner explicitly supports them.

### Phase 9 — full backend validation

Using the lockfile and repository-supported commands:

```text
npm ci
npm run prisma:generate
npx prisma validate
npm run lint:eslint
npm run build
npm test
npm run test:campaign-readiness
npm run intelligence:contracts:verify
```

Use the documented clean authority checkout for Intelligence contract verification if required, and record it.

Requirements:

- zero failed backend tests;
- explain every skip and distinguish infrastructure-gated skips;
- no regression relative to the base’s 7,222 passing tests;
- no accepted architecture, Settings, C03, C04, Home, Evidence, Intelligence, financial, or security suite may fail.

If repository scripts differ, use the actual script and record the substitution. Do not edit scripts or package metadata.

### Phase 10 — PostgreSQL migration validation

Use disposable local PostgreSQL 16 databases in UTC. Never use shared/development/staging/production data.

#### Clean path

- From the correction candidate, apply all 106 migrations to an empty database.
- Require exactly 106 successful unique migration rows and zero failed/pending/rolled-back rows.
- Verify schema/client compatibility.

#### Populated path

- Verify the exact Gate A0 fixture commit/tree/blobs/SHA-256.
- Build a fresh database with exactly the fixture commit’s 94 migrations.
- Load the 44-row/24-table fixture transactionally.
- Require the migration-94 normalized snapshot SHA-256.
- Upgrade the same database with the correction candidate’s migrations 95–106.
- Require exactly 106 successful unique migration rows.
- Prove all 44 fixture identities survive.
- Verify historical migration checksums match the clean path and immutable files.
- Verify the unaffected control row remains unchanged over migration-94 columns.
- Execute the established migration 95–106 post-upgrade assertions from Gate A V3, including negative lineage/authorization/financial/public-versus-verified tests.
- Compare clean-106 and upgraded-106 schema/migration inventories.

Use transactions/savepoints or separate disposable clones for intentional negative failures. Remove only resources created by this run.

### Phase 11 — unchanged frontend validation

At detached exact frontend target `95058a5dd6d4e89e8c65172da4d71cfe63c03018`:

```text
npm ci
npm run typecheck
npm run lint
npm test
npm run test:campaign-autosave
npm run build
```

No frontend file or branch may change. Record complete results.

### Phase 12 — final backend diff, commit, and publication

Only if every preceding gate passes:

1. Reconfirm the diff contains exactly the authorized changed paths actually needed, no more than the 18-path allowlist.
2. Commit the correction on the local branch with a precise message.
3. Record the new commit, parent, tree, and changed blobs.
4. Prove the commit has exactly `da1d19d15198699465040c32ce075b6be1c85faf` as its first/only parent unless multiple local commits were necessary and transparently documented. Prefer one reviewed correction commit.
5. Push the new correction branch without force.
6. Fetch it into a fresh namespace.
7. Verify remote commit/tree/parent/path set/blob equality and rerun a bounded fetch-back integrity check.
8. Do not create or advance `reconcile/mvp-canonical-application-v1-backend`.

### Phase 13 — authority implementation-evidence publication

After backend fetch-back succeeds:

1. Create `reconcile/mvp-canonical-application-v1-gate-a-correction-authority` from exact authority base `346ae2a5ec58506870036db832223c996311fc29`.
2. Publish only:
   `docs/ai-collaboration/core-platform-centres/canonical-reconciliation/CANONICAL_RECONCILIATION_GATE_A_BACKEND_BOUNDARY_CORRECTION_IMPLEMENTATION_EVIDENCE_V1.md`.
3. Mark the result `READY_FOR_PARENT_REVIEW`, not accepted.
4. Include:
   - all immutable inputs;
   - diagnostic classification;
   - exact correction commit/tree/parent/path/blob inventory;
   - before/after module graph;
   - provider/export/consumer boundary table;
   - registry and role/action matrices;
   - focused and full validation totals;
   - clean and populated PostgreSQL evidence;
   - frontend unchanged validation;
   - migration identity proof;
   - provider/network/write assertions;
   - timing;
   - fetch-back evidence;
   - exclusions;
   - exact proposed use: supersede `da1d19d…` only as the backend Gate A target after Parent acceptance.
5. Commit and push without force.
6. Fetch back and verify authority commit/tree/path/blob.

If authority publication fails after backend publication, do not rewrite/delete the backend branch. Report `PARTIAL_PUBLICATION`.

## Circuit breakers

Stop safely and report the exact state if any occurs:

```text
IMMUTABLE_POINTER_MISMATCH
BASE_COMMIT_OR_TREE_MISMATCH
MIGRATION_INVENTORY_NOT_106
TARGET_BRANCH_COLLISION
AUTHORITY_BRANCH_COLLISION
PRIOR_FAILURE_FAMILY_NOT_REPRODUCED
AUTHORITY_AMBIGUITY
CHANGE_OUTSIDE_18_PATH_ALLOWLIST
PRODUCT_OR_AUTHORIZATION_POLICY_CHANGE_REQUIRED
PRISMA_SCHEMA_OR_MIGRATION_CHANGE_REQUIRED
FRONTEND_CHANGE_REQUIRED
REGISTRY_RUNTIME_CHANGE_REQUIRED
INSTAGRAM_PROVIDER_BEHAVIOR_CHANGE_REQUIRED
NARROW_MODULE_STILL_EXPORTS_INTELLIGENCE_OR_MEDIA
INTELLIGENCE_MODULE_REEXPORTS_LIFECYCLE_CLIENTS
UNAUTHORIZED_CONSUMER_CAN_RESOLVE_INTELLIGENCE_CAPABILITY
AUTHORIZED_CONSUMER_CANNOT_RESOLVE_REQUIRED_CAPABILITY
CIRCULAR_MODULE_DEPENDENCY
ASSISTANT_PERMISSION_EXPANSION
C04_LEGACY_STATE_BECOMES_OPERATIONAL
LIVE_PROVIDER_CALL_OR_CREDENTIAL_REQUIRED
FINANCIAL_WRITE_OR_PAYOUT_AUTHORITY_CHANGE
FOCUSED_TEST_FAILURE
FULL_BACKEND_TEST_FAILURE
LINT_TYPECHECK_OR_BUILD_FAILURE
CLEAN_DATABASE_106_FAILURE
POPULATED_94_TO_106_FAILURE
MIGRATION_ASSERTION_FAILURE
FRONTEND_REGRESSION
WORKTREE_NOT_CLEAN
FETCH_BACK_MISMATCH
```

Do not weaken a boundary assertion merely to make the suite pass. Do not repair unrelated failures.

## Required final response

Return:

```text
CORRECTION_IMPLEMENTATION_RESULT =
READY_FOR_PARENT_REVIEW |
CIRCUIT_BREAKER |
PARTIAL_PUBLICATION

RUN_STARTED_AT_UTC =
RUN_COMPLETED_AT_UTC =
WALL_CLOCK_SECONDS =
ACTIVE_EXECUTION_SECONDS =
COMMAND_COUNT =
LONGEST_COMMAND =
LONGEST_COMMAND_SECONDS =
DEPENDENCY_INSTALL_SECONDS =
IMPLEMENTATION_SECONDS =
FOCUSED_VALIDATION_SECONDS =
FULL_BACKEND_VALIDATION_SECONDS =
DATABASE_VALIDATION_SECONDS =
FRONTEND_VALIDATION_SECONDS =
PUBLICATION_SECONDS =
WAITING_OR_BLOCKED_SECONDS =

IMMUTABLE_INTEGRITY =
PRIOR_FAILURE_REPRODUCTION =
CHANGED_PATH_COUNT =
CHANGE_BOUNDARY =
PRODUCT_CHANGE =
ARCHITECTURE_DECISION_CHANGE =
AUTHORIZATION_POLICY_CHANGE =
FRONTEND_CHANGE =
PRISMA_SCHEMA_CHANGE =
MIGRATION_CHANGE =
PROVIDER_BEHAVIOR_CHANGE =
FINANCIAL_BEHAVIOR_CHANGE =

PROCESSOR_REGISTRY_CORRECTION =
CREATOR_HOME_TEST_CORRECTION =
INSTAGRAM_MODULE_SEPARATION =
COLLABORATION_TEST_CORRECTION =
CREATOR_PERMISSION_TEST_CORRECTION =

FOCUSED_TESTS =
FULL_BACKEND_TESTS =
BACKEND_LINT =
BACKEND_BUILD =
CAMPAIGN_READINESS =
INTELLIGENCE_CONTRACT_VERIFY =
CLEAN_DATABASE_106 =
POPULATED_DATABASE_94_TO_106 =
MIGRATION_95_TO_106_ASSERTIONS =
FRONTEND_TYPECHECK =
FRONTEND_LINT =
FRONTEND_TESTS =
FRONTEND_BUILD =
PROVIDER_CALLS =
FINANCIAL_WRITES =

BACKEND_CORRECTION_BRANCH =
BACKEND_CORRECTION_COMMIT =
BACKEND_CORRECTION_PARENT =
BACKEND_CORRECTION_TREE =
BACKEND_CORRECTION_FETCH_BACK =

AUTHORITY_EVIDENCE_BRANCH =
AUTHORITY_EVIDENCE_COMMIT =
AUTHORITY_EVIDENCE_TREE =
AUTHORITY_EVIDENCE_BLOB =
AUTHORITY_EVIDENCE_FETCH_BACK =

MAIN_GATE_A_BRANCH_CREATED = NO
GATE_A_V4_RUN = NO
C06_OR_CAMPAIGN_OBJECTIVES_ENTERED = NO
CAMPAIGN_INTELLIGENCE_REMAINS_PAUSED = YES
SOURCE_WORKTREES_CLEAN =
BLOCKERS =
NEXT_BOUNDARY = PARENT_REVIEW_ONLY
```

Also include:

1. exact changed-file and blob inventory;
2. before/after Instagram module/provider/export/consumer graph;
3. registry ownership/exclusion proof;
4. exact Creator role/action matrix and forbidden Assistant set;
5. C04 operational/legacy proof;
6. focused and full validation tables;
7. clean/populated migration evidence;
8. publication/fetch-back proof;
9. exclusions and stop-boundary confirmation.

A successful result is a correction candidate ready for Parent review. It is not itself Parent acceptance and is not permission to rerun Gate A, converge C06/Campaign Objectives, or resume paused Campaign Intelligence work.
