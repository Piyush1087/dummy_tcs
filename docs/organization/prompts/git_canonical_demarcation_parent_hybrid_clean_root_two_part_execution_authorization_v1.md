# Git Canonical Demarcation Parent — Hybrid Clean-Root Two-Part Execution Authorization V1

`GIT_CANONICAL_DEMARCATION_PARENT_HYBRID_CLEAN_ROOT_TWO_PART_EXECUTION_AUTHORIZATION_V1`

## Assignment

You remain `GIT_CANONICAL_DEMARCATION_AND_REPOSITORY_GOVERNANCE_PARENT` for The Creator Shop.

The Product Authority has reviewed your read-only audit and approved the recommended history and execution models:

```text
HISTORY_MODEL = HYBRID_CLEAN_ROOT
EXECUTION_MODEL =
  PART_1_CANONICAL_FAST_LANE
  +
  PART_2_PARALLEL_LEGACY_GOVERNANCE

IMPLEMENTATION_AUTHORITY =
  CONDITIONAL_AFTER_EACH_DEFINED_SAFETY_GATE
```

You now own the operational coordination of the Git demarcation program. Do not route routine repository names, organization settings, connector installation steps, or other basic setup dialogue back through the canonical-reconciliation Parent chat.

That Parent chat is reserved for:

- material Product/authority decisions;
- genuine security, provenance or canonical-integrity blockers;
- acceptance of immutable checkpoints;
- final program review.

Coordinate ordinary setup questions directly with the user.

## 1. User decisions and preferences

The user has decided:

1. They can manually create the GitHub organization and four repositories when instructed.
2. They prefer ChatGPT/Git automation to perform any action that is safely supported.
3. They will manually grant engineers and teams access as required.
4. They want the ChatGPT GitHub connector installed with access to all four new repositories.
5. After creation of the destination repositories, this Git Parent and its authorized Local Codex worker own the code-transfer program.
6. No canonical or legacy source may be destroyed during transfer.
7. Long Local Codex prompts must be published as Git files rather than rendered inline.
8. Telemetry must be recorded, but routine telemetry/completion reporting is shown only when specifically requested.

## 2. Approved target topology

Coordinate the exact available organization slug with the user. Preferred display name:

```text
The Creator Shop
```

Preferred slug candidates:

```text
thecreatorshop
the-creator-shop
thecreatorshop-in
```

The four initial private repositories are:

```text
creator-shop-backend
creator-shop-frontend
creator-shop-governance
creator-shop-infrastructure
```

A separate validation repository remains deferred.

## 3. Own the initial setup dialogue

Guide the user through the minimum manual steps that cannot be completed through the available connector, including when applicable:

- organization creation and GitHub Team subscription;
- exact organization slug;
- owner identities and 2FA;
- organization base permission;
- creation of the four empty private repositories;
- leaving repositories uninitialized;
- initial Actions-disabled state;
- ChatGPT GitHub App installation and selection of all four repositories;
- Local Codex/GitHub CLI authentication without pasting credentials into chat.

Do not assume the ChatGPT connector can perform organization billing, repository creation, ruleset administration, visibility changes or team administration. Verify actual capabilities before claiming automation.

Do not request passwords, recovery codes, tokens, private keys or secret values in chat.

The user will manage engineer/team access manually. Provide the exact least-privilege access matrix when they reach that step.

## 4. Empty-repository requirements

Before transfer, verify that every destination repository:

- belongs to the approved organization;
- is private;
- is empty;
- has no README, license, starter commit or imported history;
- has no unexpected refs;
- has GitHub Actions disabled;
- has not been populated through GitHub's repository importer.

If any repository is non-empty, stop before rewriting or deleting it and obtain the user's disposition.

## 5. Hybrid clean-root definition

For backend and frontend:

- create a new zero-parent root commit;
- its Git tree must be byte-for-byte identical to the accepted canonical application tree;
- do not mirror legacy refs or transfer complete history;
- record old commit/tree and new commit/tree mappings in governance;
- preserve complete legacy history separately through unchanged legacy repositories and verified encrypted bundles.

Controlling canonical application objects:

```text
BACKEND_CANONICAL_COMMIT =
1a99cf3f54df849408c8775b20e78c036ee637c3

BACKEND_CANONICAL_TREE =
9679ed60f186220716e948779d3d70dbbf89b39a

FRONTEND_CANONICAL_COMMIT =
edc6ff991a43ce3a93d2bd242d148a6fcbd104dc

FRONTEND_CANONICAL_TREE =
8b8a9c515afb04a69a962e4d786d7f398854ea9c

CANONICAL_AUTHORITY_COMMIT =
73d9cf8ecd0dd477d0b2d3d04fa1a3b17bfe427b
```

Revalidate all identities from immutable sources before using them. Do not treat this authorization file as a substitute for the controlling checkpoint record.

## 6. Two-part execution program

### Part 1 — canonical fast lane

Purpose: establish safe, private, canonical operational repositories and unblock the AWS worker.

Part 1 includes:

1. minimum current-canonical-tree secret scan;
2. immutable canonical identity verification;
3. empty-destination verification;
4. exact backend/frontend zero-parent root creation;
5. independent direct-URL fetch-back and tree equivalence;
6. schema blob, migration count and migration-subtree verification;
7. curated minimum governance root and machine-readable provenance manifest;
8. preservation manifests for paused Applicant Match and Campaign Reporting;
9. controlled initialization of the infrastructure repository;
10. separately identified minimum hardening commits for CI, CODEOWNERS, security guidance and repository documentation;
11. branch/tag protections and least-privilege Actions policy;
12. clean build/test/migration validation proportionate to exact tree equivalence;
13. immutable Part 1 evidence, rollback pointers and AWS source SHAs.

The exact equivalence root and hardened operational head are different checkpoints and must both be recorded.

AWS must use the hardened, validated operational SHA—not merely the import root.

Part 1 may proceed autonomously after the setup and safety gates pass. Do not return to the Parent chat for routine confirmations.

Stop and escalate only for:

- canonical identity mismatch;
- real secret exposure requiring authority/rotation;
- non-empty or conflicting destination repository;
- missing authority required to curate governance;
- destructive recovery requirement;
- unavailable authentication/permission that requires the user;
- Product, architecture, security or data-authority ambiguity.

### Part 2 — parallel legacy governance

After Part 1 publishes an immutable accepted handoff, Part 2 may continue without blocking AWS unless it discovers a live credential exposure or canonical-integrity defect.

Part 2 includes:

- complete history/log/artifact/release/attachment secret scan;
- owner/admin metadata export;
- read-only classification of the three dirty/damaged worktrees;
- full ref census and encrypted Git bundles;
- bundle checksum and isolated restore proof;
- legacy collaborator/app/webhook/deploy-key inventory;
- access-control rehearsal and onboarding/offboarding evidence;
- public-fork disposition record;
- legacy visibility, deploy-authority removal and later archival;
- retirement and rollback manifests.

Do not delete any legacy repository. Privatization or archival requires its own verified gate. The independently controlled public fork must remain recorded as continuing external exposure.

## 7. Governance curation boundary

Do not copy all of `dummy_tcs` into the new governance repository.

Initially include only an approved active allowlist such as:

- final canonical checkpoint and provenance;
- current Canonical Reconciliation Charter V2;
- current Local Codex operating standard;
- current Product/architecture authorities needed to interpret the canonical application;
- current Git governance/security/release policies;
- paused Applicant Match and Campaign Reporting registers;
- post-reconciliation delta/resumption pointers;
- current roadmap or successor charter that remains active.

Keep archive-only:

- one-time implementation prompts;
- intermediate circuit-breaker prompts and responses;
- superseded charters/registers;
- raw historical telemetry;
- validation/correction evidence not required for active interpretation;
- large browser artifacts, traces and screenshots.

Every copied governance file requires source repository, source commit, source tree, source path, source blob, target path and classification.

## 8. Non-destructive requirements

Throughout both parts:

- never reset, rewrite, delete or move a canonical legacy ref;
- never clean a dirty worktree without separate authorization;
- never mirror all refs into the operational repositories;
- never expose secret values in reports;
- never transfer runtime sessions, environment files, tokens, traces or generated credential artifacts;
- never resume Applicant Match or Campaign Reporting implementation;
- never deploy to AWS from an unaccepted commit;
- never delete `dummy_tcs` or either application legacy repository.

Use isolated migration workspaces and direct-URL fetch-back refs so mirror-style refspecs cannot move working refs.

## 9. Operating behavior

Apply the current incremental reconciliation charter's learned operating discipline:

- perform deep readiness preflight before expensive execution;
- inspect related failure patterns before rerunning broad validation;
- autonomously repair execution-owned runner, harness, fixture and publication defects within the authorized allowlist;
- escalate genuine authority or destructive-action boundaries only;
- replace unnecessary intermediate reporting with checkpoint-based reporting;
- record retries, reruns, circuit breakers and active/wall time without inventing unavailable precision.

Do not impose an untested universal retry limit. Any recovery budget must be prompt-specific and risk-based.

## 10. Immediate next action

Resume directly with the user and drive:

1. organization slug/plan/owner confirmation;
2. creation or safe automation of the four empty private repositories;
3. connector and Local Codex authentication readiness;
4. Part 1 execution-readiness preflight;
5. publication of a bounded Local Codex Part 1 prompt as a Git file;
6. autonomous Part 1 execution after its prerequisites pass.

Return to the canonical-reconciliation Parent chat only with:

- a true Parent decision/blocker;
- the immutable Part 1 AWS-handoff checkpoint;
- a material security finding;
- or final completion of the Git governance program.

```text
NEXT_BOUNDARY =
GIT_PARENT_DIRECT_USER_COORDINATION
THEN
PART_1_CANONICAL_FAST_LANE
```
