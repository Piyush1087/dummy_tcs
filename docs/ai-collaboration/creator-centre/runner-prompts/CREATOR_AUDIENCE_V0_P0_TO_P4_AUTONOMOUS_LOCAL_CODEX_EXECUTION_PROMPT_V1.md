# CREATOR AUDIENCE V0 — P0 TO P4 AUTONOMOUS LOCAL CODEX EXECUTION PROMPT V1

`CREATOR_AUDIENCE_V0_P0_TO_P4_AUTONOMOUS_LOCAL_CODEX_EXECUTION_PROMPT_V1`

You are the authorized external Local Codex execution runner for Creator Audience V0.

Execute the accepted five-packet program:

```text
P0 — exact convergence + executable contract freeze
→ P1 — shared Brand/Creator owner-scope foundation
→ P2 — Creator Audience authenticated backend vertical slice
→ P3 — Creator Insights/Audience frontend
→ P4 — consolidated integrated acceptance + immutable handoff
```

This prompt provides conditional continuation authority across successful packets. After each packet, perform a separate acceptance review against that packet's frozen gates, record the exact immutable checkpoint in the single Creator Audience execution ledger, publish normally, fetch independently, and continue only if every mandatory gate passes.

Do not require Parent relay between successful packets.

Stop immediately for:

```text
ACCEPTED CIRCUIT BREAKER
UNRESOLVED PRODUCT AMBIGUITY
MATERIAL AUTHORITY CONFLICT
FAILED MANDATORY PACKET GATE AFTER BOUNDED CORRECTION
```

Do not reinterpret Product decisions.

---

## 1. OPERATING TOPOLOGY

```text
CREATOR CENTRE SA
=
Product / Program authority

CREATOR INSTAGRAM INTELLIGENCE TECHNICAL SA
=
technical architecture
packet sequence
acceptance criteria
runner prompt authority

EXTERNAL LOCAL CODEX
=
local repositories
implementation
Docker/PostgreSQL
runtime/browser/Axe
normal authenticated Git publication
immutable evidence

PARENT
=
final authority
```

The execution runner may make bounded technical decisions necessary to implement the frozen Product and accepted architecture. It must not make new Product decisions or weaken acceptance gates.

---

## 2. FROZEN PRODUCT AUTHORITY

Repository:

```text
Piyush1087/dummy_tcs
```

Audience V0 Product decision register:

```text
docs/ai-collaboration/creator-centre/
CREATOR_INSIGHTS_AUDIENCE_V0_PRODUCT_DECISION_REGISTER.md

commit = 64bf4c454213277bb9fbe247e61bc78a203a227d
tree   = 0d69059b51877bb6e63e5ab4756188df967b5ace
```

Technical handoff:

```text
docs/ai-collaboration/creator-centre/
CREATOR_INSIGHTS_AUDIENCE_V0_TECHNICAL_HANDOFF.md

commit = 59e05b25e4ff2dea29d89026643bb4e21e0cef60
tree   = 5c75e26fda4cf77a1a6194cc7d9e75950415da5c
```

Parent authorization:

```text
CREATOR_AUDIENCE_V0_PARENT_REVIEW_AND_EXECUTION_AUTHORIZATION_V1
```

Frozen disposition:

```text
CREATOR_AUDIENCE_V0_PRODUCT = FROZEN
DONOR_REUSE_PREFLIGHT = ACCEPTED
FINITE_EXECUTION_PLAN = ACCEPTED_WITH_PARENT_PRODUCT_CLARIFICATIONS
IMPLEMENTATION = AUTHORIZED
```

---

## 3. ACCEPTED CURRENT APPLICATION AND DONOR CHECKPOINTS

Before any mutation, fetch/prune normally and verify every SHA/tree and worktree.

### Current Creator MVP application

Backend:

```text
repository = Piyush1087/creator-commerce-backend-v2-clone
branch     = freeze/mvp-canonical-application-v1
SHA        = 129b291ecbca4a1e79451215a81726000cfb5bff
tree       = 9fe6585addf8d8ccee9973b9b55cc0312c6e2b0b
migrations = 94
```

Frontend:

```text
repository = Piyush1087/creator-commerce-frontend-v2-clone
branch     = freeze/mvp-canonical-application-v1
SHA        = 6ea628b09817c4784a248658ddfc0202a13fbb00
tree       = 7de1702cda439c6f0d77000ee0c0c53f40a76c4d
```

Authority:

```text
repository = Piyush1087/dummy_tcs
branch     = freeze/mvp-canonical-application-v1
SHA        = 1748c97b65e8c57d5a640b262c1f9ec4f6c54170
tree       = f47e18b32157cc9924fa97bc6e5efe9f9fd540ac
```

### Accepted Brand Instagram donor

Backend:

```text
branch = program/instagram-intelligence-v1-backend
SHA    = fef32afb0fdef52f00c7c22b3d0a85967a68fded
tree   = 7ef6bd705b55ef82836b3f57f06a2832a867e781
migrations = 91
```

Frontend:

```text
branch = program/instagram-intelligence-v1-frontend
SHA    = 5866d0ac82f742957f53a8db2144a9a166628b75
tree   = 7c095290ffdef4c84f37382bc594bef536c92a49
```

Program authority:

```text
branch = program/instagram-intelligence-v1-authority
SHA    = b3f04918435db5043c5498ea92250762047291b1
tree   = 253ea239ffa2cdb9f1f87c546037c4a722512b46
```

Shared development ancestors recorded by the accepted preflight:

```text
backend  = 4c5f42858b950b7cd342f8972f99f548f3daa942
frontend = 323658d4b147b95b5629ff8d91fa90b8fe9077e4
```

The current MVP freeze and Instagram donor are parallel descendants. Neither may be silently discarded. The expected converged backend predecessor contains 98 migration directories: 87 common + 7 current-MVP-only + 4 donor-only. Prove this; do not assume it.

If any exact fetched checkpoint differs, any required ancestry is missing, or any worktree contains overlapping changes, stop before mutation with:

```text
CREATOR_AUDIENCE_STARTING_STATE_MISMATCH
```

Do not reset, clean, stash, discard, force checkout, or overwrite user work.

---

## 4. DEFINITIVE BRANCHES AND LEDGER

Use one implementation program and one ledger.

Preferred definitive branches:

```text
backend   = program/creator-audience-v0-backend
frontend  = program/creator-audience-v0-frontend
authority = program/creator-audience-v0-authority
```

If an identically named branch already exists, fetch it and prove it is either the exact authorized continuation or stop with a branch-authority conflict. Do not overwrite or rewrite it.

Authority artifacts belong under:

```text
docs/ai-collaboration/creator-centre/creator-audience-v0/
```

Create and maintain exactly one ledger:

```text
docs/ai-collaboration/creator-centre/creator-audience-v0/
EXECUTION_LEDGER.md
```

Record both the Creator Product/current-MVP authority lineage and the Brand Instagram donor lineage. Never substitute one for the other.

After every accepted packet:

1. commit backend/frontend/authority changes in logical order;
2. normal non-force push only;
3. fetch/prune independently;
4. resolve local and remote SHA/tree;
5. prove equality and accepted-predecessor ancestry;
6. prove all worktrees clean;
7. record the accepted packet SHA/tree chain before beginning the next packet.

No force push. No history rewrite. No merge to `development`. No deployment.

---

## 5. FROZEN CREATOR AUDIENCE V0 PRODUCT

### 5.1 Purpose and hierarchy

Creator Audience V0 is a read-only factual/deterministic understanding workspace.

Required visible hierarchy:

```text
Audience

source / freshness context

Audience Highlights
0–3 deterministic observations

Followers | Engaged
whole-section cohort switch

selected cohort:
- size where semantically valid
- age
- gender
- country
- city

Data status / limitations
```

Audience Highlights is Audience-level Intelligence. It is not owned by the selected cohort view. Selecting Followers or Engaged must not hide a valid cross-cohort highlight.

Highlights may use:

- Follower facts only;
- Engaged facts only; or
- compatible Follower-versus-Engaged facts.

When only one cohort is usable, Highlights may derive only from that cohort. Never manufacture Highlights to reach three.

### 5.2 Cohort availability

```text
USABLE COHORT
= at least one supported demographic dimension is available

PARTIAL COHORT
= usable, but one or more requested dimensions is unavailable

UNUSABLE COHORT
= no supported demographic dimension is available
```

Follower cohort selectable = YES only when at least one supported demographic dimension is usable.

An observed `followers_count` remains valid account-level context even when follower demographics are suppressed or unavailable. It does not make the demographic Follower cohort selectable by itself.

If both cohorts are usable, show one whole-section Followers/Engaged switch. If one is usable, render it directly without a one-option switch. If neither is usable, render the truthful state/recovery surface and no empty chart.

Never imply missing/suppressed/unavailable data is zero.

### 5.3 Distribution truth

```text
OBSERVED BUCKET COUNTS
= may be displayed when valid

PERCENTAGE SHARE
= display only when denominator validity is established

NO VALID DENOMINATOR
= count-based distribution only
```

Do not infer completeness from an incomplete or top-limited distribution. Existing 5-point notable and 10-point material comparison logic may be reused only when denominator, unit, breakdown, and timeframe bases are compatible.

### 5.4 Role authority

Add and use an explicit Audience/Insights read action:

```text
INSIGHTS_AUDIENCE_READ
```

Freeze:

```text
OWNER     = YES
MANAGER   = YES
ASSISTANT = YES
```

This is read-only access. Assistant access does not authorize Creator Settings, source lifecycle, refresh, purge, or any other mutation.

### 5.5 Settings and deletion

Creator Settings remains the canonical Instagram connection lifecycle authority.

Audience may project source state and deep-link to:

```text
/creator/settings/instagram
```

Audience must not implement connect, reconnect, disconnect, identity-conflict resolution, reauthorization, token mutation, permission mutation, delete-data UI, or delete-data endpoint.

Freeze:

```text
INTERNAL CREATOR-SOURCE PURGEABILITY = REQUIRED
NEW USER-FACING DELETE INSTAGRAM DATA ACTION/ENDPOINT/UI = NOT AUTHORIZED
```

### 5.6 Deferred or prohibited Product

Do not implement:

- Content or Creator Content & Insights;
- Persona/psychographics;
- Creator Brand;
- Portfolio;
- Commercial Setup;
- Media Kit or its visibility controls;
- semantic editing/manual override;
- Audience customization controls;
- Audience manual refresh;
- Campaign, Collaboration, AI Match, Marketplace, or Campaign Performance behavior;
- canonical Creator identity mutation;
- a Content placeholder or disabled Content tab.

---

## 6. ACCEPTED DONOR SEMANTICS TO PRESERVE

### Provider eligibility

- Instagram Professional account type must be `BUSINESS` or `CREATOR`; `PERSONAL` is rejected.
- `instagram_business_basic` supports basic connection capability.
- `instagram_business_manage_insights` supports audience insights.
- Fence exact provider account and authorization generation.
- Credentials remain Creator Settings-owned and are decrypted only inside the provider-read adapter.

### Audience reads

Followers:

```text
metric     = follower_demographics
period     = lifetime
timeframe  = this_month
breakdowns = age, city, country, gender
```

Engaged:

```text
metric     = engaged_audience_demographics
period     = lifetime
timeframe  = this_month
breakdowns = age, city, country, gender
```

This is a provider-defined current-month snapshot, not a rolling 30-day window. Label it truthfully. `capturedAt` is successful acquisition time, not a fabricated provider observation timestamp.

### Provider limitations

- Registry privacy threshold context: 100.
- Empty versus privacy-suppressed is not distinguishable; preserve `PROVIDER_EMPTY_OR_THRESHOLD_SUPPRESSED`.
- Accepted category cap: 45.
- A retained 45-row result carries `TOP_45_PROVIDER_LIMIT`.
- Do not present internal signal/cohort constants as provider thresholds.
- No model/LLM is required for Audience V0.

### Freshness/cadence

```text
initial connection processing = immediate
audience refresh              = weekly
dispatcher                    = hourly with due gates and deterministic jitter
stale threshold               = 8 days / 192 hours
manual Audience refresh       = none
```

Reuse one shared scheduler/coordinator. Do not create a second Creator scheduler.

### Lifecycle/current invariants

- Resource → Capture → successful completion with non-null authoritative `capturedAt` → Evidence.
- Current/generation publication uses shared verified runtime and CAS/transition semantics.
- Failure or unavailable execution never replaces valid current with empty/zero.
- Existing current remains visible while processing/freshness/limitations degrade truthfully.
- Exact replay is idempotent.

---

## 7. SHARED FOUNDATION AUTHORITY

The typed shared Brand/Creator owner-scope uplift is accepted.

Preserve:

```text
NO parallel Creator DE runtime
NO parallel Creator Intelligence current store
NO second scheduler
NO synthetic Brand identity
NO destructive or ambiguous Brand backfill
NO weakening of Brand/Offering/Creator tenant or subject isolation
```

The foundation must be reusable for future Creator Content/Insights without implementing those future workspaces now.

Preferred semantic model:

- one durable typed owner/scope identity with `BRAND` or `CREATOR`;
- Creator scope binds to canonical Owner `CreatorProfile` and, where authorization requires, canonical active Creator workspace;
- existing Brand rows backfill deterministically to Brand scopes;
- scope becomes the tenancy component of uniqueness, lineage, jobs, generation/current, and deletion;
- Offering remains a Brand-owned Intelligence subject;
- rows with neither or both Brand/Creator ownership are prohibited;
- internal Creator source purge is exact and cross-domain safe.

The physical schema strategy is a P1 technical decision after Prisma/PostgreSQL review. It must satisfy the semantic rules above.

---

## 8. PACKET EXECUTION AND CONDITIONAL ACCEPTANCE RULE

For each packet:

1. reconcile applicable `AGENTS.md`, engineering, database, validation, AI integration, design, and authority instructions;
2. inspect current accepted code before designing changes;
3. make only packet-scoped changes;
4. run changed-surface tests plus mandatory packet gates;
5. inspect the diff and reject accidental scope;
6. produce a packet evidence artifact;
7. perform a separate reviewer pass against every frozen acceptance criterion;
8. if and only if all gates pass, record `P<n> = ACCEPTED`, commit, push, fetch back, prove equality, and continue;
9. if a gate fails, use at most two bounded technical correction cycles within the same packet and same root cause;
10. if still failing, or if scope/Product/authority expands, stop and return the exact blocker.

This conditional rule is the Technical SA's pre-authorized acceptance logic. It does not authorize the runner to invent Product behavior.

---

## 9. P0 — EXACT CONVERGENCE + EXECUTABLE CONTRACT FREEZE

### Purpose

Create one authoritative Creator Audience predecessor from both accepted lineages, without implementing Audience functionality.

### Required work

- Create the definitive backend/frontend/authority branches from current MVP freeze checkpoints.
- Converge the exact accepted Instagram donor commits using normal Git history.
- Resolve only bounded mechanical conflicts whose semantics are already fixed. A semantic conflict stops P0.
- Preserve current Creator Home/Settings/Team and the full accepted Instagram donor implementation.
- Establish the single Creator Audience ledger and record every input SHA/tree.
- Prove the expected 98 pre-feature migrations with no duplicate migration identity/order conflict.
- Freeze executable versions for:
  - typed owner-scope invariant;
  - Creator Audience Object/component ownership;
  - normalization and deterministic Highlights algorithm;
  - strict consumer DTO;
  - authenticated route;
  - explicit role action/matrix;
  - Settings projection/non-mutation boundary;
  - internal purge boundary.
- Archive this runner prompt in the authority repository.

### Required evidence

- exact Git merge-base/ancestry and merge commit(s);
- changed/conflict file inventory;
- migration inventory and duplicate-name/hash scan;
- clean PostgreSQL application of all 98 migrations;
- upgrade proof from each relevant accepted predecessor where technically meaningful;
- Prisma generate/validate;
- backend production build;
- frontend install/typecheck/build;
- focused convergence regressions for Creator Home/Settings and Brand Instagram;
- diff, secret, forbidden-artifact checks;
- normal push and independent fetch-back equality.

### P0 acceptance

```text
P0_ACCEPTED = YES
```

only if both authority lineages are present, migration/runtime convergence is clean, no Product semantic conflict exists, and the executable contracts reflect the frozen clarifications.

Stop with `P0_AUTHORITY_CONVERGENCE_CONFLICT` if not.

---

## 10. P1 — SHARED BRAND/CREATOR OWNER-SCOPE FOUNDATION

### Purpose

Extend existing DE, Evidence, shared Intelligence generation/current, and Instagram sync ownership to real Creator subjects while keeping Brand behavior unchanged.

### Required work

- Implement typed Brand/Creator owner scope and reviewed migration(s).
- Deterministically backfill existing Brand-owned DE, Evidence, Observation, Intelligence, and sync rows.
- Generalize shared repositories and ports around a discriminated owner scope.
- Extend exact scope ownership through Resource, Capture, artifact, capability execution, Evidence, Observation/support/relation/freshness/provider link, Intelligence subject/execution/attempt/action/Object/component generation/current/candidate/transition/Evidence references, and Instagram sync jobs wherever the actual schema requires.
- Preserve Offering as Brand-owned.
- Add CreatorProfile/workspace deletion and internal source-purge constraints/traversal.
- Preserve Settings disconnect as distinct from deletion.
- Reuse/extract scheduler ownership; do not create a second scheduler.
- Add architecture tests preventing Brand and Creator Settings adapters from importing or mutating each other's canonical lifecycle.

### Mandatory migration proof

- exact P0 migration base;
- clean apply to disposable PostgreSQL;
- production-shaped populated upgrade from P0;
- deterministic Brand backfill with before/after row counts and identity/hash/current preservation;
- no ambiguous or nullable ownership after migration;
- cross-scope FK, uniqueness, tenant, subject, account, and generation rejection;
- rollback/recovery review appropriate to the actual migration, without destructive execution against non-task data.

### Mandatory full Brand regression

Because P1 changes shared persistence ownership, run the complete affected Brand suite, including:

- Owned Website DE/Evidence paths;
- Brand Intelligence subject/current/candidate/generation transitions;
- Offering Intelligence ownership;
- Brand Instagram B1–C4 and accepted later foundations;
- scheduler/cadence/lease/retry/failure preservation;
- Settings delete-data and cross-Brand preservation;
- consumer/API behavior;
- exact replay/idempotency.

Also prove Creator scope insert/read/delete without any live provider call.

### P1 acceptance

Accept only if the shared runtime remains one runtime, all Brand semantics remain equivalent, Creator ownership is first-class, and every isolation/backfill gate passes.

Stop with:

```text
CREATOR_AUDIENCE_SHARED_SCOPE_CIRCUIT_BREAKER
```

if implementation requires a parallel DE/current/scheduler, synthetic Brand, destructive/ambiguous backfill, Brand processor rewrite, or weakened isolation.

---

## 11. P2 — CREATOR AUDIENCE AUTHENTICATED BACKEND VERTICAL SLICE

### Purpose

Complete Creator Instagram Audience from Settings-managed source through shared current to strict authenticated consumer.

### Creator subject resolution

Server-side only:

```text
authenticated actor
→ active Creator workspace membership
→ canonical workspace
→ Owner CreatorProfile subject
→ exact CreatorSocialIntegration(INSTAGRAM)
```

No caller-supplied Creator, owner, provider account, or generation selector.

### Required implementation

- Add a Creator `CreatorSocialIntegration` credential/fence adapter; reuse the stateless accepted Instagram provider client.
- Reuse accepted follower/engaged calls and four breakdowns.
- Persist Creator-scoped Resource/Capture/Evidence with exact subject/account/generation/capturedAt lineage.
- Preserve per-dimension and per-cohort availability, suppression, failure, partiality, and limitations.
- Normalize `COUNT`, total, completeness, and denominator metadata only when source truth proves it.
- Reuse C2 arithmetic and compatible 5/10 point rules.
- Add a versioned deterministic Creator Audience Object/output bundle in shared Intelligence runtime/current.
- Add deterministic Audience Highlights capped at three; no model call.
- Keep Highlights audience-level and independent of selected cohort.
- Preserve valid current on later failure.
- Integrate immediate initial processing and shared weekly schedule/hourly dispatcher/jitter; no Audience manual refresh.
- Add internal exact Creator source purgeability callable from Settings later, without a public mutation.
- Add strict authenticated consumer:

```text
GET /api/v1/creator/insights/audience
```

Recommended narrow DTO semantics:

```text
contractVersion
generatedAt
status = READY | PARTIAL | UNAVAILABLE
creator role/context projection
source = INSTAGRAM
sourceStatus
snapshotBasis = lifetime + this_month + capture timestamp
defaultCohort = FOLLOWERS | ENGAGED | null
highlights[0..3]
cohorts[]
  id
  availability
  size where semantically valid
  dimensions[]
    AGE | GENDER | COUNTRY | CITY
    state
    valid bucket counts
    percentage only when denominatorValid
    coverage/limitation
freshness
processingState
currentPreserved
limitations[]
settingsRecoveryRoute
```

The public API must not expose Evidence IDs, internal hashes, raw provider payloads, prompts, candidates, leases, tokens, signed locators, or mutation controls.

### Mandatory fixtures

- both cohorts complete;
- both usable but partial;
- Followers only;
- Engaged only;
- neither;
- follower count observed while follower demographics suppressed;
- privacy/empty suppression;
- top-45 limitation;
- provider failure;
- observed follower zero versus unavailable;
- invalid denominator with count-only output;
- valid denominator and compatible 5/10-point comparisons;
- incompatible comparison basis;
- expired/disconnected/partial/unknown authorization;
- changed provider account and stale generation;
- subsequent failure with previous current preserved;
- Owner/Manager/Assistant read allowed;
- inactive/no-workspace/cross-Creator denied;
- internal exact purge with other Creator/Brand/website preservation.

### Mandatory evidence

- clean PostgreSQL migration status;
- exact Integration → Resource → Capture → Evidence → Object/component generation → current → consumer lineage;
- non-null authoritative `capturedAt` before Evidence;
- idempotent replay/stable row counts;
- current preservation;
- scheduler due-gate/cadence proof;
- authorization, tenant, account, generation, and subject isolation;
- backend build/scoped lint/diff/secret scans;
- no live Meta/model calls.

### P2 acceptance

Accept only if every fixture is truthful and the authenticated consumer is complete for V0. Stop if a new Product decision, new Settings mutation, or parallel runtime is required.

---

## 12. P3 — CREATOR INSIGHTS / AUDIENCE FRONTEND

### Purpose

Mount the frozen Audience V0 in the active Creator application shell.

### Required route/navigation

```text
/creator/insights/audience
```

`/creator/insights` may redirect to the Audience route.

Add `Insights` as a durable active Creator primary destination. Do not revive legacy/unmounted `src/features/creator-centre/**` as Product authority. Do not add a Content placeholder.

Preserve current Creator Home, Campaigns, Collaborations, and Settings.

### Required frontend behavior

- strict Zod response parsing;
- existing authenticated fetch and no-store behavior;
- last-good-data preservation;
- Aurora components/tokens/focus/reduced-motion patterns;
- source/freshness context near the top;
- Audience Highlights before cohort selection/content;
- whole-section Followers/Engaged switch only when both usable;
- direct single-cohort view when only one usable;
- selected cohort size only when semantically valid;
- age, gender, country, city dimensions independently shown/hidden by truth state;
- count-only rendering when denominator invalid;
- percentage rendering only when valid;
- Data status/limitations last;
- Settings recovery deep link only.

Required states:

- loading;
- ready;
- partial;
- stale/preserved current;
- one usable cohort;
- no usable cohort;
- follower count present with demographics suppressed;
- disconnected;
- reauth required;
- partial/unknown capability;
- provider failure;
- malformed contract;
- retryable UI read error.

Do not expose edit, customization, Media Kit controls, manual refresh, provider/pipeline details, or one-option platform/cohort controls.

### Responsive/accessibility proof

Run authenticated proofs at:

```text
390px
767px
768px
desktop
```

Prove no clipping, hidden content, navigation collision, or horizontal overflow. Prove keyboard operation, visible focus, correct landmark/tab semantics, and Axe serious/critical = 0 for the changed route/states.

### P3 acceptance

Accept only if the rendered hierarchy exactly matches frozen Product, roles/routes are correct, all truth states are represented without fabrication, and targeted browser/accessibility gates pass.

---

## 13. P4 — CONSOLIDATED INTEGRATED ACCEPTANCE + HANDOFF

### Purpose

Accept and document the first full frontend/backend Creator Centre Intelligence slice.

### Required integrated gates

- exact accepted P0→P3 SHA/tree chain and clean worktrees;
- clean database apply and production-shaped converged-predecessor upgrade;
- backend boot plus `/health/live` and `/health`;
- authenticated fixture provider → DE → Evidence → deterministic Audience Object → shared current → API → UI;
- complete cohort/missingness/denominator/highlight matrix;
- initial immediate processing, weekly cadence, hourly due gates/jitter, 8-day stale policy;
- failure-current preservation end to end;
- Owner, Manager, Assistant Audience read allowed and mutation authority unchanged;
- inactive/cross-workspace/cross-Creator isolation;
- provider account and authorization-generation fencing;
- internal Creator source purge with other Creator, Brand, Offering, and website preservation;
- no public delete-data endpoint/UI;
- 390/767/768/desktop browser matrix;
- keyboard/focus and Axe serious/critical = 0;
- backend and frontend production builds;
- changed/affected test suites;
- diff, dependency, schema/migration, secret, raw-provider, and forbidden-artifact scans;
- no live Graph or model requirement.

### Final authority artifacts

Create:

```text
P4_FINAL_EVIDENCE.md
CREATOR_AUDIENCE_V0_DEVELOPER_HANDOFF.md
CREATOR_AUDIENCE_V0_IMPLEMENTATION_LEARNING_REPORT.md
```

The implementation-learning report must distinguish:

- Product decisions validated by implementation;
- shared donor code reused byte-identically;
- shared code generalized by owner scope;
- Creator-specific additions;
- unexpected complexity or debt;
- reusable seams for future Creator Content/Insights;
- explicit deferred Product/build scope;
- recommendations for the Creator Product SA, without designing the next workspace.

### Publication

Commit and normally push all final backend/frontend/authority checkpoints. Independently fetch back and prove SHA/tree equality and ancestry. All worktrees must be clean.

Do not merge to `development`. Do not deploy.

### P4 acceptance

Only after all gates pass record:

```text
P0 = ACCEPTED
P1 = ACCEPTED
P2 = ACCEPTED
P3 = ACCEPTED
P4 = ACCEPTED
CREATOR_AUDIENCE_V0_IMPLEMENTATION = ACCEPTED
```

---

## 14. TEST EXECUTION MODEL

```text
PACKET
= changed-surface unit/contract/architecture tests
+ focused PostgreSQL where persistence changes
+ production build/typecheck
+ scoped lint/format
+ diff/secret/forbidden-artifact integrity

P1 WAVE GATE
= full affected Brand regression

P4 FINAL GATE
= full Creator Audience runtime/browser/accessibility acceptance
```

Do not rerun unrelated expensive suites at every packet. Do not waive migration, tenant isolation, missingness, denominator, current-preservation, deletion, or accessibility gates.

Use existing Docker/PostgreSQL/browser/repository tooling. Repository-scoped dependencies may be added only if strictly necessary and justified. Do not silently install or materially reconfigure system-wide software. If mandatory local infrastructure is absent, stop with `LOCAL_ENVIRONMENT_PREREQUISITE_REQUIRED` and name the exact prerequisite.

No live credentials are required. If existing local credentials happen to be available, do not use them in this program unless separately authorized. Never emit secret values.

---

## 15. GLOBAL PROHIBITIONS

Do not:

- merge to `development`;
- deploy to AWS or production;
- force push or rewrite history;
- reset, clean, stash, discard, or overwrite unrelated work;
- weaken existing Evidence/current/tenant/security invariants;
- create a Creator-only DE runtime/current store/scheduler;
- use a synthetic Brand;
- duplicate Creator Settings lifecycle mutations;
- expose provider payloads or credentials;
- fabricate percentages, zeros, completeness, concentration, or highlights;
- use an LLM/model for Audience V0;
- implement Content, Creator Brand, Portfolio, Commercial Setup, Media Kit, Campaign, Collaboration, AI Match, Marketplace, Persona, or editing;
- change Product hierarchy to fit donor code;
- treat legacy/unmounted Creator Centre components as current Product authority.

---

## 16. CIRCUIT BREAKERS

Stop and preserve the last accepted checkpoint if:

- exact starting authority cannot be reconstructed;
- convergence produces a material semantic or migration conflict;
- Creator subject support requires a parallel runtime or synthetic Brand;
- migration/backfill is destructive or ambiguous;
- Brand/Offering/Creator isolation would weaken;
- existing Brand Intelligence/Instagram behavior cannot be preserved;
- provider behavior materially contradicts the frozen Product/donor contract;
- denominators cannot be established but an accepted contract would require percentages;
- Creator workspace/Owner subject resolution is ambiguous;
- a new Settings mutation or canonical domain becomes necessary;
- active shell integration requires Product IA reinterpretation;
- live Meta permission/config mutation becomes mandatory;
- a later Creator workspace becomes a dependency.

Return the exact state and the smallest decision needed. Do not route around a circuit breaker.

---

## 17. IMMUTABLE RETURN CONTRACT

Return one report:

```text
CREATOR_AUDIENCE_V0_P0_TO_P4_EXTERNAL_LOCAL_CODEX_REPORT_V1
```

It must include:

### A. Starting authority

- all repository/branch SHA/tree values;
- local/fetched equality;
- worktree cleanliness;
- ancestry and migration inventory.

### B. P0 report

- convergence method and commits;
- conflict inventory/resolution;
- 98-migration proof;
- frozen executable contracts;
- tests/builds;
- accepted SHA/tree and publication equality;
- correction accounting.

### C. P1 report

- exact schema/owner-scope design;
- migrations and hashes;
- Brand backfill counts/identity preservation;
- repository/runtime changes;
- Creator scope smoke/isolation/delete;
- complete affected Brand regression;
- accepted SHA/tree and publication equality;
- correction accounting.

### D. P2 report

- Creator subject/credential/fence adapter;
- provider/capture/Evidence/Object/current/consumer lineage;
- all fixture truth states;
- Highlights algorithm/version and outputs;
- cadence/failure-current/idempotency/isolation/purge proof;
- PostgreSQL row counts;
- route/role tests;
- accepted SHA/tree and publication equality;
- correction accounting.

### E. P3 report

- exact frontend changed files;
- route/navigation hierarchy;
- strict parser/client/hook/state matrix;
- responsive/keyboard/Axe results;
- builds/tests;
- accepted SHA/tree and publication equality;
- correction accounting.

### F. P4 report

- integrated environment and commands;
- end-to-end lineage;
- full state/role/isolation/cadence/deletion matrix;
- browser/accessibility matrix;
- secret/forbidden-artifact scan;
- final evidence, developer handoff, learning report paths;
- final backend/frontend/authority SHA/tree and independent fetch-back equality;
- clean worktree/runtime cleanup;
- no merge/deployment proof.

### G. Scope proof

- no Content or other Creator workspace started;
- no user-facing delete-data or manual refresh;
- no live Graph/model calls;
- no raw provider/media persisted;
- no development merge;
- no deployment.

### H. Final disposition

On full success, end exactly:

```text
RESULT =
READY_FOR_CREATOR_AUDIENCE_V0_FINAL_ACCEPTANCE_AND_PRODUCT_LEARNING_REVIEW

P0 = ACCEPTED
P1 = ACCEPTED
P2 = ACCEPTED
P3 = ACCEPTED
P4_EVIDENCE_READY = YES

CREATOR_AUDIENCE_V0_IMPLEMENTATION_ACCEPTED =
NO — AWAITING CREATOR TECHNICAL SA / PARENT FINAL REVIEW

CONTENT = NOT_STARTED
CREATOR_BRAND = NOT_STARTED
PORTFOLIO = NOT_STARTED
COMMERCIAL_SETUP = NOT_STARTED
MEDIA_KIT = NOT_STARTED

DEVELOPMENT_MERGE = NOT_PERFORMED
DEPLOYMENT = NOT_PERFORMED

LIVE_GRAPH_CALLS = NONE
LIVE_MODEL_CALLS = NONE
META_PROVIDER_MUTATIONS = NONE
RAW_PROVIDER_DATA_EXPOSED = NO

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
FOR CREATOR_AUDIENCE_V0 FINAL ACCEPTANCE
AND PRODUCT LEARNING REVIEW
```

On a stopped run, instead return the accepted packet chain, exact failing gate/root cause, worktree/runtime state, corrections used, and the smallest required Parent decision. Do not claim later packets started or accepted.

