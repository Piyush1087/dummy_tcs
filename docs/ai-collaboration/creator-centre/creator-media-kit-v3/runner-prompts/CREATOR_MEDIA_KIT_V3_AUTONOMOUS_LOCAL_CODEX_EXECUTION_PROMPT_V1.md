# CREATOR MEDIA KIT V3 — AUTONOMOUS LOCAL CODEX EXECUTION PROMPT V1

`CREATOR_MEDIA_KIT_V3_AUTONOMOUS_LOCAL_CODEX_EXECUTION_V1`

## 0. Assignment and operating topology

You are the External Local Codex implementation runner for Creator Media Kit V3.

Operate under this topology:

```text
PARENT / CREATOR ORCHESTRATOR
= final Product and program authority

CREATOR INSTAGRAM INTELLIGENCE TECHNICAL SA
= architecture, implementation orchestration and technical acceptance authority

EXTERNAL LOCAL CODEX
= bounded implementation runner
```

This prompt authorizes one autonomous implementation run through:

```text
P0 — authority, legacy and executable-contract convergence
→ P1 — canonical composition, lifecycle and persistence
→ P2 — public/verified projections, instrumentation and PDF
→ P3 — Creator editor, previews and public/verified frontend
→ P4 — integrated acceptance, publication and handoff
```

Proceed from one packet to the next only after that packet's mandatory gates pass. Do not require Parent relay between successful packets. Preserve an immutable checkpoint and single execution ledger for every packet.

Stop and return an immutable circuit-breaker report only when a defined circuit breaker in this prompt occurs. Ordinary implementation, fixture, build, test, accessibility, browser-harness and bounded integration defects are correction work inside this run.

Do not merge to `development`. Do not deploy.

---

## 1. Exact Product authority

Use only this active Media Kit Product authority:

```text
Repository:
Piyush1087/dummy_tcs

Path:
docs/ai-collaboration/creator-centre/
CREATOR_MEDIA_KIT_V0_PRODUCT_DECISION_REGISTER_V3.md

Publishing commit:
d74184568d64155e0fab22739210805f954f01e6

Git blob:
414c32c014f0a1ac1a9416fff3770cc452fe93f8
```

V3 supersedes Media Kit V1 and V2. Any Media Kit composition note is non-authoritative reference material only. Do not infer Product authority from moving `main`, chat history, legacy code, old routes, or donor behavior.

The Parent's instruction to proceed with Media Kit implementation, together with this prompt, is the explicit implementation authorization that the Product register itself does not provide.

### 1.1 Frozen Product summary

The register is complete authority. This summary is only an execution guardrail:

```text
ONE canonical Media Kit per Creator

LIFECYCLE = DRAFT / LIVE

ONE stable unlisted link
NO expiring link
NO per-Brand link
NO secret access token
NO indexing/search discovery

PUBLIC SHELL =
Creator identity
+ bounded explicitly selected public-safe Instagram visuals, up to 3
+ Work with Creator CTA
+ Reveal Email ID CTA

VERIFIED BRAND LAYER =
Creator
+ Audience
+ Content & Performance
+ Portfolio
+ Work With Me / Rate Card
+ current Availability
+ CTAs
+ PDF download

UPSTREAM TRUTH = consumed, never copied as new truth or edited by Media Kit

PDF = on-demand point-in-time Verified Brand presentation
NOT a quote, contract, live document or commercial lock

WORK WITH CREATOR AUTHORITY =
render CTA
→ accept and instrument CTA click
→ hand control to separately governed downstream entry point
→ END MEDIA KIT AUTHORITY

POST-CLICK ROUTE / SIGNUP / AUTH / VERIFICATION / ONBOARDING /
INTELLIGENCE TRIGGERS / DESTINATION / C04 ENTRY / ENQUIRY CREATION
= EXCLUDED
```

### 1.2 Parent execution clarification — Creator roles

Apply the established Creator-owned configuration policy consistently:

```text
OWNER
= read, configure, curate, publish/unpublish, preview, copy link, download PDF

MANAGER
= read, configure, curate, publish/unpublish, preview, copy link, download PDF

ASSISTANT
= read and preview only
= no configuration, curation, publish/unpublish or other mutation
```

Use explicit Media Kit actions in the shared Creator Team authorization vocabulary. Do not infer these permissions from Settings, Campaign, Brand Centre or unrelated actions.

This clarification does not authorize an Assistant to reveal or mutate any Settings-owned identity, source lifecycle or private operational state.

---

## 2. Exact accepted starting checkpoints

The direct predecessor is accepted Creator Portfolio V3.

Before any mutation, fetch/prune normally and independently verify repository identity, exact local/fetched SHA and tree equality, clean worktrees and required ancestry.

```text
BACKEND_REPOSITORY =
Piyush1087/creator-commerce-backend-v2-clone

BACKEND_BRANCH =
program/creator-portfolio-v3-backend

BACKEND_SHA =
aaae1e3062137eda30e13e2fd7bcddbb1b239842

BACKEND_TREE =
458517fa01a50f43f0b1cd8a948767e4f7600d50
```

```text
FRONTEND_REPOSITORY =
Piyush1087/creator-commerce-frontend-v2-clone

FRONTEND_BRANCH =
program/creator-portfolio-v3-frontend

FRONTEND_SHA =
9631e8b261f02f331dcf84f09dfd61dcc28eea83

FRONTEND_TREE =
25a89b63cec1a576ef605076c717a7cc447e5acc
```

```text
AUTHORITY_REPOSITORY =
Piyush1087/dummy_tcs

AUTHORITY_BRANCH =
program/creator-portfolio-v3-authority

AUTHORITY_SHA =
6697ba5d4fc99beb3ace86869fa2ec146ce7483e

AUTHORITY_TREE =
292b4e2e2fa6b8d5a27a812bba17eb9f2c9e8864
```

```text
MIGRATION_COUNT = 105

MIGRATION_HEAD =
the accepted Portfolio V3 additive migration at the direct predecessor
```

Do not guess the head name or checksum. Resolve and record the exact accepted migration inventory and SHA-256 before mutation.

### 2.1 Required inherited producer history

The Portfolio predecessor report states that Audience V1 and inherited Content, Creator Brand and Commercial histories are preserved. Independently verify that the direct predecessor contains the required accepted histories and executable producer contracts.

At minimum reconcile these accepted checkpoints:

| Producer | Backend | Frontend | Authority |
|---|---|---|---|
| Creator Content V0 | `5f7712108886305b40cbcf1c0d89f5ecdc22ab24` | `7edd26d3cdad0ec84083884b34039952368a1295` | `5df3bff917dd294f3f86642809c66558e37a3b8c` |
| Creator Brand V0 | `6206f43c6a13c304c971b810e1dd99a20aaaa11f` | `c505c0679e39effdd9608e319112591d5ae4c079` | accepted Creator Brand authority lineage ending at or descending from `08c72433b32ed8a199d29ae8875d668dc2f0eddf` |
| Commercial Setup + Rate Card V0 | `3504a3cc8f0dc684431b73046f5796f157708f68` | `e6e7ae8ea9f5f98f882f52230e4cae163bda1e89` | `897026bb39e7390397b9fea48a696ee488c31a4c` |
| Creator Audience V1 | `6f000bd9364f48d338ae88cec78dcfed0f4fd522` | `36d75f4eceded7e9e35aaa280b544164d086ca05` | `a403a9eae752a4201b263f3e1fb518e0a115e329` |
| Creator Portfolio V3 | direct predecessor above | direct predecessor above | direct predecessor above |

If an exact implementation commit is represented through a normal merge rather than direct ancestry, prove the merge and tree equivalence. Do not replay or cherry-pick accepted implementation blindly.

### 2.2 Starting-state failure

If any exact direct predecessor SHA/tree, clean-worktree, remote identity or required accepted ancestry check fails:

```text
RESULT = MEDIA_KIT_V3_STARTING_STATE_MISMATCH
```

Stop. Do not reset, clean, stash, discard, reconstruct or overwrite user work.

---

## 3. Branches and authority convergence

Create fresh task worktrees/branches only after Section 2 passes:

```text
program/creator-media-kit-v3-backend
program/creator-media-kit-v3-frontend
program/creator-media-kit-v3-authority
```

Base backend and frontend on the accepted Portfolio V3 checkpoints.

For authority, normally converge:

```text
accepted Portfolio V3 authority lineage
+ exact Media Kit V3 Product authority
+ this exact runner prompt
```

Preserve all parents and ancestry. Do not flatten, rebase, rewrite, reset or substitute moving `main`. If a normal convergence has a material authority conflict, stop with:

```text
MEDIA_KIT_V3_AUTHORITY_CONFLICT
```

Archive this exact prompt under:

```text
docs/ai-collaboration/creator-centre/creator-media-kit-v3/runner-prompts/
CREATOR_MEDIA_KIT_V3_AUTONOMOUS_LOCAL_CODEX_EXECUTION_PROMPT_V1.md
```

Prove normalized prompt source/archive byte equality and SHA-256 equality.

---

## 4. Mandatory standards and donor reconciliation

Before implementation, read and reconcile:

- all applicable repository `AGENTS.md` files;
- `AI_ENGINEERING_STANDARD.md`;
- backend, database, validation, AI-integration, frontend, design-system, responsive and accessibility standards;
- the exact Media Kit V3 Product register;
- the accepted final developer and Product-learning handoffs for Creator Audience V1, Content V0, Creator Brand V0, Commercial Setup/Rate Card V0 and Portfolio V3;
- the Creator Team actor/authorization contracts;
- Settings-owned business email projection and source lifecycle boundaries;
- canonical Brand identity and verification contracts;
- current Creator shell/navigation contracts;
- existing Media Kit, public profile, PDF, telemetry and share-link implementations, including all mounted legacy routes and callers.

Use donor behavior only when it is semantically compatible with V3. Legacy Media Kit code is not Product authority.

Classify every relevant donor surface as:

```text
REUSE
REUSE_WITH_ADAPTATION
REPLACE_BEHIND_SAFE_CUTOVER
PRESERVE_UNTOUCHED
DEFER
PROHIBITED
```

Do not create a generic profile/page-builder platform, second Creator shell, second owner-scoped runtime, new Intelligence engine, new scheduler, new Settings lifecycle, new Brand verification workflow or new downstream enquiry system.

---

## 5. Non-negotiable ownership boundaries

### 5.1 Upstream truth

Media Kit may store only its own:

- lifecycle and stable-link identity;
- coarse section visibility;
- public-thumbnail selection/order;
- Portfolio featured-item selection/order;
- presentation/composition revisions;
- bounded instrumentation records if no accepted shared telemetry donor exists.

It must not copy upstream fields into a second editable truth store or expose upstream editing.

### 5.2 Settings — Creator

Settings owns:

- designated business/contact email identity;
- Instagram/source connection lifecycle;
- `Delete my Instagram data`;
- `Delete my data`;
- Creator roles and Settings operational controls.

Do not add either delete action, endpoint or UI in this run.

Preserve the accepted future deletion boundary:

```text
Delete my Instagram data
= Settings-owned deletion of Instagram base/source data
+ preservation of derived Creator Centre Intelligence and Creator Centre state

Delete my data
= separately governed Settings-owned full-user deletion
```

Media Kit must neither redefine nor invoke those future actions. Record both as Settings-owned external/deferred debt where appropriate.

### 5.3 Brand verification

Media Kit may consume only a canonical, server-resolved assertion that the authenticated viewer belongs to a verified Brand.

Do not implement or modify:

- Brand signup;
- Brand authentication architecture;
- identity/domain verification;
- onboarding;
- Gatekeeper, Data Extraction or Intelligence triggers;
- a client-claimed `verified=true` or caller-supplied Brand selector.

### 5.4 Work with Creator

Media Kit owns only rendering and accepting the click plus best-effort instrumentation inside its boundary.

Do not choose, build, route, redirect, deep-link or test a downstream signup/C04/enquiry destination. A test may prove that the boundary event is emitted and that no downstream internal network call, mutation, enquiry or Collaboration row occurs.

If a frontend needs a terminal representation before a separately governed destination exists, use an inert, explicitly bounded handoff seam that performs no downstream Product action. Do not invent a destination.

### 5.5 Reveal Email ID

Use the Settings-owned designated business/contact email projection. Do not duplicate or edit it.

The reveal interaction is public, enabled by default, requires no Brand authentication, unlocks no verified commercial content, creates no C04/enquiry record and cannot be disabled by the Creator.

### 5.6 Campaign/C04/transactions

Media Kit does not own quote, agreement, Application, Campaign, Collaboration, payment, payout, KYC, shipping, usage-rights or transactional truth.

---

## 6. P0 — authority, legacy and executable-contract convergence

P0 is a real implementation packet, not a documentation-only preflight.

### 6.1 Required audit

Inventory and prove:

1. every currently mounted legacy Media Kit/public-profile backend route;
2. every frontend route and caller;
3. legacy persistence fields, especially any `isMediaKitPublic`, theme, arbitrary rate or cached-profile state;
4. whether any legacy record is currently public by default or by prior state;
5. current Brand verification assertion and exact tenant/Brand/actor fence;
6. current Settings business-email read boundary;
7. current upstream consumer contracts and publishability/freshness states;
8. PDF donors, including existing `jsPDF`/print utilities and their security/Unicode/memory limits;
9. telemetry donors and their failure behavior;
10. secure media/image primitives and the accepted Portfolio source-link/no-image fallback.

### 6.2 Executable contracts

Implement strict, versioned Media Kit contracts for at least:

- lifecycle `DRAFT | LIVE`;
- stable public identity/link;
- Creator configuration/revision/audit;
- public-shell projection;
- verified-Brand projection;
- Creator Public/Verified preview projections;
- coarse Audience, Content and Rate Card visibility;
- public visual selection, maximum 3;
- Portfolio selection/order, bounded 4–6 when used, without manufacturing a minimum when fewer eligible items exist;
- Availability projection only for verified Brand;
- Settings email reveal;
- CTA events and terminal boundary;
- PDF snapshot contract;
- missing, unavailable, stale, partial and last-good semantics;
- strict role actions.

Freeze route ownership and a safe legacy cutover strategy.

### 6.3 Fail-closed legacy rule

All new V3 Kits begin `DRAFT`.

Never translate legacy `isMediaKitPublic=true` or other old public flags into V3 `LIVE` automatically. Never guess visibility, curation or verified-layer eligibility.

Preserve legacy storage until mounted callers and public exposure are fully inventoried. Cut over reads/writes safely. Do not destructively drop legacy data in this run unless an additive, reviewed compatibility migration makes the removal provably safe; removal is not expected.

### 6.4 P0 gates

Required:

- exact authority and donor inventory;
- strict contract/unit tests;
- legacy public-exposure tests;
- Brand-verification adapter contract tests;
- role-policy tests;
- build, scoped lint/format and diff integrity;
- secret/forbidden-artifact scan;
- immutable P0 artifact and ledger checkpoint.

Do not proceed if canonical verified-Brand admission cannot be implemented without changing Brand verification Product, or if legacy public state cannot fail closed without ambiguous exposure/destructive backfill.

---

## 7. P1 — canonical composition, lifecycle and persistence

### 7.1 Persistence

Add only the smallest additive Media Kit-owned schema required for:

- exactly one Kit per canonical Creator;
- DRAFT/LIVE lifecycle;
- one stable non-secret public identifier/link;
- monotonic revision/CAS;
- idempotent actor-bound mutations;
- immutable configuration/lifecycle audit history;
- coarse section visibility;
- public visual selection/order;
- Portfolio featured selection/order;
- publish/unpublish timestamps and actor attribution where appropriate.

Do not persist provider credentials, signed/ephemeral media URLs, raw media, raw Intelligence payloads, raw Evidence, private Settings state or copied upstream commercial truth.

All migration changes must be additive, reviewed and reversible/forward-repairable. Do not backfill guessed values. Existing users remain DRAFT.

### 7.2 Composition service

Compose current publishable views from accepted upstream owners:

- Creator Brand identity/style;
- Audience V1 compact publishable facts;
- Content V0 compact themes/performance/representative facts;
- Portfolio V3 eligible individual items;
- Commercial Setup V2 `Based in`, Availability and allowed work-preference presentation;
- Rate Card V2 current valid rates/terms/currency;
- Settings business email.

Composition must be server-owned, tenant/Creator fenced and projection-minimized.

Requirements:

- no missing/unavailable value becomes zero;
- no stale/failed upstream execution publishes an empty replacement over a valid last-good upstream current;
- hidden/unavailable sections collapse;
- a Kit may publish with any valid subset, including no Portfolio, insufficient Audience, hidden Content or hidden Rate Card;
- no completeness score;
- no top-level reordering;
- upstream updates resolve live for a LIVE Kit without copying or mutating downloaded PDFs;
- Portfolio items removed or made ineligible upstream disappear safely from the composed output without deleting Portfolio truth;
- public visuals and Portfolio features remain separate selection domains.

### 7.3 Authenticated Creator API

Implement the bounded Creator management surface needed for:

- read current configuration and composed preview states;
- mutate visibility/selection/order with CAS and idempotency;
- Publish/Unpublish;
- copy-link data;
- Public/Verified preview;
- Creator PDF download request.

Use server-resolved canonical Creator subject. No caller-selected Creator ID.

### 7.4 P1 PostgreSQL proof

Use disposable PostgreSQL 17 and prove:

- clean application of all migrations;
- populated accepted predecessor → new head upgrade;
- all predecessor table counts and deterministic digests preserved;
- DRAFT default and no accidental public exposure;
- one-Kit uniqueness;
- revision/CAS/idempotency/concurrency;
- role/tenant/Creator isolation;
- source-independent manual Media Kit configuration;
- publish/unpublish lifecycle;
- last-good composition behavior;
- exact target-only cleanup for test-owned Media Kit aggregate without broadening Settings deletion;
- other Creator and all upstream truth survive.

Publish P1 evidence and proceed only if all mandatory gates pass.

---

## 8. P2 — public/verified projections, instrumentation and PDF

### 8.1 Public shell API

Implement a strict unauthenticated LIVE-only projection that returns only:

- Creator name;
- avatar when safely publishable;
- Instagram handle;
- bounded positioning/bio/style presentation allowed by Creator Brand;
- up to 3 explicitly selected public-safe Instagram static visuals when a safe publishable asset exists;
- source destination for each shown visual;
- the two fixed CTA labels/subtexts.

Requirements:

- DRAFT/unpublished/nonexistent Kit fails closed without leaking existence/private state;
- no Audience, performance, Portfolio, Rate Card, Availability, work preferences, private Settings state, Evidence, confidence, readiness, provider payload or operational state;
- add unlisted/noindex protections at HTTP and page levels;
- no signed/ephemeral locator or unsafe redirect target;
- no player, embed, autoplay or internal preview.

If a public-safe static asset is unavailable, show a truthful no-image state or omit that visual. Do not make V0 dependent on new durable provider-media storage.

### 8.2 Verified Brand API

Implement a separate authenticated, server-authorized projection for a LIVE Kit.

Admission requires:

- authenticated actor;
- active canonical Brand membership/context;
- canonical verified-Brand assertion;
- exact tenant/Brand/actor fencing.

Do not accept a caller Brand selector or verification claim.

The response may contain only Product-authorized composed fields. It must exclude all private/internal fields listed in Product Section 28.

Availability is informational only. It cannot gate access, hide either CTA or alter the CTA boundary.

### 8.3 Instrumentation

Instrument at minimum:

```text
KIT_VIEW
PUBLIC_THUMBNAIL_SOURCE_OPENED
WORK_WITH_CREATOR_CLICK
REVEAL_EMAIL_ID_CLICK
MEDIA_KIT_PDF_DOWNLOAD
```

Preserve Kit/link attribution and recognized Brand identity where applicable, without leaking it to public clients.

Instrumentation must be best effort and must not block viewing, links, CTA clicks, email reveal or PDF delivery.

`WORK_WITH_CREATOR_CLICK` is terminal. Prove it creates no signup, onboarding, Intelligence, C04, Campaign, enquiry or Collaboration mutation and makes no downstream internal call.

### 8.4 Reveal Email ID

The public reveal endpoint/interaction reads the Settings-owned designated business email through a bounded non-secret projection. It does not copy or allow editing the email.

No Creator toggle. No Brand auth requirement. No commercial-content unlock.

If the designated email is unavailable, return a truthful unavailable state without exposing another private identity or manufacturing an address.

### 8.5 PDF

Implement on-demand PDF generation for:

- Creator downloading current Verified preview from DRAFT or LIVE;
- verified Brand downloading a LIVE Kit.

Anonymous/public shell cannot download it.

Prefer an accepted existing PDF/runtime donor when it can satisfy all gates. Do not add a broad new document platform. A narrowly scoped dependency change is allowed only if no accepted donor can safely satisfy V3 and the change is bounded, reviewed, locked and justified in evidence; otherwise stop for scope expansion.

PDF requirements:

- point-in-time only;
- current Verified Brand composition only;
- `Generated on <date>`;
- source-derived performance retains `Observed as of <date>`;
- canonical Creator currency and `Starting from` wording;
- standard platform Rate Card conditions;
- static images/covers only when safely obtainable;
- truthful no-image fallback plus readable/clickable source link;
- no video/player/carousel interaction;
- no hosted public PDF URL, archive, version manager, per-Brand customization or durable generated-file record;
- bounded memory/size/time and remote-media acquisition;
- no provider credentials, signed URLs or private fields;
- generation failure does not alter or block the LIVE Kit;
- a PDF may link back to the live Kit, but cannot bypass live access rules.

Downloaded bytes are portable and are not revoked or mutated later.

### 8.6 P2 gates

Prove with focused tests and PostgreSQL/API round trips:

- anonymous public minimization;
- DRAFT/nonexistent fail-closed behavior;
- verified admission and cross-Brand denial;
- unverified/authenticated/public distinctions;
- Settings email projection;
- both CTA boundaries;
- instrumentation nonblocking behavior;
- PDF actor matrix, content minimization, dates, links, currency, media fallback and non-persistence;
- no downstream CTA traffic/mutations;
- no raw/signed/private leakage.

Publish P2 evidence and proceed only if all mandatory gates pass.

---

## 9. P3 — frontend, previews and responsive experience

### 9.1 Creator workspace

Implement the Media Kit workspace within the existing Creator Centre shell and navigation system. Do not create a second shell or navigation framework.

Required Creator experience:

- DRAFT/LIVE status;
- Publish/Unpublish;
- stable Copy link;
- Public view and Verified Brand view previews;
- coarse Audience, Content and Rate Card SHOW/HIDE;
- Portfolio SHOW/HIDE and explicit 4–6 item feature/order control, bounded by eligible inventory;
- public visual selection/order up to 3;
- current composed read-only upstream facts;
- Creator PDF download;
- truthful loading, unavailable, partial, stale, conflict and retry states;
- no completeness score;
- no arbitrary themes, colors, fonts, layouts or top-level reordering;
- no upstream field editing.

Owner/Manager may mutate. Assistant may read and preview but mutation controls must be absent/disabled consistently and backend remains authoritative.

### 9.2 Public page

Implement a directly addressable unlisted public Media Kit route with:

- noindex protections;
- fixed responsive hierarchy;
- public-only fields;
- static media/source links;
- fixed `Work with Creator` and `Reveal Email ID` interactions;
- no commercial-section flash during loading/hydration/error;
- no downstream route invented for `Work with Creator`.

### 9.3 Verified Brand view

Render verified content only after the verified API authorizes it. Do not infer verified state client-side or hydrate private data into public markup.

Preserve:

```text
Creator
Audience
Content & Performance
Portfolio
Work With Me / Rate Card
Availability
CTAs
```

Hidden/unavailable sections collapse. Availability remains informational and does not disable CTAs.

### 9.4 Static media and links

Reuse accepted safe presentation assets where available. Otherwise use the Product-authorized no-image/source-link fallback.

All external links require exact safe URL validation and `noopener/noreferrer` behavior. No scraping, internal preview, player, embed, autoplay, raw provider URL persistence or remote-content execution.

### 9.5 P3 gates

Required focused frontend tests:

- strict DTO parsers and rejection of extra/private fields;
- Creator client and role behavior;
- lifecycle, controls, preview modes and mutation conflicts;
- public/verified separation and no private flash;
- email reveal and CTA click boundary;
- PDF access/download states;
- selection/order bounds;
- no upstream editing;
- regression for Creator Audience, Content, Brand, Commercial, Portfolio, shell, sidebar/mobile drawer and five-item bottom navigation;
- production typecheck/build;
- scoped ESLint/Prettier/diff integrity.

Publish P3 evidence and proceed only if mandatory gates pass.

---

## 10. P4 — integrated acceptance and closeout

Run one integrated acceptance against a fresh disposable PostgreSQL 17 route and production backend/frontend builds.

### 10.1 Migration and boot

Prove:

- clean application of every migration;
- migration status current;
- populated accepted predecessor → final head preservation;
- exact migration count, names and SHA-256 inventory;
- production backend boot;
- `/health/live` HTTP 200;
- `/health` HTTP 200 with PostgreSQL up.

### 10.2 Real API/database/UI lineage

Using real password/JWT sessions and production routes, prove:

```text
accepted upstream Creator truth
→ Media Kit composition
→ DRAFT configuration
→ public and verified Creator previews
→ Publish
→ LIVE public shell
→ verified Brand projection
→ PDF snapshot
```

No production auth bypass or direct database insertion of canonical/current rows is allowed outside clearly bounded fixture setup through accepted test builders.

### 10.3 Actor/access matrix

At minimum prove:

- Creator Owner;
- Creator Manager;
- Creator Assistant;
- inactive/missing Creator actor;
- anonymous public visitor;
- authenticated unverified Brand actor;
- authenticated verified Brand Owner/member consistent with canonical Brand policy;
- foreign Brand/tenant;
- direct URL substitution and caller-supplied subject attempts.

Expected:

- public LIVE shell is anonymously available and minimized;
- DRAFT is not publicly exposed;
- only canonical verified Brand actors receive commercial projection/PDF;
- Creator preview does not change public lifecycle;
- Creator roles follow Section 1.2;
- no cross-Creator/Brand/tenant leakage.

### 10.4 State and failure matrix

Prove:

- DRAFT, LIVE, Publish, Unpublish, copy link;
- no valid upstream section;
- partial/stale/unavailable upstream sections;
- valid last-good producer current during later failure;
- hidden Audience/Content/Portfolio/Rate Card;
- fewer than four eligible Portfolio items;
- selected Portfolio item removed upstream;
- no safe image/cover;
- no Settings business email;
- instrumentation sink failure;
- PDF generation/media failure;
- Rate Card currency/visibility and standard conditions;
- Availability states without CTA filtration;
- no missing metric becomes zero;
- no downloaded PDF mutation/revocation claim.

### 10.5 CTA boundary proof

The browser/API acceptance must explicitly show:

```text
WORK_WITH_CREATOR_CLICK
= event accepted best effort
+ Media Kit boundary terminates
+ no redirect/destination Product assertion
+ no signup/onboarding/verification mutation
+ no Data Extraction/Intelligence trigger
+ no C04/Campaign/Collaboration/enquiry row or call
```

The implementation must not make downstream availability part of Media Kit acceptance.

### 10.6 Browser/accessibility matrix

Run production API-to-UI browser proof at:

```text
390
767
768
1440
```

Cover Creator Owner/Manager/Assistant, public LIVE, DRAFT fail-closed, unverified Brand and verified Brand views.

Required:

- keyboard-only operation;
- visible focus and correct return;
- modal/drawer/menu behavior;
- semantic headings, labels and status announcements;
- no horizontal overflow;
- zero serious/critical Axe violations;
- zero lesser Axe violations unless an inherited, independently reproduced baseline is explicitly accepted by authority;
- zero uncaught page errors and no unexpected console errors;
- external traffic blocked except deliberately exercised safe source-link navigation in an isolated assertion;
- no private commercial-content flash in public/unverified states.

### 10.7 Regression scope

Run changed-surface and shared-surface regressions for:

- Creator Audience V1;
- Creator Content V0;
- Creator Brand V0;
- Commercial Setup/Rate Card V0;
- Portfolio V3;
- Creator Team policies;
- Settings email projection;
- Brand verification/read admission;
- Creator shell/navigation;
- legacy Media Kit/public-profile cutover;
- secure URL/media primitives;
- PDF donor/runtime.

Do not run unrelated expensive suites unless shared-surface changes make them necessary. If a shared authorization, routing, migration or public-access surface changes materially, run the corresponding full regression.

---

## 11. Security, privacy and data-minimization gates

Mandatory throughout:

- strict tenant, Creator, Brand, actor and role fencing;
- server-owned subject resolution;
- no client-claimed Brand verification;
- no credentials, tokens or Settings operational state in DTOs;
- no raw provider payload, raw Evidence, raw confidence, prompts or reasoning;
- no signed/ephemeral media locators;
- no raw media persistence;
- bounded SSRF-safe media acquisition only where already authorized;
- no accidental public legacy state;
- no public PDF endpoint;
- no secret link treated as authorization;
- no public commercial data in HTML, hydration, logs, errors or telemetry;
- no cache mixing public and verified projections;
- safe HTTP cache headers by projection;
- no public indexing;
- rate limiting/throttling consistent with accepted public endpoints;
- audit fields do not expose secret or private payloads;
- no tracked `.env`, database volume, browser auth state, PDF output, screenshot, runtime log, raw image or temporary media.

Scan source, diffs, staged contents, planned commits and authority artifacts before publication.

---

## 12. Explicitly prohibited work

Do not implement or change:

- post-click `Work with Creator` destination;
- signup, login, Brand verification, onboarding or Gatekeeper flows;
- Data Extraction or Intelligence triggers from the CTA;
- C04 entry, enquiry creation, Campaign or Collaboration behavior;
- Agency onboarding;
- multiple/per-Brand Media Kits, multiple links or custom domains;
- public commercial sections or public PDF download;
- custom page builder, templates, fonts, arbitrary colors/CSS or section order;
- field-level Audience, Content or Rate Card visibility;
- Media Kit completeness score or Creator analytics dashboard;
- automatic Portfolio publication;
- project/case-study Portfolio abstraction;
- video player, autoplay, embeds, full carousel or internal preview;
- Creator-controlled email reveal toggle;
- CTA filtering by geography, preferences, Rate Card or Availability;
- manual FX/viewer-currency conversion;
- arbitrary legal T&C editing;
- upstream Creator Brand/Audience/Content/Portfolio/Commercial editing;
- Settings `Delete my Instagram data` or `Delete my data`;
- KYC, payout, shipping or payment execution;
- Story/provider/OAuth/permission expansion;
- live Graph/model calls unless separately authorized (none are required);
- dependency upgrades unrelated to a narrowly justified PDF requirement;
- merge to `development` or deployment.

---

## 13. Circuit breakers

Stop and return an immutable report if any of these occurs:

1. exact predecessor identity/cleanliness/ancestry cannot be proven;
2. Product and accepted implementation authority cannot be normally converged with all required ancestors;
3. a required upstream producer is not actually present/accepted in the Portfolio predecessor;
4. legacy public Media Kit state cannot be moved to fail-closed V3 DRAFT without ambiguous exposure, destructive backfill or silent public carryover;
5. canonical verified-Brand admission cannot be consumed without changing Brand verification Product/onboarding;
6. public and verified projections cannot be isolated without raw/private data leakage;
7. PDF requires a broad new runtime/platform, durable public archive, unsafe remote fetch, raw/signed locator persistence or Product expansion;
8. safe source-link/no-image fallback cannot satisfy V3 without provider/OAuth expansion;
9. implementation requires a second Creator shell, owner-scoped runtime, scheduler or upstream truth store;
10. implementation crosses the `Work with Creator` click boundary;
11. a schema/runtime change materially outside Media Kit-owned lifecycle/composition/instrumentation is required;
12. an unresolved Product ambiguity materially changes user-visible behavior or commercial/security semantics.

Return the exact blocker and evidence. Do not weaken a gate or invent Product behavior.

---

## 14. Publication discipline

For every accepted packet:

1. preserve exact changed-file inventory and statistics;
2. record commands and results;
3. update the single Media Kit execution ledger;
4. publish the packet evidence artifact;
5. commit in dependency order;
6. normal non-force push;
7. independent fetch/prune;
8. prove local/fetched SHA and tree equality plus accepted ancestry;
9. prove clean worktrees.

Never force push.

At P4 publish:

```text
docs/ai-collaboration/creator-centre/creator-media-kit-v3/
  EXECUTION_LEDGER.md
  P0_AUTHORITY_LEGACY_AND_EXECUTABLE_CONTRACT_CONVERGENCE.md
  P1_CANONICAL_COMPOSITION_LIFECYCLE_AND_PERSISTENCE.md
  P2_PUBLIC_VERIFIED_PROJECTIONS_INSTRUMENTATION_AND_PDF.md
  P3_CREATOR_EDITOR_PREVIEWS_AND_PUBLIC_FRONTEND.md
  P4_INTEGRATED_ACCEPTANCE_AND_CLOSEOUT.md
  CREATOR_MEDIA_KIT_V3_DEVELOPER_HANDOFF.md
  CREATOR_MEDIA_KIT_V3_FINAL_ACCEPTANCE_AND_PRODUCT_LEARNING_HANDOFF_V1.md
  runner-prompts/CREATOR_MEDIA_KIT_V3_AUTONOMOUS_LOCAL_CODEX_EXECUTION_PROMPT_V1.md
```

Do not create a second ledger.

---

## 15. Final cleanup

Stop all task-owned backend/frontend/browser processes. Remove only exact verified task-owned PostgreSQL containers/volumes, synthetic environment files, downloaded PDFs, screenshots, browser state and temporary media.

Do not delete developer databases, shared dependencies, user work or unrelated containers.

If runner policy blocks deletion of an exact task-owned ignored build/dependency directory, classify and disclose it. It may be a nonblocking local hygiene exception only when:

- path is inside the exact task root;
- it is ignored/untracked and absent from every commit;
- it contains no secret, database, raw media, auth state or runtime credential;
- no process depends on it;
- publication trees are clean and unaffected.

---

## 16. Required immutable return contract

Return exactly:

```text
CREATOR_MEDIA_KIT_V3_AUTONOMOUS_EXTERNAL_LOCAL_CODEX_REPORT_V1
```

Include at minimum:

```text
A. exact starting SHA/tree/worktree/remote verification
B. Product, standards and authority reconciliation
C. authority convergence and accepted producer ancestry
D. legacy route/data/caller inventory and cutover decision
E. exact changed-file inventory and packet statistics
F. P0 executable contracts and role policy proof
G. canonical Media Kit schema/migration review
H. DRAFT/LIVE, stable-link, revision/CAS/idempotency proof
I. live upstream composition and non-copy ownership proof
J. public-shell data-minimization proof
K. verified-Brand admission and isolation proof
L. Audience/Content/Portfolio/Commercial/Rate Card projection proof
M. Availability informational/non-filtering proof
N. Settings business-email reveal proof
O. Work-with-Creator terminal click-boundary proof
P. instrumentation and nonblocking-failure proof
Q. PDF architecture, access, content and failure proof
R. Creator API and role matrix
S. frontend workspace/public/verified implementation
T. responsive/browser/Axe/keyboard/overflow evidence
U. PostgreSQL version, migrations and populated-upgrade proof
V. row counts, lineage and cross-tenant isolation
W. upstream failure/last-good/section-collapse proof
X. legacy public-exposure regression
Y. upstream and shell regression matrix
Z. build/typecheck/lint/format/diff results
AA. dependency/schema/migration identity and changes
AB. secret/raw-media/locator/private-data/forbidden-artifact scan
AC. live-call and provider-mutation counts
AD. backend publication/fetch-back SHA/tree
AE. frontend publication/fetch-back SHA/tree
AF. authority publication/fetch-back SHA/tree
AG. prompt archive digest equality
AH. cleanup and final worktree/runtime state
AI. primary-run/correction accounting per packet
AJ. deferred/external owner debts
AK. exact blocker, if any
```

The final status block must be:

```text
RESULT =
READY_FOR_CREATOR_TECHNICAL_SA_PROGRAM_ORCHESTRATOR_FINAL_REVIEW

P0 = ACCEPTED
P1 = ACCEPTED
P2 = ACCEPTED
P3 = ACCEPTED
P4_EVIDENCE_READY = YES

CREATOR_MEDIA_KIT_V3_EVIDENCE_READY = YES

CREATOR_AUDIENCE_V1 = ACCEPTED_PREDECESSOR_PRESERVED
CREATOR_CONTENT_V0 = ACCEPTED_PREDECESSOR_PRESERVED
CREATOR_BRAND_V0 = ACCEPTED_PREDECESSOR_PRESERVED
CREATOR_COMMERCIAL_SETUP_AND_RATE_CARD_V0 = ACCEPTED_PREDECESSOR_PRESERVED
CREATOR_PORTFOLIO_V3 = ACCEPTED_PREDECESSOR_PRESERVED

WORK_WITH_CREATOR_POST_CLICK_JOURNEY = EXCLUDED
SETTINGS_CREATOR_INSTAGRAM_SOURCE_DELETE_RECONCILIATION = DEFERRED_TO_SETTINGS
SETTINGS_CREATOR_DELETE_MY_DATA = DEFERRED_TO_SETTINGS

LIVE_GRAPH_CALLS = NONE
LIVE_MODEL_CALLS = NONE
META_PROVIDER_MUTATIONS = NONE
RAW_MEDIA_PERSISTED = NO
PUBLIC_PDF_ARCHIVE_CREATED = NO

DEVELOPMENT_MERGE = NOT_PERFORMED
DEPLOYMENT = NOT_PERFORMED

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CREATOR INSTAGRAM INTELLIGENCE TECHNICAL SA /
PROGRAM ORCHESTRATOR FOR FINAL REVIEW
```

If stopped, replace the status values with exact completed/not-started states and the precise circuit breaker. Never self-declare final Product acceptance.

---

## 17. Final execution instruction

Begin with exact starting-state verification. Preserve all accepted producer implementations and ownership boundaries. Execute autonomously through P0→P4 only while mandatory gates pass.

The objective is a complete, secure and truthful Creator Media Kit V3 implementation—not a legacy profile reskin, not a new Intelligence engine and not a downstream enquiry journey.
