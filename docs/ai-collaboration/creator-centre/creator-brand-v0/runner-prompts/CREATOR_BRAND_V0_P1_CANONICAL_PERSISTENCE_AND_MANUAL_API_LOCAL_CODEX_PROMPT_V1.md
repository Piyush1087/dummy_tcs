# Creator Brand V0 — P1 Canonical Persistence + Manual API — Local Codex Prompt V1

`CREATOR_BRAND_V0_P1_CANONICAL_PERSISTENCE_AND_MANUAL_API_LOCAL_CODEX_PROMPT_V1`

You are the **External Local Codex execution runner**. The Parent is mechanically relaying this exact prompt. You have no Product authority and must not self-accept P1.

Execute only:

```text
P1 — CANONICAL CONFIRMED-TRUTH PERSISTENCE
     + EXPLICIT ROLE ACTIONS
     + MANUAL BACKEND VERTICAL SLICE
```

Do not start P2, P3 or P4. Return one immutable report for Technical-SA / Program-Orchestrator review.

---

## 1. Technical-SA P0 adjudication

The Technical SA reviewed the published P0 code, contracts, authority convergence and repository identities.

Canonical disposition:

```text
P0 = ACCEPTED
P0_CORRECTION_CYCLES_USED = 0
P1 = AUTHORIZED_FOR_EXTERNAL_LOCAL_CODEX
```

The runner's only P0 exception was inability to delete a task-created ignored backend `dist` directory and task-local dependency junction because local execution policy rejected deletion. This is accepted as **non-blocking local-runner hygiene**, because:

- both artifacts were ignored and absent from every published tree;
- all tracked worktrees were clean;
- no database, service, port, container, media, locator or credential remained;
- package, lockfile, schema and migration identities were unchanged;
- local/fetched SHA and tree equality passed.

At P1 start, use fresh/isolated worktrees from the exact published checkpoints below. Do not reuse the old P0 worktree or its ignored artifacts. Do not delete or traverse the old dependency target. Record this P0 acceptance and exception in the next normal Creator Brand authority-ledger update.

---

## 2. Operating topology

```text
PARENT / CREATOR CENTRE SA
= Product and final authority

CREATOR INSTAGRAM INTELLIGENCE TECHNICAL SA
= technical architecture, packet sequencing and acceptance

YOU — EXTERNAL LOCAL CODEX
= local repository implementation, PostgreSQL, tests,
  Git publication and immutable evidence

PARENT
= mechanical prompt/report relay
```

Do not ask the Parent to reinterpret frozen Product. Stop only for a defined circuit breaker.

---

## 3. Exact starting checkpoints

Fetch/prune normally and verify exact repository identities, local/fetched equality, ancestry and clean worktrees before mutation.

### Backend

```text
repository = Piyush1087/creator-commerce-backend-v2-clone
branch = program/creator-brand-v0-backend
SHA = 3a919c2a9a8263c4734df87bda3ed21e092420cf
tree = fc888dd548ef85246438327588c07bfe835d9e83
parent = 0fa145ac6a021337929e87b9eb9e0c67ebc82b7e
```

### Frontend

```text
repository = Piyush1087/creator-commerce-frontend-v2-clone
branch = program/creator-brand-v0-frontend
SHA = 7edd26d3cdad0ec84083884b34039952368a1295
tree = 0cc596ca1ef1d1c4a51857125547de156e487c8b
```

### Program authority

```text
repository = Piyush1087/dummy_tcs
branch = program/creator-brand-v0-authority
SHA = 3ea60c9faef4fdb292a1c18c490b249227560dc7
tree = 00c565634da85ae608a2fa3782f6cbdcf172bf1d
```

Verify both authority ancestors:

```text
Parent Product authorization = 7e54d3980ca3454263600e44766041dbb58e0a06
Corrected Content authority = cb9ee23118eafb2dc156c25eb09702ea93252db4
```

### Database predecessor

```text
migration count = 101
migration head = 20260914191000_creator_audience_owner_scope_lineage
```

Use fresh isolated task worktrees. If any exact identity differs, or overlapping user work exists, stop before mutation. Do not reset, clean, stash, discard or overwrite it.

---

## 4. Authority to read before implementation

Read all applicable `AGENTS.md`, engineering/backend/database/validation standards, and these exact authority artifacts:

```text
docs/ai-collaboration/creator-centre/CREATOR_BRAND_V0_PRODUCT_DECISION_REGISTER_V2.md
docs/ai-collaboration/creator-centre/CREATOR_BRAND_V0_TECHNICAL_HANDOFF_AND_ROLE_COMPREHENSION_AUDIT_V2.md
docs/ai-collaboration/creator-centre/CREATOR_BRAND_V0_PRODUCT_AMENDMENT_1_TAXONOMIES_AND_BOUNDS.md
docs/ai-collaboration/creator-centre/creator-brand-v0/CREATOR_BRAND_V0_PARENT_REVIEW_AND_AUTONOMOUS_EXECUTION_AUTHORIZATION_V1.md
docs/ai-collaboration/creator-centre/creator-brand-v0/P0_AUTHORITY_CONVERGENCE_AND_EXECUTABLE_CONTRACTS.md
docs/ai-collaboration/creator-centre/creator-brand-v0/EXECUTION_LEDGER.md
```

Inspect current canonical Creator Workspace actor resolution, Team policy, CreatorProfile, CreatorSocialIntegration, Creator Settings lifecycle/purge, Audience/Content authenticated consumer patterns, idempotency/CAS/revision donors and Prisma migration conventions.

Do not reopen P0 contract decisions.

---

## 5. P1 canonical ownership boundary

Implement:

```text
CANONICAL CONFIRMED CREATOR BRAND TRUTH
= CreatorBrandProfile
  + immutable CreatorBrandRevision history
```

Do not store confirmed Creator Brand truth in shared Intelligence current, Creator Content, CreatorProfile legacy fields, Media Kit fields, CreatorSocialIntegration or Settings.

Manual creation/editing must work when:

```text
Instagram integration = absent
Content current = absent
semantic/model adapters = unavailable
provider refresh = failed
```

P1 contains no suggestion generation or confirmation runtime.

---

## 6. Authorized migration 102

Exactly one additive migration after the accepted 101-migration predecessor is authorized.

Expected result:

```text
migration count = 102
new persistence = CreatorBrandProfile + CreatorBrandRevision
```

Use repository naming conventions. The schema must establish, at minimum:

- exactly one canonical Creator Brand profile per exact Creator Workspace / canonical Owner CreatorProfile pair;
- explicit Creator Workspace and Owner CreatorProfile foreign keys with a composite integrity relationship where compatible with the existing workspace invariant;
- current revision number or exact current-revision reference;
- current validated Creator Brand snapshot;
- immutable append-only revision snapshots;
- server-authenticated actor user and actor membership provenance;
- mutation origin constrained to `MANUAL | SUGGESTION_USED | SUGGESTION_EDITED` for future compatibility;
- idempotency identity scoped to the canonical profile/subject and mutation;
- revision uniqueness and strict monotonic predecessor relationship enforced by transactional runtime and database constraints where proportionate;
- nullable server-verified suggestion-generation/component/candidate references for future P2, never client-authored truth;
- created/updated timestamps;
- deletion behavior that preserves historical integrity and never cascades from Instagram source deletion.

The current snapshot may use bounded JSON only if strict runtime parsing plus database identity/revision constraints preserve the accepted aggregate. Prefer the smallest repository-consistent design; do not normalize taxonomies into new tables.

Database defense must reject structurally invalid canonical data as far as practical without duplicating the entire Zod contract in SQL. Runtime validation remains mandatory before and after mutation.

No legacy backfill is authorized. Existing Creators begin with no Creator Brand profile and can create it manually.

Do not add a second migration. If a second migration or materially broader schema is required, stop with:

```text
CREATOR_BRAND_P1_SCHEMA_SCOPE_EXPANSION_REQUIRED
```

---

## 7. Explicit Creator Brand actions

Extend the canonical Creator Workspace action union/policy using the P0 identifiers:

```text
CREATOR_BRAND_READ
CREATOR_BRAND_EDIT
CREATOR_BRAND_CONFIRM_SUGGESTION
```

Freeze enforcement:

```text
OWNER     READ YES / EDIT YES / CONFIRM YES
MANAGER   READ YES / EDIT YES / CONFIRM YES
ASSISTANT READ YES / EDIT NO  / CONFIRM NO
```

P1 uses READ and EDIT. CONFIRM must be registered/policy-correct for P2 but no confirmation endpoint is implemented in P1.

Reuse `CreatorWorkspaceActorService` and server-resolved active membership. The caller must not select CreatorProfile, owner user, owner scope or workspace identity through request parameters/body. Preserve existing workspace-selection behavior; do not weaken it.

Assistant access authorizes only read. It does not authorize Settings/source/refresh or any Creator Brand mutation.

---

## 8. Manual read/mutation API

Implement the smallest authenticated Creator Brand backend consumer/mutation surface consistent with existing Creator Centre routing and guards.

Preferred route unless current conventions prove a more exact equivalent:

```text
GET /api/v1/creator/brand
PUT /api/v1/creator/brand
```

If repository route conventions require a different technical path, document the exact reason without changing the frontend Product route `/creator/brand`.

### GET

Return a strict, versioned DTO containing only:

- read-only projected identity;
- canonical profile snapshot or explicit unconfigured state;
- current revision;
- authenticated actor role and allowed Creator Brand actions/capabilities;
- truthful manual-first/source-independent context needed by later frontend;
- suggestions absent/not implemented, without fake placeholders that imply processing.

No raw database shape, provider payload, token, locator, Intelligence internals or caller-controlled owner identifiers.

### Projected identity

Use canonical owners and document precedence:

```text
creator name = canonical CreatorProfile display identity
avatar = canonical CreatorProfile avatar projection
primary Instagram handle = current Settings-owned connected CreatorSocialIntegration handle when available
```

If current repository authority proves a safer canonical avatar projection, use it and document the reason. Do not expose encrypted token fields or create mutation endpoints for name/avatar/handle. When no connected Instagram handle exists, return explicit null/absence rather than a stale or fabricated identity.

### PUT

Use the strict P0 manual mutation contract:

- full validated snapshot;
- `expectedRevision`;
- UUID idempotency key;
- manual intent only in P1;
- server-resolved actor/subject;
- no projected identity fields;
- no client suggestion provenance.

Expected revision rules:

```text
create = expectedRevision 0 → revision 1
update = exact current revision N → revision N+1
```

Use one transaction with serialized subject/profile locking or an equivalent proven CAS strategy. Concurrent commands against the same expected revision must produce at most one new canonical revision.

Idempotency:

- exact replay of the same authorized command returns the same committed result with no extra revision;
- reuse of the same idempotency key with materially different input is rejected;
- idempotency is isolated by exact canonical profile/subject;
- rejected/failed commands do not reserve false success.

Validate both before persistence and after reconstructing the stored result.

Use stable bounded domain errors mapped through existing HTTP/error conventions. Do not leak row existence across tenants.

---

## 9. Source lifecycle and historical-domain boundaries

Prove:

```text
Instagram disconnect/provider failure/source unavailable
→ confirmed CreatorBrandProfile retained
→ CreatorBrandRevision retained
```

```text
Creator Settings Instagram delete-data purge
→ existing source DE/Evidence/derived Creator Intelligence purge behavior preserved
→ CreatorBrandProfile retained
→ CreatorBrandRevision retained
```

P1 must not add a user-facing delete-data action or modify Settings lifecycle semantics.

Creator Brand changes must not rewrite historical Applications, Campaigns or Collaborations, and must not mutate Portfolio, Media Kit, Commercial Setup, payouts/KYC or CreatorProfile identity fields.

---

## 10. Required PostgreSQL proof

Use disposable PostgreSQL 17 through the accepted local Docker route.

Prove both:

### Clean apply

```text
empty database
→ all 102 migrations apply
→ Prisma migration status current
```

### Upgrade

```text
apply exact accepted 101 migrations
→ seed representative Creator Workspace/Team/Profile/Settings/Audience/Content and unrelated Brand/Campaign data
→ apply migration 102
→ predecessor rows preserved
→ no ambiguous/destructive backfill
→ migration status current
```

PostgreSQL-backed behavior matrix must include:

1. Owner manual create without Instagram/Content/model.
2. Manager manual create/update.
3. Assistant read succeeds and edit fails atomically.
4. Anonymous, inactive/non-Creator and cross-Creator access denied without row-existence leakage.
5. Exact workspace/owner subject integrity.
6. Strict profile bounds/taxonomy/archetype validation.
7. Empty archetype list as partial/unconfigured; confirmed 1–3 only.
8. Campaign archetype 1–5 remains unchanged.
9. `UGC_CREATOR` never reads/writes commercial UGC willingness.
10. Initial revision 1 and monotonic immutable history.
11. Exact idempotent replay creates no additional profile/revision.
12. Same key/different payload rejected.
13. Stale expected revision rejected.
14. Concurrent same-revision writers produce one accepted revision at most.
15. Failed validation/authorization leaves counts unchanged.
16. GET projection after each mutation exactly matches current revision.
17. Projected name/avatar/handle are read-only and server-derived.
18. Manual profile survives disconnect/failure/delete-source operations.
19. Another Creator and unrelated Brand/Audience/Content/Campaign rows survive target operations.
20. Historical Application/Collaboration rows remain byte/semantically unchanged.

Prove no direct write to Intelligence current or `creator_content` occurs.

---

## 11. Focused regression and runtime gates

Required:

- P0 Creator Brand contract/bundle tests;
- new domain/repository/service/controller/authorization tests;
- fresh PostgreSQL clean-migrate and 101→102 upgrade tests;
- Creator Workspace actor/team policy regressions;
- Creator Settings purge regressions;
- Creator Audience and Content owner-scope/current regressions affected by schema or policy changes;
- focused Campaign archetype regressions;
- existing Brand owner-scope isolation regressions where shared persistence is touched;
- Prisma generate/validate;
- backend production build;
- backend boot;
- `/health/live` HTTP 200;
- `/health` HTTP 200 with PostgreSQL connected;
- scoped ESLint/Prettier;
- `git diff --check`;
- package/lockfile integrity;
- secret/private-key/`.env`/raw-media/signed-locator/runtime-artifact scan.

Frontend source remains unchanged in P1. Prove exact frontend SHA/tree and clean worktree; do not run full frontend/browser/Axe suites.

No live Graph, Meta/OAuth/provider mutation or live model call is required or authorized.

---

## 12. P1 prohibited work

Do not:

- generate or persist Creator Brand suggestions;
- implement the Content read adapter or P2 processor;
- implement suggestion use/edit/confirm endpoints;
- enable the disabled P0 suggestion bundle;
- add scheduling, refresh, retry or provider work;
- modify Creator Content/Audience Object semantics;
- alter canonical Creator archetypes or Campaign cardinality;
- add Creator Brand frontend, route or navigation;
- modify name/avatar/Instagram handle ownership;
- modify Media Kit, Portfolio, Commercial Setup, Marketplace, AI Match, Applications or Collaborations;
- add a Creator Brand score/rank;
- create a second migration;
- change dependencies or lockfiles unless an unavoidable authorized repository tool requires it; stop for review first;
- merge to `development` or deploy;
- force push or rewrite accepted history.

P2, P3 and P4 remain prohibited.

---

## 13. Authority artifacts and ledger

On a fully passing P1:

1. Update the existing single Creator Brand `EXECUTION_LEDGER.md`.
2. Record `P0 = ACCEPTED_BY_TECHNICAL_SA`, correction cycles `0`, and the non-blocking ignored-artifact hygiene exception exactly as described in Section 1.
3. Create:

```text
docs/ai-collaboration/creator-centre/creator-brand-v0/
  P1_CANONICAL_PERSISTENCE_AND_MANUAL_API.md
  runner-prompts/
    CREATOR_BRAND_V0_P1_CANONICAL_PERSISTENCE_AND_MANUAL_API_LOCAL_CODEX_PROMPT_V1.md
```

4. Record exact migration identity/hash, clean/upgrade evidence, role matrix, transaction/idempotency/CAS proof, source-retention proof, routes, changed files/tests and repository checkpoints.
5. Archive this exact prompt; permit only CRLF/LF and one terminal-newline normalization, and prove normalized SHA-256 equality.
6. Record P1 as evidence-ready but not self-accepted. P2 remains prohibited pending Technical-SA review.

---

## 14. Publication and cleanup

Only after every mandatory P1 gate passes:

- commit bounded backend P1 implementation to `program/creator-brand-v0-backend`;
- commit P0 adjudication + P1 evidence/ledger/prompt to `program/creator-brand-v0-authority`;
- make no meaningless frontend commit;
- normal non-force push only;
- independent fetch/prune;
- prove local/fetched SHA and tree equality and exact predecessor ancestry;
- prove all tracked worktrees clean;
- stop task-owned backend/database/container/browser processes;
- remove exact task-owned PostgreSQL container/volume/database and temporary runtime artifacts;
- do not create a dependency junction inside the isolated worktree if an immutable repository-scoped install can be used;
- if a task-local junction is strictly necessary, record it and use a runner-policy-compatible cleanup route before claiming evidence-ready.

Do not delete or alter shared dependency stores or user work.

---

## 15. Circuit breakers

Stop immediately with exact evidence for:

```text
CREATOR_BRAND_P1_STARTING_STATE_MISMATCH
PRODUCT_DECISION_REQUIRED
CONFIRMED_TRUTH_WOULD_BE_DERIVED_ONLY
MANUAL_NO_INTELLIGENCE_FLOW_NOT_POSSIBLE
CREATOR_IDENTITY_OWNER_AMBIGUITY
CROSS_CREATOR_ISOLATION_RISK
ROLE_AUTHORITY_CONFLICT
UGC_ARCHETYPE_WILLINGNESS_COLLISION
CANONICAL_ARCHETYPE_LIBRARY_CONFLICT
DESTRUCTIVE_OR_AMBIGUOUS_LEGACY_MIGRATION
CREATOR_BRAND_P1_SCHEMA_SCOPE_EXPANSION_REQUIRED
MIGRATION_CHAIN_CONFLICT
SOURCE_PURGE_REQUIRES_CONFIRMED_TRUTH_DELETION
SHARED_AUDIENCE_CONTENT_BRAND_CAMPAIGN_REGRESSION
DEPENDENCY_EXPANSION_REQUIRED
LIVE_META_OR_PROVIDER_MUTATION_REQUIRED
```

Ordinary schema syntax, repository/service/DTO/controller, migration SQL, fixture and test defects inside this exact boundary are bounded primary-run/correction work. Do not weaken a gate.

---

## 16. Required immutable return

Return exactly:

```text
CREATOR_BRAND_V0_P1_EXTERNAL_LOCAL_CODEX_REPORT_V1
```

with sections:

```text
A. EXACT STARTING SHA/TREE/WORKTREE/REMOTE PROOF
B. STANDARDS AND AUTHORITY RECONCILIATION
C. P0 TECHNICAL ACCEPTANCE CANONICALIZATION
D. EXACT CHANGED FILES AND DIFF STATISTICS
E. PRISMA SCHEMA AND MIGRATION 102
F. CLEAN 102-MIGRATION APPLY
G. 101→102 UPGRADE AND PREDECESSOR PRESERVATION
H. CANONICAL PROFILE/REVISION DOMAIN MODEL
I. TRANSACTION, CAS, IDEMPOTENCY AND IMMUTABILITY
J. EXPLICIT ROLE-ACTION ENFORCEMENT
K. MANUAL-NO-INTELLIGENCE PATH
L. AUTHENTICATED GET/PUT API CONTRACT
M. PROJECTED IDENTITY OWNERSHIP
N. SOURCE DISCONNECT/FAILURE/PURGE RETENTION
O. TENANT/CREATOR/WORKSPACE/ACTOR ISOLATION
P. CAMPAIGN/ARCHETYPE/UGC-WILLINGNESS REGRESSION
Q. AUDIENCE/CONTENT/BRAND/HISTORICAL-DOMAIN REGRESSIONS
R. POSTGRESQL ROW COUNTS AND AUDIT LINEAGE
S. BUILD/BOOT/HEALTH/TEST/LINT/DIFF RESULTS
T. FRONTEND UNCHANGED PROOF
U. SCHEMA/MIGRATION/DEPENDENCY IDENTITY
V. SECRET/RAW-MEDIA/FORBIDDEN-ARTIFACT SCAN
W. LIVE CALL COUNTS
X. BACKEND PUBLICATION/FETCH-BACK SHA/TREE
Y. FRONTEND UNCHANGED SHA/TREE
Z. AUTHORITY PUBLICATION/FETCH-BACK SHA/TREE
AA. PROMPT ARCHIVE DIGEST EQUALITY
AB. CLEANUP AND FINAL WORKTREE/RESOURCE STATE
AC. PRIMARY-RUN/CORRECTION ACCOUNTING
AD. EXACT BLOCKER, IF ANY
```

End exactly:

```text
RESULT =
READY_FOR_CHILD_SA_PROGRAM_ORCHESTRATOR_REVIEW
| CREATOR_BRAND_P1_CORRECTION_REQUIRED
| CREATOR_BRAND_P1_CIRCUIT_BREAKER

P0 =
ACCEPTED

P1_ACCEPTED =
NO — AWAITING CHILD-SA / PROGRAM-ORCHESTRATOR REVIEW

P1_EVIDENCE_READY =
YES | NO

P1_PRIMARY_RUNS_USED =
<integer>

P1_CORRECTION_CYCLES_USED =
<integer>

P2 =
PROHIBITED

LIVE_GRAPH_CALLS =
NONE

LIVE_MODEL_CALLS =
NONE

META_PROVIDER_MUTATIONS =
NONE

NEW_MIGRATIONS =
1 | NONE

MIGRATION_COUNT =
102 | 101

DEVELOPMENT_MERGE =
NOT_PERFORMED

DEPLOYMENT =
NOT_PERFORMED

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CHILD SA / PROGRAM ORCHESTRATOR
```

Do not self-declare `P1_ACCEPTED = YES`. Do not start P2.
