# CREATOR BRAND V0 — P4 INTEGRATED ACCEPTANCE AND IMMUTABLE CLOSEOUT — LOCAL CODEX EXECUTION PROMPT V1

`CREATOR_BRAND_V0_P4_INTEGRATED_ACCEPTANCE_AND_IMMUTABLE_CLOSEOUT_LOCAL_CODEX_PROMPT_V1`

You are the authorized External Local Codex execution runner for the final Creator Brand V0 packet.

This is a bounded P4 verification, evidence, developer-handoff and immutable-closeout assignment.

Do not reinterpret Product. Do not begin another Creator Centre workspace. Do not merge to `development`. Do not deploy.

---

## 1. Operating topology and acceptance authority

Preserve:

```text
CREATOR CENTRE SA
= Product / Program authority

CREATOR INSTAGRAM INTELLIGENCE TECHNICAL SA
= technical architecture
= Local Codex assignment author
= technical checkpoint and final acceptance authority

LOCAL CODEX
= local implementation/runtime/PostgreSQL/browser/Git/evidence runner
= no Product authority
= no self-acceptance

PARENT
= final authority
= mechanical relay
```

Your return is evidence for Technical-SA/Parent review. It must not mark P4 accepted itself.

---

## 2. Sole Product and execution authority

Use only this Creator Brand authority chain:

```text
Creator Brand Product Register V2
2d561da03ae5df296443f32f79f24264cc8a9361

Creator Brand Technical Handoff V2
0db5974c562aea96568c4e12b240cbb1ef5cd09d

Creator Brand Product Amendment 1
07917b2191a6de3f0c8ffb86500abc597478fc2b

Creator Brand Parent execution authorization
7e54d3980ca3454263600e44766041dbb58e0a06

Corrected Creator Content authority ancestor
cb9ee23118eafb2dc156c25eb09702ea93252db4
```

Ignore any later or competing Creator Brand Product register unless it is an explicit descendant amendment named in this prompt. Do not substitute Product material from another Parent/C03 conversation.

Current Campaign objective authority remains:

```text
AWARENESS
TRUST
ASSETS
ACTION
```

`PULSE / PROOF / PRODUCTION / PUSH` is legacy only and must not be reintroduced.

---

## 3. Accepted packet state

Technical-SA adjudication:

```text
P0 = ACCEPTED
P1 = ACCEPTED
P2 = ACCEPTED
P3 = ACCEPTED

P3_PRIMARY_RUNS_USED = 1
P3_CORRECTION_CYCLES_USED = 1

P4 = AUTHORIZED_FOR_EXTERNAL_LOCAL_CODEX
```

P3's initial access-gate report was a prompt-authority conflict, not a Product failure. The binding corrected access model is accepted:

```text
/creator/brand
→ authenticated App shell
→ active Creator Team membership
→ server-resolved canonical Owner subject
→ explicit CREATOR_BRAND_READ

NOT
→ RequireCreatorPlatformAccess
→ Creator Entry canEnterCreatorPlatform
→ Instagram connection/readiness
→ Creator Content readiness
```

Existing Creator Entry and `RequireCreatorPlatformAccess` behavior remains unchanged for the routes it already governs.

---

## 4. Exact starting checkpoints

Before mutation, independently fetch/prune and prove exact local/fetched SHA and tree equality, clean worktrees, expected repository identity and accepted ancestry.

```text
BACKEND_REPOSITORY =
Piyush1087/creator-commerce-backend-v2-clone

BACKEND_BRANCH =
program/creator-brand-v0-backend

BACKEND_SHA =
6206f43c6a13c304c971b810e1dd99a20aaaa11f

BACKEND_TREE =
533f543612b856cfaf3b57769fe0b5541b803c3f
```

```text
FRONTEND_REPOSITORY =
Piyush1087/creator-commerce-frontend-v2-clone

FRONTEND_BRANCH =
program/creator-brand-v0-frontend

FRONTEND_SHA =
c505c0679e39effdd9608e319112591d5ae4c079

FRONTEND_TREE =
18dd8ed798aae509baa7d0d51ab8e31d7ac2dbbd
```

```text
AUTHORITY_REPOSITORY =
Piyush1087/dummy_tcs

AUTHORITY_BRANCH =
program/creator-brand-v0-authority

AUTHORITY_SHA =
ea3fe45e0cb16977684b4dbbb68be8f91a8d9a86

AUTHORITY_TREE =
19603220605fc64674c8bb8695bd6715ddb97ea9
```

Expected database authority:

```text
MIGRATION_COUNT = 102

HEAD_MIGRATION =
20260915100000_creator_brand_canonical_profile_revision

HEAD_MIGRATION_SHA256 =
ec6484427c24d5755549ff9e71bae989f5ff592a6fc5da953ca07494b8744452
```

If any identity, tree, clean-worktree state or required ancestry fails, stop before mutation with:

```text
CREATOR_BRAND_P4_STARTING_STATE_MISMATCH
```

Do not reset, clean, stash, discard or overwrite user work.

---

## 5. P4 purpose and mutation boundary

P4 owns:

```text
integrated acceptance
PostgreSQL/runtime verification
authenticated browser/accessibility verification
cross-module regression verification
immutable evidence closeout
developer handoff
Product-learning handoff
single-ledger finalization
authority publication/fetch-back proof
```

Default source boundary:

```text
BACKEND SOURCE MUTATION = NOT AUTHORIZED
FRONTEND SOURCE MUTATION = NOT AUTHORIZED
SCHEMA/MIGRATION MUTATION = NOT AUTHORIZED
DEPENDENCY/LOCKFILE MUTATION = NOT AUTHORIZED
```

Use the accepted P0–P3 trees as the product under test. You may create task-local fixtures, disposable runtime configuration and test evidence outside Git, but they must be removed before closeout.

If a real implementation defect is found, do not repair it in this run. Preserve exact evidence and return:

```text
CREATOR_BRAND_P4_CORRECTION_REQUIRED
```

If the defect requires Product/architecture/schema/runtime expansion beyond accepted authority, return:

```text
CREATOR_BRAND_P4_CIRCUIT_BREAKER
```

Only after every mandatory gate passes may you modify and publish authority documentation.

---

## 6. Required authority reconciliation

Read and reconcile before execution:

- repository `AGENTS.md` files;
- applicable engineering, backend, database, validation, AI-integration, frontend and Aurora standards;
- the sole Product/technical/amendment/authorization documents named above;
- `P0_AUTHORITY_CONVERGENCE_AND_EXECUTABLE_CONTRACTS.md`;
- `P1_CANONICAL_PERSISTENCE_AND_MANUAL_API.md`;
- `P2_CONTENT_BACKED_SUGGESTIONS_AND_CONFIRMATION.md`;
- `P3_CREATOR_BRAND_FRONTEND.md`;
- the single Creator Brand `EXECUTION_LEDGER.md`;
- accepted Audience and corrected Content developer/final handoffs where directly relevant;
- executable Creator Brand contracts, services, controllers, policy, persistence, frontend schemas/clients/views/routes and test harnesses.

Do not treat the old P3 full-frontend prompt's `RequireCreatorPlatformAccess` wording as authority. The published P3 access-gate correction and this prompt supersede it.

---

## 7. PostgreSQL and migration gates

Use a uniquely named disposable PostgreSQL 17 route with task-owned ephemeral storage.

Prove:

1. clean application of all 102 migrations;
2. migration status current;
3. exact head-migration byte checksum;
4. populated 101→102 upgrade using an accepted migration-101 predecessor snapshot or deterministic fixture route;
5. preservation of all predecessor tables/rows outside the additive Creator Brand boundary;
6. no destructive/ambiguous Creator backfill;
7. required extensions and connectivity;
8. backend production boot;
9. `/health/live` HTTP 200;
10. `/health` HTTP 200 with PostgreSQL up.

Do not create migration 103.

Record PostgreSQL version, migration inventory, upgrade fixture basis, before/after counts or digests, and cleanup proof without credentials.

---

## 8. Canonical Creator Brand truth gates

Through real services/repositories and authenticated APIs, prove:

### Manual-first

- a Creator with active Team membership can create and maintain Creator Brand with no Instagram connection;
- no current Creator Content Object, provider access or model call is required;
- missing/suppressed source Intelligence never prevents manual setup;
- confirmed Creator-authored truth is separate from derived suggestion current.

### Identity and subject

- name, avatar and Instagram handle are projected read-only;
- canonical Owner subject is server-resolved;
- delegated Manager/Assistant actor identity never replaces the canonical Owner;
- no caller-selected Creator subject or workspace escape exists.

### Roles

```text
OWNER
CREATOR_BRAND_READ = YES
CREATOR_BRAND_EDIT = YES
CREATOR_BRAND_CONFIRM_SUGGESTION = YES

MANAGER
CREATOR_BRAND_READ = YES
CREATOR_BRAND_EDIT = YES
CREATOR_BRAND_CONFIRM_SUGGESTION = YES

ASSISTANT
CREATOR_BRAND_READ = YES
CREATOR_BRAND_EDIT = NO
CREATOR_BRAND_CONFIRM_SUGGESTION = NO
```

Also prove inactive/missing Team context denied and anonymous access denied.

### Revision and audit behavior

- optimistic revision/CAS conflict behavior;
- idempotent mutation replay;
- immutable revision/audit history;
- MANUAL, SUGGESTION_USED and SUGGESTION_EDITED origins remain distinguishable;
- suggestion edit records the final Creator-authored value;
- Cancel/failed mutation does not publish draft state;
- server-held suggestion references prevent client-fabricated provenance.

### Taxonomies and bounds

- exact 27 Primary Niche IDs, maximum 3;
- exact 12 Voice/Personality descriptor IDs, maximum 3;
- optional bounded short description;
- languages use canonical BCP-47 behavior and the Product label “Languages I create in”;
- palettes accept normalized uppercase `#RRGGBB` only and are never fabricated by suggestions;
- duplicate/over-bound/invalid values fail closed.

### Creator Archetypes

- reuse the canonical Campaign/Creator archetype artifact;
- exact 30 active IDs;
- Creator Brand allows unconfigured or 1–3 confirmed archetypes;
- Campaign behavior remains 1–5;
- self-declared archetype is not represented as a proven match;
- `UGC_CREATOR` archetype never becomes “Open to UGC projects” willingness;
- no copied/forked Creator-only archetype library.

---

## 9. Derived suggestion gates

Prove exactly the five frozen executable slices and no sixth:

```text
$/f/positioning
$/f/voice_personality
$/f/creator_style
$/f/visual_identity
$/f/languages
```

Preserve their frozen typed-candidate ownership: positioning covers only its admitted niche/headline facts; voice/personality covers bounded voice facts; creator style covers canonical archetypes; visual identity covers bounded visual/palette cues; languages covers canonical language tags. Do not rename, split, merge or infer another family.

For every emitted suggestion, prove:

- exact Creator subject, source Object/component generation and Evidence subset;
- Content→Creator Brand admission matches the frozen evidence matrix;
- availability, coverage and post-count gates pass;
- confidence is only LOW or MEDIUM according to the frozen contract;
- insufficient evidence yields no suggestion, not an empty/negative fact;
- suggestion remains optional and never auto-applies;
- current/partial/stale/degraded/unavailable and last-good preservation remain truthful;
- exact replay causes no additional semantic/provider work or duplicate lineage/current;
- changed failed execution preserves last valid suggestion current.

Prove UI/API behavior for both:

```text
USE_SUGGESTION
EDIT_SUGGESTION
```

The client must send only accepted server references/revision/idempotency plus the permitted authored profile, never client-claimed Evidence or fabricated origin.

Prove no:

- Bio suggestion;
- auto-application;
- HIGH confidence;
- score, match score, ranking or superiority claim;
- UGC commercial-willingness inference;
- Media Kit, Portfolio or Commercial Setup mutation.

---

## 10. Source lifecycle and purge gates

Prove with real PostgreSQL rows:

```text
source refresh success
source refresh failure
source unavailable/degraded
Instagram disconnect
```

never overwrites or deletes confirmed Creator Brand profile/revisions.

Prove the existing internal Creator-source purge route:

- removes the target Creator's derived Creator Brand suggestion lineage/current;
- removes only the authorized target source data;
- retains the target Creator's canonical Creator Brand profile and immutable revisions;
- preserves another Creator/tenant;
- preserves unrelated Brand, Audience, Content and website rows;
- exposes no new user-facing delete action from Creator Brand.

Creator Settings remains source lifecycle owner.

---

## 11. Frontend and authenticated browser acceptance

Run the production-built frontend against the accepted backend and migrated PostgreSQL route using real authentication/actor-context/Team-context/API paths. Provider/model behavior remains deterministic and fixture-backed.

Required responsive widths:

```text
390
767
768
1440
```

At minimum prove Owner manual-first creation/edit/reload and responsive composition at all four widths. Prove Manager edit/suggestion confirmation and Assistant read-only behavior in authenticated browser at least at desktop, with the complete role matrix also proven through authenticated API tests.

Prove:

- `/creator/brand` works without Instagram and without `RequireCreatorPlatformAccess`;
- active Team membership + `CREATOR_BRAND_READ` controls access;
- inactive/missing Team and anonymous actors are denied;
- existing Audience/Content route guards remain unchanged;
- desktop sidebar and expanded mobile drawer expose Creator Brand;
- five-item bottom navigation remains exactly Home, Insights, Campaigns, Collaborations, Settings;
- Creator Brand is not misclassified as an Insights tab;
- projected identity is read-only and neutral when missing;
- full manual setup and configured editing;
- Save, Cancel, retry and revision-conflict review behavior;
- inline suggestion states and permitted actions;
- canonical last-good truth remains visible through suggestion/source degradation;
- loading, partial, empty, stale, unavailable and error states are truthful;
- keyboard-only navigation and actions;
- visible focus and correct focus return;
- one H1 and coherent heading order;
- no horizontal overflow at any required width;
- Axe serious = 0;
- Axe critical = 0;
- no lesser Axe violations unless explicitly classified and accepted by existing authority;
- console errors = 0;
- uncaught page errors = 0;
- no raw pipeline/provider/model/provenance internals exposed.

External browser traffic must be blocked except the task-local application/API route.

---

## 12. Regression and static gates

Use the proportional accepted test model:

### Backend

- Creator Brand P0 contract tests;
- P1 persistence/service/controller/policy/PostgreSQL tests;
- P2 suggestion/runtime/confirmation/PostgreSQL tests;
- P3 authorization regressions;
- Creator Settings purge tests;
- owner-scope and actor/subject isolation;
- Audience/Content shared-runtime regressions affected by common code;
- Campaign archetype artifact/cardinality regression;
- frozen Brand contract-bundle verification;
- Prisma generate and validate;
- production build;
- scoped ESLint/Prettier;
- `git diff --check`.

### Frontend

- full frontend regression suite once;
- Creator Brand strict parser/client/hook/view/route/navigation tests;
- Creator Entry/platform-guard regressions;
- Creator Audience/Content shell and navigation regressions;
- Campaign archetype regression;
- TypeScript/typecheck;
- production build;
- scoped ESLint/Prettier;
- `git diff --check`.

Do not repeatedly run unrelated full suites. If a shared surface fails, isolate the failure and report whether it is caused by Creator Brand or is inherited. Do not weaken a test or gate.

Record versions and exact test counts/commands. Disclose inherited dependency audit findings separately; do not perform unauthorized upgrades.

---

## 13. Security, secrets and artifact hygiene

Prove:

- tenant, workspace, Creator subject, canonical Owner and actor isolation;
- no credentials/tokens/provider payloads/signed locators/raw media in Git or report;
- no `.env` or browser authentication state committed;
- no database/container/build/runtime output committed;
- no private keys or synthetic-secret false positives included;
- package and lockfile byte identity;
- schema and all migration identities unchanged;
- no live Graph call;
- no live model call;
- no Meta/provider/OAuth mutation.

Task-local runtime configuration may contain only synthetic local values and must be removed during cleanup.

---

## 14. Required closeout artifacts

Only after every mandatory gate passes, update/create in the existing authority repository and single lineage:

```text
docs/ai-collaboration/creator-centre/creator-brand-v0/
  EXECUTION_LEDGER.md
  P4_INTEGRATED_ACCEPTANCE_AND_IMMUTABLE_CLOSEOUT.md
  CREATOR_BRAND_V0_DEVELOPER_HANDOFF.md
  CREATOR_BRAND_V0_FINAL_ACCEPTANCE_AND_PRODUCT_LEARNING_HANDOFF_V1.md
  runner-prompts/
    CREATOR_BRAND_V0_P4_INTEGRATED_ACCEPTANCE_AND_IMMUTABLE_CLOSEOUT_LOCAL_CODEX_PROMPT_V1.md
```

Do not create a second ledger.

The ledger must record:

```text
P0 = ACCEPTED
P1 = ACCEPTED
P2 = ACCEPTED
P3 = ACCEPTED
P4 = EVIDENCE_READY_AWAITING_TECHNICAL_SA_REVIEW
```

and exact primary/correction-cycle accounting for every packet.

### Developer handoff must include

- shipped capability summary;
- canonical truth versus derived suggestion ownership;
- backend/frontend source ownership map;
- public routes/actions and strict contracts;
- roles and actor/subject behavior;
- taxonomies/archetype source and bounds;
- source lifecycle/purge behavior;
- migration and local-runtime requirements;
- environment-variable names only, never values;
- focused developer verification commands;
- state/failure/current-preservation semantics;
- accessibility/responsive expectations;
- known inherited diagnostics;
- remaining V1/V1.1/deployment/provider debt.

### Product-learning handoff must include

- whether manual-first architecture remained simpler or introduced meaningful cost;
- effectiveness of inline suggestions versus separate Intelligence presentation;
- reusable architecture for future editable Creator-controlled workspaces;
- actor-versus-canonical-Owner lessons;
- whether five suggestion families and bounds were sufficient;
- Product decisions that should remain frozen;
- genuine Product questions for a later version;
- no unauthorized Product answer or redesign.

### Deferred/debt register must clearly separate

- merge/deployment work;
- production model/provider adapter configuration where still fail-closed;
- inherited Creator Entry delegated-owner projection debt, if still present;
- future Content V1/longitudinal Intelligence;
- Creator Brand future editing/suggestion expansion not in V0;
- Media Kit/Portfolio/Commercial Setup integrations;
- any repository-wide dependency diagnostics;
- no item falsely represented as a Creator Brand V0 defect.

Archive the exact prompt content. Prove source/archive SHA-256 equality after only LF and terminal-newline normalization.

---

## 15. Publication and fetch-back

If backend and frontend remain byte-identical, do not create meaningless commits. Fetch their branches again and prove exact unchanged local/remote SHA/tree equality.

For authority closeout:

1. commit only the accepted P4 authority files;
2. use normal non-force push;
3. fetch/prune independently;
4. prove local/fetched SHA equality;
5. prove local/fetched tree equality;
6. prove `ea3fe45e0cb16977684b4dbbb68be8f91a8d9a86` ancestry;
7. prove both Product and corrected Content authority ancestors remain present;
8. prove clean worktrees.

Do not force push or rewrite history.

---

## 16. Cleanup

Stop task-owned backend/frontend/browser processes. Remove exact task-owned PostgreSQL containers, volumes, databases, runtime configuration, browser evidence, screenshots, logs and temporary artifacts.

Do not delete unrelated or ambiguous paths. If local policy blocks deletion of verified task-owned ignored output, report the exact path/classification and confirm it is outside Git; do not bypass policy.

Final published worktrees must be clean.

---

## 17. Circuit breakers

Stop and return exact evidence if any of these occurs:

- Product/authority conflict;
- inability to keep confirmed truth outside derived Intelligence current;
- manual setup depends on Instagram, Content or a model;
- Creator archetype/commercial-willingness semantic collision;
- cross-Creator or actor/subject authorization weakness;
- destructive/ambiguous migration behavior;
- source purge requires deletion of confirmed truth;
- a sixth suggestion family or new downstream consumer is required;
- live provider mutation is required;
- schema expansion beyond migration 102 is required;
- accepted Campaign, Brand, Audience or Content behavior cannot be preserved;
- accepted P3 access architecture cannot be maintained.

---

## 18. Immutable Local Codex return contract

Return exactly:

```text
CREATOR_BRAND_V0_P4_EXTERNAL_LOCAL_CODEX_REPORT_V1
```

with sections:

```text
A. exact starting SHA/tree/worktree/remote proof
B. standards and sole-authority reconciliation
C. P0–P3 acceptance and ancestry preservation
D. exact changed files and diff statistics
E. clean-102 and populated-101→102 migration proof
F. backend boot and health proof
G. manual-first no-Instagram/no-Content/no-model proof
H. projected identity and canonical-Owner subject proof
I. Owner/Manager/Assistant and denial matrix
J. canonical fields/taxonomies/bounds proof
K. archetype reuse/cardinality/UGC-separation proof
L. revision/CAS/idempotency/immutable-audit proof
M. five-family suggestion/Evidence-sufficiency proof
N. USE_SUGGESTION and EDIT_SUGGESTION proof
O. no-auto-apply/no-score/non-ownership proof
P. refresh/failure/disconnect confirmed-truth preservation
Q. source-purge and cross-tenant/cross-domain preservation
R. current/replay/failure-preservation and lineage row counts
S. backend regression/build/lint/diff results
T. frontend full/focused regression/build/lint/diff results
U. authenticated browser matrix at 390/767/768/1440
V. keyboard/focus/overflow/Axe/console proof
W. schema/migration/package/lockfile identity
X. secret/raw-media/forbidden-artifact scan
Y. live-call and provider-mutation counts
Z. developer-handoff path and source-map completeness
AA. Product implementation-learning summary
AB. deferred/debt register
AC. backend/frontend unchanged fetch-back proof
AD. authority publication/fetch-back SHA/tree equality
AE. prompt archive digest equality
AF. cleanup and final worktree state
AG. P0–P4 primary/correction accounting
AH. exact blocker, if any
```

End exactly:

```text
RESULT =
READY_FOR_CHILD_SA_PROGRAM_ORCHESTRATOR_FINAL_REVIEW
|
CREATOR_BRAND_P4_CORRECTION_REQUIRED
|
CREATOR_BRAND_P4_CIRCUIT_BREAKER

P0 =
ACCEPTED

P1 =
ACCEPTED

P2 =
ACCEPTED

P3 =
ACCEPTED

P4_ACCEPTED =
NO — AWAITING CHILD-SA / PROGRAM-ORCHESTRATOR REVIEW

P4_EVIDENCE_READY =
YES | NO

CREATOR_BRAND_V0_TECHNICAL_CLOSEOUT_READY =
YES | NO

DEVELOPER_HANDOFF_READY =
YES | NO

LIVE_GRAPH_CALLS =
NONE

LIVE_MODEL_CALLS =
NONE

META_PROVIDER_MUTATIONS =
NONE

NEW_MIGRATIONS =
NONE

MIGRATION_COUNT =
102

DEVELOPMENT_MERGE =
NOT_PERFORMED

DEPLOYMENT =
NOT_PERFORMED

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CHILD SA / PROGRAM ORCHESTRATOR
FOR FINAL REVIEW
```

Do not self-accept P4. Do not begin another workspace. Stop after returning the immutable report.
