# Creator Brand V0 — P0 Authority Convergence + Executable Contracts — Local Codex Prompt V1

`CREATOR_BRAND_V0_P0_AUTHORITY_CONVERGENCE_AND_EXECUTABLE_CONTRACTS_LOCAL_CODEX_PROMPT_V1`

You are the **External Local Codex execution runner**. The Parent is mechanically relaying this exact prompt. You have no Product authority and must not self-accept P0.

Execute only:

```text
P0 — AUTHORITY / BASE CONVERGENCE
     + EXECUTABLE CREATOR BRAND CONTRACTS
```

Do not start P1. Return one immutable report for Technical-SA / Program-Orchestrator review.

---

## 1. Operating topology and relay mode

```text
CREATOR CENTRE SA / PARENT
= Product and final authority

CREATOR INSTAGRAM INTELLIGENCE TECHNICAL SA
= technical architecture, packet sequencing and acceptance

YOU — EXTERNAL LOCAL CODEX
= local repository implementation, tests, Git publication and immutable evidence

PARENT
= mechanical prompt/report relay
```

The earlier autonomous no-relay instruction is operationally superseded only to the extent necessary to restore mechanical Parent relay. Product, architecture, packet scope and acceptance authority remain unchanged.

---

## 2. Repository inputs

The Parent supplies or identifies three local repositories:

```text
AUTHORITY_REPO = local clone of Piyush1087/dummy_tcs
BACKEND_REPO   = local clone of Piyush1087/creator-commerce-backend-v2-clone
FRONTEND_REPO  = local clone of Piyush1087/creator-commerce-frontend-v2-clone
```

Use the repositories' configured authenticated Git remotes. Do not request or print credentials.

Before any mutation:

1. Read every applicable `AGENTS.md`.
2. Read the authority repository's `AI_ENGINEERING_STANDARD.md` and applicable backend/database/validation/AI/frontend standards.
3. Fetch/prune normally in all three repositories.
4. Verify repository identity, clean worktrees and exact starting objects below.
5. Stop on overlapping user work. Do not reset, clean, stash, discard, force-checkout or overwrite it.

---

## 3. Frozen authority

Read and preserve all of the following.

### Product V2

```text
repository = Piyush1087/dummy_tcs
commit = 2d561da03ae5df296443f32f79f24264cc8a9361
tree = f4bb98547066317ac37f2ce2505e8f86aa44ba2b
file = docs/ai-collaboration/creator-centre/CREATOR_BRAND_V0_PRODUCT_DECISION_REGISTER_V2.md
```

### Technical handoff V2

```text
commit = 0db5974c562aea96568c4e12b240cbb1ef5cd09d
tree = 7ff915a2e2f95a84238e0b7d742d5a4111aff903
file = docs/ai-collaboration/creator-centre/CREATOR_BRAND_V0_TECHNICAL_HANDOFF_AND_ROLE_COMPREHENSION_AUDIT_V2.md
```

### Product Amendment 1

```text
commit = 07917b2191a6de3f0c8ffb86500abc597478fc2b
tree = 2b30768d7fa706564a3442389d223a6423a679ca
file = docs/ai-collaboration/creator-centre/CREATOR_BRAND_V0_PRODUCT_AMENDMENT_1_TAXONOMIES_AND_BOUNDS.md
```

### Parent execution authorization

```text
commit = 7e54d3980ca3454263600e44766041dbb58e0a06
tree = f54f3f0d652d4275a3dd09c9b44656957b589cf0
file = docs/ai-collaboration/creator-centre/creator-brand-v0/CREATOR_BRAND_V0_PARENT_REVIEW_AND_AUTONOMOUS_EXECUTION_AUTHORIZATION_V1.md
```

This commit is the exact Creator Brand Product/authorization starting point. Do not substitute newest `main` merely because it is newer.

### Corrected Creator Content authority

```text
branch = program/creator-content-v0-authority
commit = cb9ee23118eafb2dc156c25eb09702ea93252db4
tree = 26c1598ed4a90ecef5d0d1c14145945a5a7304aa
```

The Product/authorization lineage and corrected Content authority diverge after common ancestor:

```text
179ba0860cba0d256b5ee13757f308ebe99a078d
```

P0 must preserve both histories in one Creator Brand authority branch. Do not flatten either lineage by copying only selected documents.

---

## 4. Accepted runtime predecessors

### Backend

```text
repository = Piyush1087/creator-commerce-backend-v2-clone
starting branch = program/creator-content-v0-backend
SHA = 0fa145ac6a021337929e87b9eb9e0c67ebc82b7e
tree = 750065a56a4a060c125a9bee7ddc9fb842204e7e
```

### Frontend

```text
repository = Piyush1087/creator-commerce-frontend-v2-clone
starting branch = program/creator-content-v0-frontend
SHA = 7edd26d3cdad0ec84083884b34039952368a1295
tree = 0cc596ca1ef1d1c4a51857125547de156e487c8b
```

### Database

```text
migration count = 101
migration head = 20260914191000_creator_audience_owner_scope_lineage
```

Verify exact local/fetched equality and ancestry. If a named remote branch has advanced beyond these SHAs, determine whether the accepted SHA remains present and use the exact accepted SHA as P0 base. A runtime-changing authority conflict is a circuit breaker.

---

## 5. P0 branch and authority-convergence rules

Expected execution branches:

```text
authority = program/creator-brand-v0-authority
backend   = program/creator-brand-v0-backend
frontend  = program/creator-brand-v0-frontend
```

Before creation, prove no conflicting branch already exists locally or remotely.

If a branch exists and is not an exact safe continuation of the required starting authority, stop with:

```text
CREATOR_BRAND_P0_EXISTING_BRANCH_CONFLICT
```

Create branches non-destructively:

- Authority: begin at exact Parent authorization `7e54d398...`, then normally merge the exact corrected Content authority `cb9ee231...` so both are ancestors. Resolve no semantic conflict silently. The previously observed read-only merge hypothesis was conflict-free, but independently verify it.
- Backend: begin at exact accepted Content backend `0fa145ac...`.
- Frontend: begin at exact accepted Content frontend `7edd26d...`.

Do not rebase or rewrite accepted histories. Do not force push.

P0 may leave the frontend tree unchanged if no executable frontend contract is genuinely required yet. Still prove its branch/base identity or explicitly defer branch creation to P3 if repository convention supports that more safely. Do not add placeholder UI.

---

## 6. P0 Product and architecture invariants

Preserve:

```text
CONFIRMED CREATOR BRAND TRUTH
= platform-neutral CreatorBrandProfile
  + immutable CreatorBrandRevision history

DERIVED SUGGESTIONS
= creator_brand_suggestions through the existing owner-scoped
  verified Intelligence generation/current/CAS runtime

CONTENT INPUT
= read-only adapter over accepted creator_content current,
  component generations and Evidence refs
```

P0 must not implement persistence, APIs, mutation runtime, suggestions, scheduler behavior or UI. It freezes executable contracts needed for later packets.

Never create:

```text
parallel Creator DE runtime
parallel Creator Intelligence current store
second scheduler
synthetic Brand identity
Instagram-specific canonical Creator Brand
direct suggestion-to-canonical mutation
Creator Brand score, match score, ranking or eligibility score
```

Manual Creator Brand setup must remain possible with no Instagram, no usable Content current and no model.

---

## 7. Exact P0 executable-contract scope

Inspect current repository contract/bundle/taxonomy conventions first. Reuse accepted primitives. Implement only the smallest compile-time/runtime-verifiable contracts necessary to make P1 and P2 non-interpretive.

### 7.1 Canonical Creator Brand contract

Freeze strict backend domain/validation contracts for the future confirmed profile/revision boundary without persisting them yet:

- server-resolved Creator Workspace / canonical Owner Creator subject;
- projected identity fields are read-only and excluded from canonical mutation input;
- headline/positioning;
- Creator-authored Commercial Bio;
- Primary Niches, maximum 3;
- `creatorArchetypeIds`: zero means partial/unconfigured; a confirmed non-empty selection is 1–3 canonical IDs;
- Voice/Personality descriptors, maximum 3;
- optional Creator-authored voice description, maximum 300 characters;
- visual-style descriptors, maximum 5;
- optional palette, maximum 5 exact uppercase-normalized or canonical `#RRGGBB` values according to repository validation convention;
- languages, maximum 10 normalized BCP-47 tags;
- strict unknown-key rejection;
- explicit state distinguishing partial/unconfigured from confirmed values where required by frozen Product.

Do not invent unbounded free-form taxonomies or duplicate identity ownership.

### 7.2 Frozen niche and voice vocabularies

Install one authoritative technical source or generated adapter for the exact Amendment 1 vocabularies.

Primary Niche IDs, exactly:

```text
BEAUTY
FASHION
LIFESTYLE
HEALTH_WELLNESS
FITNESS
FOOD_COOKING
TRAVEL
PARENTING_FAMILY
HOME_INTERIORS
TECHNOLOGY
GAMING
EDUCATION
CAREER_PRODUCTIVITY
BUSINESS_ENTREPRENEURSHIP
PERSONAL_FINANCE
ARTS_CULTURE
ENTERTAINMENT_COMEDY
MUSIC
PHOTOGRAPHY_FILM
BOOKS_WRITING
SPORTS
AUTOMOTIVE
PETS_ANIMALS
SUSTAINABILITY
DIY_CRAFTS
RELATIONSHIPS
SPIRITUALITY_MINDFULNESS
```

Voice/Personality IDs, exactly:

```text
EDUCATIONAL
WARM
DIRECT
PLAYFUL
ASPIRATIONAL
PRACTICAL
ANALYTICAL
CONVERSATIONAL
BOLD
HUMOROUS
CALM
ENERGETIC
```

These are Creator Brand vocabularies. They are not Brand `IndustryVertical`, Campaign Audience affinities, Commercial Setup Brand-industry preferences or proof of performance.

### 7.3 Canonical Creator Archetype adapter

Audit and pin the exact frozen canonical library:

```text
campaign/canonical/creator_archetypes.yaml
Git blob = 5a2819f8a765857bb03c4b9ecc68d6fd4e7b707c
SHA-256 = ab4a9a3615baa3ffc5815eaae405f793dc08b5ae9404cb3e57db40f37beeb970
expected active IDs = 30
version = 1.0
status = FROZEN
```

Verify the backend's existing runtime artifact is byte/semantic-equivalent to this source or explain the exact accepted generation path. Create/reuse one bounded validation adapter so Creator Brand consumes canonical IDs without introducing an independently maintained Creator copy.

Preserve:

```text
Campaign cardinality = existing 1..5
Creator Brand configured cardinality = 1..3
Creator Brand empty selection = partial/unconfigured
UGC_CREATOR archetype = style/identity only
UGC_CREATOR != Open to UGC projects willingness
```

Campaign behavior must remain byte/semantically unchanged.

### 7.4 Derived suggestion Object contract

Install/freeze the minimum verified executable contract bundle for one derived Object:

```text
Object = creator_brand_suggestions
owner scope = CREATOR
authority = CREATOR_SHOP_DERIVED
protection = UNPROTECTED
```

Its component ownership must represent exactly the five Product-authorized families and no sixth family:

1. Content themes → niche / positioning suggestion.
2. Content communication patterns → Voice / Personality suggestion.
3. Content creative structures / patterns → Creator Archetype suggestion.
4. Content visual execution → visual-style / palette-cue suggestion.
5. Source-supported language observation → languages-I-create-in suggestion.

Choose stable component/path names consistent with current shared runtime conventions. A family may contain multiple typed candidate fields where Product explicitly allows them, but the bundle must remain exactly five family-level ownership slices. Commercial Bio is manual-only and must not appear in suggestion output.

Each future suggestion contract must be capable of retaining:

- exact Creator subject;
- exact source `creator_content` current/component-generation identity;
- exact Evidence refs;
- processor/contract/model or deterministic algorithm version, as applicable;
- per-field availability/intentional absence;
- support confidence `LOW | MEDIUM` only;
- no `HIGH` in V0;
- no auto-apply semantics.

Freeze the sufficiency profile as contract constants/metadata, not Product scores:

```text
LOW
= at least 3 distinct eligible posts
  + complete provider inventory
  + semantic coverage >= 50%

MEDIUM
= at least 5 distinct eligible posts
  + semantic coverage >= 70%
  + at least 2 publication dates

HIGH = not authorized
```

Do not build the P2 processor or read adapter in P0.

### 7.5 Explicit role-action contracts

Freeze explicit action identifiers and policy matrix for later P1 enforcement:

```text
CREATOR_BRAND_READ
CREATOR_BRAND_EDIT
CREATOR_BRAND_CONFIRM_SUGGESTION
```

```text
OWNER     READ YES / EDIT YES / CONFIRM YES
MANAGER   READ YES / EDIT YES / CONFIRM YES
ASSISTANT READ YES / EDIT NO  / CONFIRM NO
```

Do not infer these actions from unrelated permissions. Do not implement controllers or mutations in P0 unless an existing contract registry requires a minimal compile-safe registration; no runtime route is authorized yet.

### 7.6 Mutation/audit contract boundary

Freeze, but do not persist or execute, the future mutation contract:

```text
server-resolved actor and subject
expected revision / optimistic concurrency
idempotency
serialized transaction-safe update
strict post-mutation validation
immutable revision
```

Allowed origins only:

```text
MANUAL
SUGGESTION_USED
SUGGESTION_EDITED
```

Client-claimed suggestion provenance is never trusted. Historical Applications/Collaborations are never rewritten.

---

## 8. P0 prohibited work

Do not:

- start P1, P2, P3 or P4;
- create or alter a Prisma migration;
- change migration count 101 or migration head;
- create `CreatorBrandProfile`/`CreatorBrandRevision` tables yet;
- build repository/service/controller/consumer routes;
- generate suggestions or invoke a model;
- change Creator Content/Audience behavior;
- change Brand Intelligence/Product Intelligence behavior;
- change Campaign archetype IDs, labels or 1–5 Campaign cardinality;
- change Creator Settings, Instagram connection, refresh or deletion runtime;
- add a scheduler or manual refresh;
- modify Media Kit, Portfolio, Commercial Setup, Application or Collaboration;
- add frontend UI, navigation or placeholder screens;
- add dependencies or lockfile changes unless a direct pre-existing tool-generation requirement makes this unavoidable; if so, stop for review first;
- call Graph/Meta/OAuth or any live model;
- merge to `development` or deploy;
- force push or rewrite upstream history.

---

## 9. Required P0 evidence

At minimum prove:

1. Exact starting SHA/tree/worktree/remote equality for all three repositories.
2. Exact authority commits/trees and both-lineage convergence.
3. Exact runtime predecessor ancestry.
4. Migration count/head unchanged at 101.
5. Canonical archetype path/blob/SHA-256/version/status/30 IDs.
6. Creator Brand 0-or-1..3 contract while Campaign remains 1..5.
7. `UGC_CREATOR` cannot mutate or imply UGC willingness.
8. Exact niche/voice ID sets and every bound.
9. Strict canonical profile input contract excludes projected identity mutation.
10. Exactly five derived suggestion ownership families; no Commercial Bio.
11. LOW/MEDIUM evidence thresholds; HIGH rejected.
12. Explicit role action matrix.
13. Verified contract/bundle registry acceptance and deterministic bundle/hash identity using existing repository conventions.
14. Existing Brand/Audience/Content frozen bundle verification and focused Campaign archetype regressions.
15. Backend typecheck/build plus focused contract/unit/architecture tests.
16. Frontend unchanged proof; run no broad frontend suite if no frontend surface changed.
17. Scoped lint/format/diff integrity.
18. Package/lockfile/schema/migration identity.
19. Secret/private-key/`.env`/raw-media/signed-locator/runtime-artifact scan.
20. No live provider/model calls and no provider mutations.

Use the packet testing model: changed-surface tests + build + scoped lint + diff/secret integrity. Do not run unrelated full browser or PostgreSQL acceptance in P0. No database is needed unless a changed executable contract genuinely requires one; P0 is expected to remain schema/persistence-free.

---

## 10. Authority artifacts and ledger

On successful P0, update the single Creator Brand authority lineage with:

```text
docs/ai-collaboration/creator-centre/creator-brand-v0/
  EXECUTION_LEDGER.md
  P0_AUTHORITY_CONVERGENCE_AND_EXECUTABLE_CONTRACTS.md
  runner-prompts/
    CREATOR_BRAND_V0_P0_AUTHORITY_CONVERGENCE_AND_EXECUTABLE_CONTRACTS_LOCAL_CODEX_PROMPT_V1.md
```

If no Creator Brand ledger exists, create this single ledger. Do not create a duplicate program ledger elsewhere.

Archive the exact relayed prompt. Permit only CRLF/LF and one terminal-newline normalization; record normalized SHA-256 equality.

The P0 authority artifact must record:

- exact authority convergence ancestry;
- starting/final repository SHA/tree;
- executable contract inventory and hashes;
- frozen taxonomy/archetype dispositions;
- tests and static gates;
- P0 scope/prohibitions;
- correction-cycle accounting;
- P1 remains prohibited pending Technical-SA acceptance.

---

## 11. Commit, publication and cleanup

Only after every mandatory P0 gate passes:

1. Commit bounded backend contract work, if any, on `program/creator-brand-v0-backend`.
2. Commit P0 evidence/ledger/prompt on `program/creator-brand-v0-authority`.
3. Do not create a meaningless frontend commit. If the frontend branch is created with no source change, report exact unchanged base identity.
4. Push normally and non-force.
5. Fetch/prune independently.
6. Prove local/fetched SHA and tree equality and required predecessor ancestry.
7. Prove clean worktrees.
8. Remove only task-owned temporary resources.

If any mandatory gate fails, do not commit partial acceptance or publish an accepted-looking checkpoint. Preserve bounded work safely and return the exact blocker.

---

## 12. Circuit breakers

Stop and return exact evidence for:

```text
PRODUCT_DECISION_REQUIRED
CREATOR_BRAND_P0_STARTING_STATE_MISMATCH
CREATOR_BRAND_P0_EXISTING_BRANCH_CONFLICT
AUTHORITY_CONVERGENCE_CONFLICT
CONTENT_RUNTIME_AUTHORITY_CONFLICT
CANONICAL_ARCHETYPE_LIBRARY_CONFLICT
ARCHETYPE_SEMANTICS_REQUIRE_PRODUCT_CHANGE
UGC_ARCHETYPE_WILLINGNESS_COLLISION
CONFIRMED_TRUTH_WOULD_BE_DERIVED_ONLY
MANUAL_NO_INTELLIGENCE_FLOW_NOT_POSSIBLE
ROLE_AUTHORITY_CONFLICT
SHARED_RUNTIME_REGRESSION
MIGRATION_OR_SCHEMA_CHANGE_REQUIRED_IN_P0
DEPENDENCY_EXPANSION_REQUIRED
LIVE_META_OR_PROVIDER_MUTATION_REQUIRED
```

Ordinary compile, test, DTO/contract-registry and fixture defects inside this exact P0 scope are bounded correction work within the primary run. Do not weaken a gate to pass.

---

## 13. Required immutable return contract

Return exactly:

```text
CREATOR_BRAND_V0_P0_EXTERNAL_LOCAL_CODEX_REPORT_V1
```

with these sections:

```text
A. EXACT STARTING SHA/TREE/WORKTREE/REMOTE PROOF
B. STANDARDS AND AUTHORITY RECONCILIATION
C. AUTHORITY CONVERGENCE AND ANCESTRY
D. EXACT CHANGED FILES AND DIFF STATISTICS
E. CANONICAL CREATOR BRAND CONTRACT
F. NICHE AND VOICE VOCABULARY CONTRACTS
G. CANONICAL CREATOR ARCHETYPE ADAPTER AND CAMPAIGN REGRESSION
H. CREATOR_BRAND_SUGGESTIONS OBJECT/BUNDLE CONTRACT
I. EVIDENCE SUFFICIENCY AND AVAILABILITY CONTRACT
J. ROLE ACTION AND MUTATION/AUDIT CONTRACTS
K. MANUAL-FIRST AND TRUTH-OWNERSHIP STATIC PROOF
L. EXISTING BRAND/AUDIENCE/CONTENT CONTRACT REGRESSIONS
M. MIGRATION/SCHEMA/DEPENDENCY IDENTITY
N. BUILD/TEST/LINT/FORMAT/DIFF RESULTS
O. SECRET/RAW-MEDIA/FORBIDDEN-ARTIFACT SCAN
P. LIVE CALL AND PROVIDER-MUTATION COUNTS
Q. BACKEND PUBLICATION/FETCH-BACK SHA/TREE
R. FRONTEND UNCHANGED OR PUBLICATION/FETCH-BACK PROOF
S. AUTHORITY PUBLICATION/FETCH-BACK SHA/TREE
T. PROMPT ARCHIVE DIGEST EQUALITY
U. CLEANUP AND FINAL WORKTREE STATE
V. PRIMARY-RUN/CORRECTION ACCOUNTING
W. EXACT BLOCKER, IF ANY
```

End exactly:

```text
RESULT =
READY_FOR_CHILD_SA_PROGRAM_ORCHESTRATOR_REVIEW
| CREATOR_BRAND_P0_CORRECTION_REQUIRED
| CREATOR_BRAND_P0_CIRCUIT_BREAKER

P0_ACCEPTED =
NO — AWAITING CHILD-SA / PROGRAM-ORCHESTRATOR REVIEW

P0_EVIDENCE_READY =
YES | NO

P0_PRIMARY_RUNS_USED =
<integer>

P0_CORRECTION_CYCLES_USED =
<integer>

P1 =
PROHIBITED

LIVE_GRAPH_CALLS =
NONE

LIVE_MODEL_CALLS =
NONE

META_PROVIDER_MUTATIONS =
NONE

NEW_MIGRATIONS =
NONE

DEVELOPMENT_MERGE =
NOT_PERFORMED

DEPLOYMENT =
NOT_PERFORMED

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CHILD SA / PROGRAM ORCHESTRATOR
```

Do not self-declare `P0_ACCEPTED = YES`. Do not start P1.
