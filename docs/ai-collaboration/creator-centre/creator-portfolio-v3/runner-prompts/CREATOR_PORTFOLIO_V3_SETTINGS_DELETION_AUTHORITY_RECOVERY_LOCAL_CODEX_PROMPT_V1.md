# CREATOR PORTFOLIO V3 — SETTINGS DELETION AUTHORITY RECOVERY V1

`CREATOR_PORTFOLIO_V3_SETTINGS_DELETION_AUTHORITY_RECOVERY_LOCAL_CODEX_PROMPT_V1`

You are the External Local Codex implementation runner continuing Creator Portfolio V3 after a correctly raised P0 Product-authority circuit breaker.

## 1. Recovery authority

The prior report is accepted as a true circuit breaker:

```text
CREATOR_PORTFOLIO_V3_AUTONOMOUS_EXTERNAL_LOCAL_CODEX_REPORT_V1
RESULT = CREATOR_PORTFOLIO_V3_PRODUCT_AUTHORITY_CONFLICT
```

The Parent has now resolved that conflict through the attached exact file:

```text
CREATOR_PORTFOLIO_V3_PRODUCT_AMENDMENT_1_SETTINGS_DELETION_OWNERSHIP_V1.md
```

Read that amendment completely before acting. It supplements only Portfolio V3 section 15. It does not reopen any other Product decision.

This recovery authorizes autonomous execution through:

```text
P0 → P1 → P2 → P3 → P4
```

using the complete original V2 execution prompt as the governing implementation contract:

```text
CREATOR_PORTFOLIO_V3_AUTONOMOUS_LOCAL_CODEX_EXECUTION_PROMPT_V2.md
SHA-256 = fb5ad598ce81f23d0af79557004a7879f006fc6664937cd3fa98b9acf7976417
```

This recovery prompt changes only the Settings/deletion boundary described below.

## 2. Exact preserved recovery state

Before mutation, fetch/prune and independently verify the exact clean state reported by the stopped run:

### Backend

```text
repository = Piyush1087/creator-commerce-backend-v2-clone
branch/base = program/creator-audience-v1-backend
SHA = 6f000bd9364f48d338ae88cec78dcfed0f4fd522
tree = 374f02a8181505038fbb0c084ca33de0ad97ae46
```

### Frontend

```text
repository = Piyush1087/creator-commerce-frontend-v2-clone
branch/base = program/creator-audience-v1-frontend
SHA = 36d75f4eceded7e9e35aaa280b544164d086ca05
tree = eb9c4471a5a7db6228b2f8c4c234834c5671d828
```

### Authority

```text
repository = Piyush1087/dummy_tcs
branch/base = program/creator-audience-v1-authority
SHA = a403a9eae752a4201b263f3e1fb518e0a115e329
tree = 2ae6f750078896d5a3708ba8f5eb3c66b762615e
```

### Database

```text
migration count = 104
```

The stopped run reported zero changed files and zero commits. Reuse the clean Portfolio worktrees if they still match exactly; otherwise create fresh Portfolio-specific worktrees from these checkpoints. Do not reset, clean, stash, discard or overwrite user work.

If any checkpoint, tree, worktree or remote identity does not match, stop with:

```text
CREATOR_PORTFOLIO_V3_RECOVERY_STATE_MISMATCH
```

## 3. Canonical deletion interpretation

Freeze:

```text
DELETE MY INSTAGRAM DATA
= FUTURE SETTINGS — CREATOR COMMAND

INSTAGRAM BASE / SOURCE DATA
= DELETED BY THAT FUTURE SETTINGS COMMAND

DERIVED CREATOR CENTRE INTELLIGENCE / OUTPUTS
= PRESERVED

DELETE MY DATA
= FUTURE SETTINGS — CREATOR COMMAND
```

Portfolio does not own either action.

The current internal `purgeCreatorInstagram()` behavior is accepted predecessor runtime but is not the final user-facing `Delete my Instagram data` contract. Its future reconciliation belongs to Settings — Creator.

## 4. Authorized Portfolio treatment

For this Portfolio run:

- do not modify, rename or reinterpret `purgeCreatorInstagram()`;
- do not call it from Portfolio;
- do not add Portfolio data to its current deletion target set;
- do not implement a Settings button, endpoint, confirmation, coordinator or deletion lifecycle;
- do not modify Audience, Content or Creator Brand deletion behavior;
- preserve all existing purge regressions exactly;
- do not create Portfolio-specific disconnect/delete semantics;
- do not create a source-row cascade whose intentional Product meaning is to delete derived Portfolio output;
- record the two Settings-owned deferred debts in the single Portfolio ledger and final handoffs.

The deferred debts are:

```text
SETTINGS_CREATOR_INSTAGRAM_SOURCE_DELETE_RECONCILIATION
SETTINGS_CREATOR_DELETE_MY_DATA
```

Portfolio may retain stable source identity, provenance summaries, hashes, observed times and derived output required by its frozen Product contract, while continuing to prohibit raw media and durable signed/ephemeral locators.

If Portfolio cannot be implemented without changing Settings deletion semantics or accepted predecessor purge behavior, stop with:

```text
CREATOR_PORTFOLIO_V3_SETTINGS_LIFECYCLE_SCOPE_EXPANSION_REQUIRED
```

## 5. Required recovery proof

P0 must now prove:

1. the Product amendment bytes are exact and archived under Portfolio authority;
2. existing internal purge behavior remains unchanged;
3. Portfolio introduces no Settings deletion action or route;
4. Portfolio persistence does not make Portfolio the owner of Instagram deletion;
5. derived Portfolio state is not deliberately added to the current source purge;
6. accepted Audience V1 purge and lifecycle regressions remain green;
7. the two Settings debts are recorded explicitly;
8. all original P0 donor/provider/reuse gates pass.

After P0 acceptance, continue autonomously through the original P1–P4 scope and gates.

## 6. Publication and authority

Use only these Portfolio branches:

```text
program/creator-portfolio-v3-backend
program/creator-portfolio-v3-frontend
program/creator-portfolio-v3-authority
```

Archive byte-identical normalized copies of:

- the original V2 execution prompt;
- this recovery prompt; and
- the Product amendment.

Publish only after each packet's mandatory gates pass. Use normal non-force pushes and independent fetch-back SHA/tree equality. Do not merge to `development` and do not deploy.

## 7. Accounting

Preserve:

```text
PRIOR_P0_PRIMARY_RUNS_USED = 1
PRIOR_P0_CORRECTION_CYCLES_USED = 0
```

This is a new Parent-authorized circuit-breaker recovery, not a normal correction cycle.

## 8. Immutable return

Return the complete original report contract, plus explicit sections for:

- Product amendment integrity and publication;
- existing purge non-mutation;
- absence of Portfolio/Settings deletion commands;
- Portfolio survivability boundary;
- deferred Settings debts;
- Audience V1 purge regression;
- recovery accounting.

Successful terminal state:

```text
RESULT = READY_FOR_CHILD_SA_PROGRAM_ORCHESTRATOR_FINAL_REVIEW

P0 = ACCEPTED
P1 = ACCEPTED
P2 = ACCEPTED
P3 = ACCEPTED
P4_EVIDENCE_READY = YES

CREATOR_PORTFOLIO_V3_EVIDENCE_READY = YES
AUDIENCE_V1 = ACCEPTED_PREDECESSOR_PRESERVED
MEDIA_KIT = NOT_STARTED

SETTINGS_CREATOR_INSTAGRAM_SOURCE_DELETE_RECONCILIATION = DEFERRED
SETTINGS_CREATOR_DELETE_MY_DATA = DEFERRED

LIVE_GRAPH_CALLS = NONE
LIVE_MODEL_CALLS = NONE
META_PROVIDER_MUTATIONS = NONE
RAW_MEDIA_PERSISTED = NO

DEVELOPMENT_MERGE = NOT_PERFORMED
DEPLOYMENT = NOT_PERFORMED

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CREATOR INSTAGRAM INTELLIGENCE TECHNICAL SA /
PROGRAM ORCHESTRATOR FOR FINAL REVIEW
```

Do not self-declare final technical acceptance.
