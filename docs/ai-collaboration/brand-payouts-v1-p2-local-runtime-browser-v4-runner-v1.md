# Brand Payouts — P2 Local Runtime and Browser V4 Runner V1

`BRAND_PAYOUTS_P2_LOCAL_RUNTIME_BROWSER_V4_RUNNER_V1`

## 0. Authority and objective

This is the complete copy-paste-ready assignment for the local desktop Codex
runner. Read it completely before acting. Do not combine it with an earlier
chat prompt or infer omitted requirements from memory.

Durable authority and accepted inputs:

- Product/SA execution authority:
  `689feb7a46eb2d52b984ce083be046ac6d38657d`;
- P2 contrast-correction and SA-autonomy amendment:
  `3e1120dcf361b3046c97b6ad0cc5c08e6ec2e71e`;
- accepted predecessor: `BP-P1-BACKEND-READ = ACCEPTED`;
- P2 backend runtime-recovery commit:
  `119397ff6836ad12846c1e948409d8d49185c408`;
- P2 backend runtime-recovery tree:
  `73521762dfeaf703b301e7a61c17cbda97d797a9`;
- V1 bootstrap-defect evidence:
  `259e35bc1163bfbe4402d63d363f09f1284044b6`;
- V2 overview-contrast evidence:
  `73781a3bbb6ed8b5cc88e0288fc929142397f924`;
- V3 detail-contrast evidence:
  `d62c2ebc2e79383f1e93074c20fd7fb991399029`;
- V3 evidence tree:
  `bd486b39e5122dbc43bd7909c12d2f2035bc2b27`;
- V3-affected frontend commit:
  `008a7f0c1778d0d805a0d0d5a9dd86540c0ff4b2`;
- final corrected P2 frontend commit:
  `7ff0a39470316215496dece91f717593e98c89b5`;
- final corrected P2 frontend tree:
  `8661f04b7aad64576775968af6b1f0eceaa34c84`.

V3 proved all of the following before its circuit breaker:

- the built frontend mounts with exact `VITE_STAGE=local` and the V1 bootstrap
  defect does not recur;
- the V2 eyebrow, Refresh, and Load more activity contrast failures are closed
  at 390, 767, 768, and 1440 pixels;
- the full AppModule starts against disposable PostgreSQL 17.11 with all 74
  committed migrations and no new migration;
- 178 HTTP/RBAC assertions across 64 GET responses and nine real password
  logins pass;
- 31 live responses validate against frontend schemas;
- before/after hashes across 20 canonical financial models are equal; and
- no provider action occurs.

V3 then found a distinct serious contrast failure in the keyboard-opened
obligation detail state: the **Back to Payouts** ghost button inherited
`#34d399` on `#f8f8f8` and measured 1.81:1 at every required width. This was a
new detail selector, not a recurrence of the corrected overview selectors.

Under amendment `3e1120dcf361b3046c97b6ad0cc5c08e6ec2e71e`, the Systems
Architect applied the second and final bounded P2 correction. Commit
`7ff0a39470316215496dece91f717593e98c89b5` adds a feature-local active ghost-
button color override in `brand-payouts.css` and extends the existing contrast
regression in `brand-payouts-p2.test.ts`. No TSX, shared component, global
Aurora token, global Aurora component, Product semantic, API, authorization,
financial, schema, migration, or provider code changed. Calculated contrast is
17.73:1 on the page surface and 18.83:1 on the card surface.

The correction already passes 23/23 targeted Payouts tests, 28/28 combined
focused tests, 878/878 full frontend tests, changed-file lint/format,
typecheck, diff check, and the exact local-stage production build. The
repository-wide lint baseline remains 26 errors and 13 warnings, all outside
Brand Payouts and unchanged.

Execution mode:

```text
LOCAL_DESKTOP_RUNTIME_AND_COMPLETE_BROWSER_EVIDENCE_ONLY
```

Your objective is to produce the first complete passing P2 runtime,
responsive, keyboard, and accessibility evidence package. This is an evidence
relay, not checkpoint acceptance. Do not accept P2 and do not begin P3. The
Systems Architect will review the returned package, perform the delegated P2
checkpoint decision, update the ledger, and continue only if every gate passes.

## 1. Immutable repository heads

Backend:

```text
repository = Piyush1087/creator-commerce-backend-v2-clone
branch = brand-payouts/v1-backend
required head = 119397ff6836ad12846c1e948409d8d49185c408
required tree = 73521762dfeaf703b301e7a61c17cbda97d797a9
required ancestor = 60385abbd12d5a363615100c463ac6755ce848ea
```

Frontend:

```text
repository = Piyush1087/creator-commerce-frontend-v2-clone
branch = brand-payouts/v1-frontend
required head = 7ff0a39470316215496dece91f717593e98c89b5
required tree = 8661f04b7aad64576775968af6b1f0eceaa34c84
required parent = 008a7f0c1778d0d805a0d0d5a9dd86540c0ff4b2
required base ancestor = 323658d4b147b95b5629ff8d91fa90b8fe9077e4
```

Evidence/authority repository:

```text
repository = Piyush1087/dummy_tcs
branch = brand-payouts/v1-execution-ledger
required evidence ancestor = d62c2ebc2e79383f1e93074c20fd7fb991399029
starting head = the commit containing this runner prompt
```

Before runtime work, fetch and verify all three exact remote heads, trees,
required ancestry, and clean worktrees. Verify that the frontend diff from
`008a7f0c1778d0d805a0d0d5a9dd86540c0ff4b2` to
`7ff0a39470316215496dece91f717593e98c89b5` changes exactly:

- `src/features/brand-payouts/brand-payouts.css`; and
- `src/features/brand-payouts/brand-payouts-p2.test.ts`.

Stop on any mismatch. Do not rebuild P2 from an older base, reopen P0/P1,
amend/rewrite history, merge any branch, or modify a source repository.

Preserve V1, V2, and V3 evidence byte-for-byte. V4 must be additive.

## 2. Hard scope and exhausted correction boundary

Close only:

```text
MISSING_COMPLETE_LIVE_BROWSER_RESPONSIVE_KEYBOARD_ACCESSIBILITY_EVIDENCE
```

Backend and frontend source are immutable. Environment-only work for local
ports, disposable database setup, temporary fixture/browser scripts, and
evidence collection is permitted. Do not commit temporary runtime resources
to either source repository.

The two bounded P2 corrections permitted by the autonomy amendment are now
consumed. If a prior contrast failure repeats or any third genuine P2 source
defect is discovered, source correction is not authorized. Preserve evidence
and stop with the circuit-breaker return in section 10.

Before stopping on an accessibility source defect, perform a bounded scanner
sweep of all already-mounted Payouts-owned overview, obligation-detail,
activity-detail, empty, unavailable, and action states at all required widths.
Report every reproducible serious/critical finding from that sweep together so
another one-at-a-time discovery loop is not created. Do not continue financial
actions, alter state, inject CSS, patch a bundle, weaken a scanner rule, or
change source.

Forbidden:

- backend/frontend source or dependency changes;
- Product, financial, API, RBAC, notification, support, tax, document, or
  terminology changes;
- Prisma schema changes or migration creation;
- Razorpay/provider secret inspection, CLI/API calls, webhooks, Test/Live
  transfers, returns, reversals, or any provider action;
- production/shared database use or deployment;
- merge to `development`, `main`, or another canonical branch;
- logging or committing passwords, JWTs, cookies, tokens, credentials,
  database URLs, raw provider diagnostics, or sensitive fixture values;
- execution-ledger edits or P2 acceptance; and
- P3 work.

Settings remains the sole temporary Add funds and Brand Return mutation
surface. `/brand/payouts` remains read-only.

## 3. Runtime topology and reusable V3 proof

Record OS, CPU architecture, Node, npm, package manager, PostgreSQL, browser,
browser automation, and Axe versions.

Use a fresh disposable loopback-only PostgreSQL database. From empty, apply
and verify all repository-committed migrations. Set session timezone to UTC.
Do not create or edit a migration.

Seed deterministic test-only fixtures for:

- active Brand Owner and Finance Admin;
- active Campaign Manager;
- inactive/no-membership actors;
- unrelated-Brand Owner;
- direct Creator and Admin without Brand financial membership;
- empty/new Brand;
- authoritative vault and ledger rows;
- legacy-unreconciled obligation;
- stable same-timestamp pagination; and
- partial/unavailable states where interception is necessary.

Use the normal password form and real `POST /api/v1/auth/login` with canonical
password hashing. Never inject browser tokens. Use one random non-durable test
password and redact all credentials/session material.

Build and start the pinned backend through `dist/main.js` and the complete
`AppModule`. Keep optional workers inert, provider variables absent, and
network/provider access blocked. Prove root, liveness, database health,
Payouts route mapping, ready/listening state, and controlled shutdown.

Build the exact frontend with:

```text
VITE_API_URL=http://127.0.0.1:<backend-port>
VITE_STAGE=local
```

Serve the built output with Vite preview on loopback. Do not use a dev server,
mock build, hostname workaround, bundle patch, style injection, or second API
server.

The complete V3 HTTP/RBAC, schema-validation, and financial-read-purity
artifacts may be reused by reference after all of the following are proven:

- their index hashes and Git ancestry validate;
- backend SHA/tree are unchanged;
- the only frontend production diff is the feature CSS rule above;
- frontend contracts/API client are byte-identical; and
- a fresh smoke lane confirms login, V2 negotiation, Owner/Finance read,
  Campaign Manager zero-row scope, unauthorized/cross-Brand fail-closed
  behavior, GET-only routes, and unchanged financial-state hashes.

If any condition is not proven, rerun the complete 178-assertion HTTP/RBAC lane
and 31-response schema validation. Never claim reused evidence as freshly rerun.

## 4. Accessibility preflight across all mounted Payouts states

Before the long actor/state screenshot matrix, run a complete early Axe sweep
at 390, 767, 768, and 1440 pixels across every locally reachable Payouts-owned:

- ready/partial overview;
- empty/new Brand overview;
- unavailable/error overview and section states;
- obligation detail, including loading, unavailable, access-denied, and ready;
- activity detail, including loading, unavailable, access-denied, and ready;
- pagination/load-more and retry/action-required controls; and
- Settings compatibility links and any other interactive variant rendered by
  this feature.

Explicitly capture computed foreground/background values and Axe results for:

- overview eyebrow;
- Refresh;
- Load more activity;
- Load more obligations;
- retry controls;
- **Back to Payouts** in obligation detail; and
- **Back to Payouts** in activity detail.

The three V2 elements and the V3 Back button must have no Payouts-attributable
serious/critical contrast violation at all four widths. Record moderate
landmark findings separately; do not hide or relabel them. The known nested/
duplicate-main findings are non-blocking under the current serious/critical
gate, but must be reproduced or explicitly shown absent with ownership noted.

If preflight passes, continue through every remaining section. A contrast-only
rerun is insufficient.

## 5. Complete real-browser actor/state matrix

Use Chromium, Chrome, or Edge through real browser automation at exactly:

```text
390px
767px
768px
1440px desktop
```

Record viewport height. Capture machine-readable results and screenshots for
every materially distinct actor/state, including:

- unauthenticated route entry and normal login;
- Owner and Finance Admin full projections;
- Campaign Manager read-only/zero-row projection;
- inactive membership, no membership, Creator, Admin, and unrelated-Brand
  fail-closed behavior;
- initial/loading and independently loading sections;
- partial source coverage;
- stale last-known state and refreshing;
- empty/new Brand;
- ready state;
- unavailable dependency;
- legacy-unreconciled obligation;
- obligation detail and activity detail;
- pagination/load-more and stable continuation;
- Settings Add funds and Brand Return deep links;
- browser Back restoration;
- refresh restoration; and
- direct unauthorized detail navigation.

Use the single real local backend for authoritative actor/data states. Browser
interception may only delay, abort, or return a contract-valid, explicitly
labelled test projection derived from a captured local response for otherwise
non-deterministic loading/stale/unavailable states. Do not call intercepted
state canonical truth and do not use a second API server.

At every width/state record:

```text
document.documentElement.scrollWidth
document.documentElement.clientWidth
document.body.scrollWidth
```

Require no document-level horizontal overflow. Confirm mobile cards/rows below
768px, desktop tables at/above 768px, the 767/768 boundary, and unobscured
content with bottom navigation and safe-area clearance.

## 6. Keyboard, focus, and accessibility

With keyboard-only interaction, prove:

- logical tab order and visible focus;
- accessible name for every control;
- Enter/Space activation where appropriate;
- detail-page focus entry;
- Back action behavior;
- browser Back restoration;
- Escape behavior where a dismissible surface exists;
- focus return where the interaction model requires it;
- no keyboard trap; and
- operable Settings deep links.

Run Axe 4.x or an equivalent scanner for every materially distinct responsive
and detail state. Record every violation by severity, selector, actor/state,
and viewport. Any Payouts-attributable serious or critical violation fails.

## 7. DOM, network, console, and Product truth

Prove from rendered DOM and network/console evidence:

- no protected financial data renders before authorization resolves;
- no browser-authored amount or due-date calculation occurs;
- no fixed 30/70 or TDS claim appears;
- no synthetic invoice, tax document, provider receipt, or statutory statement
  appears;
- no release, reversal, mark-paid, or caller-economics control appears;
- no provider ID, bank/KYC/tax value, credential, session secret, or raw
  diagnostic appears in the DOM;
- authorized Payouts flows have no unexpected 4xx/5xx;
- unauthorized 4xx fails closed without existence leakage;
- no Payouts-attributable unexpected console exception occurs;
- `/brand/payouts` issues no mutation request; and
- Add funds and Brand Return lead only to the Settings compatibility surface.

Record intentionally blocked external font failures separately. They are not
Payouts defects by themselves and must not invalidate computed-color evidence.

## 8. Fresh V4 evidence package

Create:

```text
docs/ai-collaboration/brand-payouts-v1-p2-local-runtime-browser-evidence-v4.md
```

Create supporting evidence under:

```text
docs/ai-collaboration/evidence/brand-payouts-p2-local-runtime-browser-v4/
```

Include at least:

- machine-readable index with hashes;
- environment and fresh-build records;
- database/migration lifecycle;
- full-AppModule startup/health/shutdown;
- V3 evidence integrity/reuse classification;
- fresh smoke HTTP/RBAC and financial-purity results, plus a complete rerun if
  the V3 reuse criteria fail;
- frontend response-schema compatibility proof;
- complete accessibility-preflight results;
- complete actor/state/viewport browser matrix;
- keyboard/focus results;
- console/network and DOM/Product-truth summaries;
- overflow, breakpoint, bottom-navigation, and safe-area measurements;
- screenshots named by width, actor, and state; and
- disposable-resource cleanup outcome.

Every matrix row must identify the pinned SHAs/trees, viewport, actor/fixture,
state, authoritative versus intercepted source, screenshot, network record,
console result, overflow result, and accessibility/focus result.

Sanitize headers, cookies, tokens, passwords, credentials, database URLs,
machine-local paths, and raw diagnostics. The V4 package must let the Systems
Architect review every P2 gate without rerunning the desktop environment.

## 9. Validation, commit, and push

Before committing:

- prove backend/frontend worktrees remain clean at pinned SHAs;
- prove no Prisma schema/migration diff and no provider action;
- close all owned processes and remove disposable resources;
- validate Markdown links, JSON, index hashes, and evidence completeness;
- run `git diff --check` in the authority repository;
- inspect the whole evidence diff for sensitive values;
- prove V1/V2/V3 evidence and the execution ledger are unchanged; and
- prove no P3 work exists.

Commit and push exactly the V4 report and V4 evidence directory as one fast-
forward descendant of the authority commit containing this prompt. Do not edit
the ledger or set P2 acceptance.

## 10. Required return

On complete passing evidence, return only:

```text
BP_P2_LOCAL_RUNTIME_BROWSER_EVIDENCE_COMPLETE

EVIDENCE_VERSION = V4
EVIDENCE_COMMIT =
EVIDENCE_TREE =
EVIDENCE_REPORT_PERMALINK =
BACKEND_SHA = 119397ff6836ad12846c1e948409d8d49185c408
BACKEND_TREE = 73521762dfeaf703b301e7a61c17cbda97d797a9
FRONTEND_SHA = 7ff0a39470316215496dece91f717593e98c89b5
FRONTEND_TREE = 8661f04b7aad64576775968af6b1f0eceaa34c84
V3_HTTP_EVIDENCE = REUSED_WITH_VERIFICATION | RERUN
SOURCE_CHANGES = NONE
MIGRATIONS_CREATED = NONE
PROVIDER_ACTIONS = NONE
RESULT = PASS
```

For an environment-only blocker, return only:

```text
P2_LOCAL_RUNTIME_BROWSER_ENVIRONMENT_BLOCKED

EVIDENCE_VERSION = V4
LAST_COMPLETED_EVIDENCE =
BLOCKER =
WHY_LOCAL_AND_UNAVOIDABLE =
SAFE_RETRY_ACTION =
SOURCE_CHANGES = NONE
MIGRATIONS_CREATED = NONE
PROVIDER_ACTIONS = NONE
```

For any repeated prior failure or third genuine P2 source defect, commit the
bounded comprehensive failure evidence and return only:

```text
P2_RUNTIME_BROWSER_CIRCUIT_BREAKER

EVIDENCE_VERSION = V4
EVIDENCE_COMMIT =
EVIDENCE_TREE =
EVIDENCE_REPORT_PERMALINK =
AFFECTED_SHA = 7ff0a39470316215496dece91f717593e98c89b5
FAILURE_SIGNATURES =
EXACT_REPRODUCTION =
COMPLETE_BOUNDED_ACCESSIBILITY_SWEEP =
SUSPECTED_SOURCE_FILES =
WHY_SOURCE_CHANGE_IS_REQUIRED =
SOURCE_CHANGES = NONE
MIGRATIONS_CREATED = NONE
PROVIDER_ACTIONS = NONE
P2_ACCEPTANCE = NOT_SET
```

STOP. Do not begin P3.
