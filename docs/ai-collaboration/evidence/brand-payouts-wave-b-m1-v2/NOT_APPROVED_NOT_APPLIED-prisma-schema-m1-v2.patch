NOT_APPROVED_NOT_APPLIED
Base tree: 6c01d65e5a32eecfcd7e42715923ea8478d071fb

diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index ad4be0e..bff09b1 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -1065,6 +1065,78 @@ enum IdempotencyExecutionState {
   FAILED
 }
 
+enum FinancialReserveApprovalStatus {
+  APPROVED_AWAITING_EXECUTION
+  EXECUTING
+  AWAITING_FUNDS
+  ACTION_REQUIRED
+  COMPLETED
+  SUPERSEDED
+}
+
+enum FinancialReserveExecutionOutcome {
+  CLAIMED
+  SUCCEEDED
+  SHORTFALL
+  RETRYABLE_FAILURE
+  TERMINAL_FAILURE
+  SUPERSEDED
+}
+
+enum PayoutObligationProvenanceMode {
+  LEGACY_UNRECONCILED
+  CANONICAL_C04
+}
+
+enum PayoutObligationLifecycle {
+  SCHEDULED
+  READY_QUEUED
+  PROCESSING
+  HELD_RELEASE_PENDING
+  SETTLED
+  FAILED_RETRYABLE
+  ACTION_REQUIRED
+  PARTIAL_REVERSAL
+  FULL_REVERSAL
+  LEGACY_UNRECONCILED
+}
+
+enum PayoutObligationGate {
+  NOT_YET_DUE
+  CREATOR_SETUP_REQUIRED
+  UNSUPPORTED_GEOGRAPHY_OR_RAIL
+  PROVIDER_REVIEW
+  FUNDING_REQUIRED
+  RESOLUTION_BLOCKED
+  PROVIDER_UNAVAILABLE
+  READY
+}
+
+enum PayoutTransferSnapshotMode {
+  LEGACY_UNRECONCILED
+  CURRENT_C05
+}
+
+enum PayoutTransferDisposition {
+  SUCCEEDED
+  RETRYABLE_FAILURE
+  TERMINAL_FAILURE
+  AMBIGUOUS_RECONCILIATION_REQUIRED
+}
+
+enum PayoutReceiptEntityType {
+  TRANSFER_ATTEMPT
+  OBLIGATION
+}
+
+enum PayoutReceiptEventClass {
+  TRANSFER_ACCEPTED
+  TRANSFER_PROCESSED
+  TRANSFER_FAILED
+  TRANSFER_HELD
+  SETTLEMENT_RECORDED
+}
+
 enum CreatorPayoutOnboardingStatus {
   NOT_STARTED
   IN_PROGRESS
@@ -1473,6 +1545,8 @@ model User {
   campaignInvitationsRevoked         CampaignOpportunityInvitation[]   @relation("CampaignInvitationRevoker")
   applicationCommandReceipts         ApplicationCommandReceipt[]       @relation("ApplicationCommandActor")
   applicationDomainEvents            ApplicationDomainEvent[]          @relation("ApplicationEventActorUser")
+  reserveApprovalsRequested          FinancialReserveApproval[]        @relation("ReserveApprovalRequesterUser")
+  reserveApprovalsApproved           FinancialReserveApproval[]        @relation("ReserveApprovalApproverUser")
 
   @@index([organizationId])
   @@index([role])
@@ -1904,6 +1978,7 @@ model BrandProfile {
   collaborationRefundInstructions CollaborationRefundInstruction[]
   escrowFundingLots               EscrowFundingLot[]
   brandReturnRequests             BrandReturnRequest[]
+  financialReserveApprovals       FinancialReserveApproval[]
 
   intelligenceExecutions              IntelligenceExecution[]
   intelligenceSubjects                IntelligenceSubject[]
@@ -3625,6 +3700,7 @@ model CreatorPayoutDestination {
   createdAt              DateTime                                  @default(now()) @map("created_at")
   updatedAt              DateTime                                  @updatedAt @map("updated_at")
   providerMappings       CreatorPayoutDestinationProviderMapping[]
+  transferAttempts       RouteTransferAttempt[]
 
   @@index([creatorProfileId, state], map: "creator_payout_destinations_profile_state_idx")
   @@map("creator_payout_destinations")
@@ -3641,6 +3717,7 @@ model CreatorPayoutDestinationProviderMapping {
   lastReconciledAt   DateTime?                @map("last_reconciled_at")
   createdAt          DateTime                 @default(now()) @map("created_at")
   updatedAt          DateTime                 @updatedAt @map("updated_at")
+  transferAttempts   RouteTransferAttempt[]
 
   @@unique([destinationId, destinationVersion, provider], map: "creator_payout_provider_mapping_version_key")
   @@index([provider, providerReference], map: "creator_payout_provider_reference_idx")
@@ -3652,15 +3729,17 @@ model CreatorPayoutDestinationProviderMapping {
 // ============================================================================
 
 model BrandTeamMember {
-  id             String       @id @default(uuid()) @map("membership_id")
-  brandProfileId String       @map("brand_id")
-  brandProfile   BrandProfile @relation(fields: [brandProfileId], references: [id], onDelete: Cascade)
-  userId         String       @map("user_id")
-  user           User         @relation(fields: [userId], references: [id], onDelete: Cascade)
-  role           BrandRole    @default(CAMPAIGN_MANAGER)
-  isActive       Boolean      @default(true) @map("is_active")
-  joinedAt       DateTime     @default(now()) @map("joined_at")
-  updatedAt      DateTime     @updatedAt @map("updated_at")
+  id                        String                     @id @default(uuid()) @map("membership_id")
+  brandProfileId            String                     @map("brand_id")
+  brandProfile              BrandProfile               @relation(fields: [brandProfileId], references: [id], onDelete: Cascade)
+  userId                    String                     @map("user_id")
+  user                      User                       @relation(fields: [userId], references: [id], onDelete: Cascade)
+  role                      BrandRole                  @default(CAMPAIGN_MANAGER)
+  isActive                  Boolean                    @default(true) @map("is_active")
+  joinedAt                  DateTime                   @default(now()) @map("joined_at")
+  updatedAt                 DateTime                   @updatedAt @map("updated_at")
+  reserveApprovalsRequested FinancialReserveApproval[] @relation("ReserveApprovalRequesterMembership")
+  reserveApprovalsApproved  FinancialReserveApproval[] @relation("ReserveApprovalApproverMembership")
 
   @@unique([brandProfileId, userId])
   @@index([userId, brandProfileId, role])
@@ -4226,6 +4305,7 @@ model Collaboration {
   trustedConfirmations            CollaborationTrustedConfirmation[]
   reserveInstructions             CollaborationReserveInstruction[]
   financialAuthorityInstructions  CollaborationFinancialAuthorityInstruction[]
+  financialReserveApprovals       FinancialReserveApproval[]
 
   @@index([campaignId, creatorUserId])
   @@index([campaignId, creatorProfileId])
@@ -4297,6 +4377,8 @@ model CollaborationCommercialAgreement {
   campaignPaymentTermSnapshot       UcePayoutTerms?                              @map("campaign_payment_term_snapshot")
   reserveInstructions               CollaborationReserveInstruction[]
   financialAuthorityInstructions    CollaborationFinancialAuthorityInstruction[]
+  financialReserveApprovals         FinancialReserveApproval[]
+  payoutObligations                 CreatorPayoutObligation[]
 
   @@index([negotiationState])
   @@index([securementState])
@@ -4330,6 +4412,7 @@ model CollaborationReserveInstruction {
   supersedesInstructionId     String?                           @map("supersedes_instruction_id")
   supersedesInstruction       CollaborationReserveInstruction?  @relation("ReserveInstructionSupersession", fields: [supersedesInstructionId], references: [id], onDelete: Restrict)
   supersededBy                CollaborationReserveInstruction[] @relation("ReserveInstructionSupersession")
+  financialReserveApproval    FinancialReserveApproval?
 
   @@unique([collaborationId, instructionVersion], map: "c04_reserve_collab_version_key")
   @@unique([collaborationId, idempotencyKey], map: "c04_reserve_collab_idempotency_key")
@@ -4362,6 +4445,7 @@ model CollaborationFinancialAuthorityInstruction {
   supersededBy             CollaborationFinancialAuthorityInstruction[] @relation("FinancialAuthoritySupersession")
   effectiveAt              DateTime                                     @map("effective_at")
   createdAt                DateTime                                     @default(now()) @map("created_at")
+  payoutObligation         CreatorPayoutObligation?
 
   @@unique([collaborationId, kind, instructionVersion], map: "c04_financial_collab_kind_version_key")
   @@index([commercialAgreementId], map: "c04_financial_agreement_idx")
@@ -4921,22 +5005,23 @@ model CollaborationFundingLotAllocation {
 }
 
 model CollaborationEscrowLock {
-  id                      String        @id @default(uuid()) @map("lock_id")
-  collaborationId         String        @unique @map("collaboration_id")
-  collaboration           Collaboration @relation(fields: [collaborationId], references: [id], onDelete: Restrict)
-  brandProfileId          String        @map("brand_id")
-  grossCreatorQuote       Decimal       @map("gross_creator_quote") @db.Decimal(15, 4)
-  platformCommissionFee   Decimal       @map("platform_commission_fee") @db.Decimal(15, 4)
-  platformCommissionGst   Decimal       @default(0) @map("platform_commission_gst") @db.Decimal(15, 4)
-  totalEscrowLockedAmount Decimal       @map("total_escrow_locked_amount") @db.Decimal(15, 4)
-  expectedTdsPercentage   Decimal       @default(0) @map("expected_tds_percentage") @db.Decimal(4, 2)
-  calculatedTdsDeduction  Decimal       @default(0) @map("calculated_tds_deduction") @db.Decimal(15, 4)
-  netCreatorPayoutPool    Decimal       @map("net_creator_payout_pool") @db.Decimal(15, 4)
-  advanceTrancheDisbursed Boolean       @default(false) @map("advance_tranche_disbursed")
-  finalTrancheDisbursed   Boolean       @default(false) @map("final_tranche_disbursed")
-  lockReleasedViaRefund   Boolean       @default(false) @map("lock_released_via_refund")
-  createdAt               DateTime      @default(now()) @map("created_at")
-  updatedAt               DateTime      @updatedAt @map("updated_at")
+  id                       String                    @id @default(uuid()) @map("lock_id")
+  collaborationId          String                    @unique @map("collaboration_id")
+  collaboration            Collaboration             @relation(fields: [collaborationId], references: [id], onDelete: Restrict)
+  brandProfileId           String                    @map("brand_id")
+  grossCreatorQuote        Decimal                   @map("gross_creator_quote") @db.Decimal(15, 4)
+  platformCommissionFee    Decimal                   @map("platform_commission_fee") @db.Decimal(15, 4)
+  platformCommissionGst    Decimal                   @default(0) @map("platform_commission_gst") @db.Decimal(15, 4)
+  totalEscrowLockedAmount  Decimal                   @map("total_escrow_locked_amount") @db.Decimal(15, 4)
+  expectedTdsPercentage    Decimal                   @default(0) @map("expected_tds_percentage") @db.Decimal(4, 2)
+  calculatedTdsDeduction   Decimal                   @default(0) @map("calculated_tds_deduction") @db.Decimal(15, 4)
+  netCreatorPayoutPool     Decimal                   @map("net_creator_payout_pool") @db.Decimal(15, 4)
+  advanceTrancheDisbursed  Boolean                   @default(false) @map("advance_tranche_disbursed")
+  finalTrancheDisbursed    Boolean                   @default(false) @map("final_tranche_disbursed")
+  lockReleasedViaRefund    Boolean                   @default(false) @map("lock_released_via_refund")
+  createdAt                DateTime                  @default(now()) @map("created_at")
+  updatedAt                DateTime                  @updatedAt @map("updated_at")
+  financialReserveApproval FinancialReserveApproval?
 
   @@index([collaborationId])
   @@index([brandProfileId])
@@ -4961,6 +5046,7 @@ model EscrowTransactionLedger {
   errorDiagnosticPayload     Json?                           @map("error_diagnostic_payload")
   createdAt                  DateTime                        @default(now()) @map("created_at")
   refundInstruction          CollaborationRefundInstruction?
+  financialReserveApproval   FinancialReserveApproval?
 
   @@index([vaultId])
   @@index([brandProfileId, collaborationId, transactionType])
@@ -5038,33 +5124,128 @@ model CreatorPayoutProfile {
   @@map("creator_payout_profiles")
 }
 
-model CreatorPayoutObligation {
-  id                      String                           @id @default(uuid()) @map("obligation_id")
-  settlementInstructionId String                           @unique @map("settlement_instruction_id")
-  collaborationId         String                           @map("collaboration_id")
-  collaboration           Collaboration                    @relation(fields: [collaborationId], references: [id], onDelete: Restrict)
-  vaultId                 String                           @map("vault_id")
-  vault                   BrandEscrowVault                 @relation(fields: [vaultId], references: [id], onDelete: Restrict)
-  brandProfileId          String                           @map("brand_id")
-  brandProfile            BrandProfile                     @relation(fields: [brandProfileId], references: [id], onDelete: Restrict)
-  creatorProfileId        String                           @map("creator_profile_id")
-  creatorProfile          CreatorProfile                   @relation(fields: [creatorProfileId], references: [id], onDelete: Restrict)
-  payoutProfileId         String                           @map("payout_profile_id")
-  payoutProfile           CreatorPayoutProfile             @relation(fields: [payoutProfileId], references: [id], onDelete: Restrict)
-  obligationType          CreatorPayoutObligationType      @map("obligation_type")
-  entitlementAmount       Decimal                          @map("entitlement_amount") @db.Decimal(15, 4)
-  currency                String                           @db.VarChar(3)
-  status                  CreatorPayoutObligationStatus    @default(ELIGIBLE)
-  instructionIssuedAt     DateTime                         @map("instruction_issued_at")
-  paymentDueAt            DateTime?                        @map("payment_due_at")
-  blockedReason           String?                          @map("blocked_reason") @db.VarChar(100)
-  settledAt               DateTime?                        @map("settled_at")
-  terminalAt              DateTime?                        @map("terminal_at")
-  createdAt               DateTime                         @default(now()) @map("created_at")
-  updatedAt               DateTime                         @updatedAt @map("updated_at")
-  transfers               RouteTransferAttempt[]
-  fundingAllocations      CreatorPayoutFundingAllocation[]
+model FinancialReserveApproval {
+  id                          String                             @id @default(uuid())
+  reserveInstructionId        String                             @unique(map: "bp_m1_reserve_instruction_key") @map("reserve_instruction_id")
+  reserveInstruction          CollaborationReserveInstruction    @relation(fields: [reserveInstructionId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  requestId                   String                             @unique(map: "bp_m1_reserve_request_key") @map("request_id")
+  collaborationId             String                             @map("collaboration_id")
+  collaboration               Collaboration                      @relation(fields: [collaborationId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  commercialAgreementId       String                             @map("commercial_agreement_id")
+  commercialAgreement         CollaborationCommercialAgreement   @relation(fields: [commercialAgreementId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  brandProfileId              String                             @map("brand_profile_id")
+  brandProfile                BrandProfile                       @relation(fields: [brandProfileId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  instructionVersion          Int                                @map("instruction_version")
+  instructionHash             String                             @map("instruction_hash") @db.Char(64)
+  agreementVersion            Int                                @map("agreement_version")
+  agreementHash               String                             @map("agreement_hash") @db.Char(64)
+  creatorFee                  Decimal                            @map("creator_fee") @db.Decimal(14, 2)
+  platformCommissionAmount    Decimal                            @map("platform_commission_amount") @db.Decimal(14, 2)
+  platformCommissionGstAmount Decimal                            @map("platform_commission_gst_amount") @db.Decimal(14, 2)
+  approvedReserveAmount       Decimal                            @map("approved_reserve_amount") @db.Decimal(14, 2)
+  currency                    String                             @db.VarChar(3)
+  requestedByUserId           String                             @map("requested_by_user_id")
+  requestedByUser             User                               @relation("ReserveApprovalRequesterUser", fields: [requestedByUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  requestedByMembershipId     String                             @map("requested_by_membership_id")
+  requestedByMembership       BrandTeamMember                    @relation("ReserveApprovalRequesterMembership", fields: [requestedByMembershipId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  requestedByRole             BrandRole                          @map("requested_by_role")
+  requestedAt                 DateTime                           @map("requested_at")
+  approvedByUserId            String                             @map("approved_by_user_id")
+  approvedByUser              User                               @relation("ReserveApprovalApproverUser", fields: [approvedByUserId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  approvedByMembershipId      String                             @map("approved_by_membership_id")
+  approvedByMembership        BrandTeamMember                    @relation("ReserveApprovalApproverMembership", fields: [approvedByMembershipId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  approvedByRole              BrandRole                          @map("approved_by_role")
+  approvedAt                  DateTime                           @default(now()) @map("approved_at")
+  status                      FinancialReserveApprovalStatus     @default(APPROVED_AWAITING_EXECUTION)
+  stateVersion                Int                                @default(0) @map("state_version")
+  idempotencyKey              String                             @map("idempotency_key")
+  executionStartedAt          DateTime?                          @map("execution_started_at")
+  executionCompletedAt        DateTime?                          @map("execution_completed_at")
+  executionFailedAt           DateTime?                          @map("execution_failed_at")
+  failureCode                 String?                            @map("failure_code") @db.VarChar(80)
+  escrowLockId                String?                            @unique(map: "bp_m1_reserve_escrow_lock_key") @map("escrow_lock_id")
+  escrowLock                  CollaborationEscrowLock?           @relation(fields: [escrowLockId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  ledgerTransactionId         String?                            @unique(map: "bp_m1_reserve_ledger_key") @map("ledger_transaction_id")
+  ledgerTransaction           EscrowTransactionLedger?           @relation(fields: [ledgerTransactionId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  createdAt                   DateTime                           @default(now()) @map("created_at")
+  updatedAt                   DateTime                           @updatedAt @map("updated_at")
+  attempts                    FinancialReserveExecutionAttempt[]
+
+  @@unique([brandProfileId, idempotencyKey], map: "bp_m1_reserve_brand_idempotency_key")
+  @@index([status, updatedAt, id], map: "bp_m1_reserve_claim_idx")
+  @@index([brandProfileId, status, createdAt], map: "bp_m1_reserve_brand_status_idx")
+  @@index([collaborationId], map: "bp_m1_reserve_collaboration_idx")
+  @@map("financial_reserve_approvals")
+}
+
+model FinancialReserveExecutionAttempt {
+  id              String                           @id @default(uuid())
+  approvalId      String                           @map("approval_id")
+  approval        FinancialReserveApproval         @relation(fields: [approvalId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  attemptSequence Int                              @map("attempt_sequence")
+  claimToken      String                           @unique(map: "bp_m1_attempt_claim_key") @map("claim_token")
+  leaseExpiresAt  DateTime                         @map("lease_expires_at")
+  outcome         FinancialReserveExecutionOutcome @default(CLAIMED)
+  failureCode     String?                          @map("failure_code") @db.VarChar(80)
+  startedAt       DateTime                         @default(now()) @map("started_at")
+  completedAt     DateTime?                        @map("completed_at")
+  createdAt       DateTime                         @default(now()) @map("created_at")
+
+  @@unique([approvalId, attemptSequence], map: "bp_m1_attempt_approval_sequence_key")
+  @@index([outcome, leaseExpiresAt, approvalId], map: "bp_m1_attempt_lease_idx")
+  @@map("financial_reserve_execution_attempts")
+}
 
+model CreatorPayoutObligation {
+  id                          String                                      @id @default(uuid()) @map("obligation_id")
+  settlementInstructionId     String                                      @unique @map("settlement_instruction_id")
+  collaborationId             String                                      @map("collaboration_id")
+  collaboration               Collaboration                               @relation(fields: [collaborationId], references: [id], onDelete: Restrict)
+  vaultId                     String                                      @map("vault_id")
+  vault                       BrandEscrowVault                            @relation(fields: [vaultId], references: [id], onDelete: Restrict)
+  brandProfileId              String                                      @map("brand_id")
+  brandProfile                BrandProfile                                @relation(fields: [brandProfileId], references: [id], onDelete: Restrict)
+  creatorProfileId            String                                      @map("creator_profile_id")
+  creatorProfile              CreatorProfile                              @relation(fields: [creatorProfileId], references: [id], onDelete: Restrict)
+  payoutProfileId             String                                      @map("payout_profile_id")
+  payoutProfile               CreatorPayoutProfile                        @relation(fields: [payoutProfileId], references: [id], onDelete: Restrict)
+  obligationType              CreatorPayoutObligationType                 @map("obligation_type")
+  entitlementAmount           Decimal                                     @map("entitlement_amount") @db.Decimal(15, 4)
+  currency                    String                                      @db.VarChar(3)
+  status                      CreatorPayoutObligationStatus               @default(ELIGIBLE)
+  instructionIssuedAt         DateTime                                    @map("instruction_issued_at")
+  paymentDueAt                DateTime?                                   @map("payment_due_at")
+  blockedReason               String?                                     @map("blocked_reason") @db.VarChar(100)
+  settledAt                   DateTime?                                   @map("settled_at")
+  terminalAt                  DateTime?                                   @map("terminal_at")
+  createdAt                   DateTime                                    @default(now()) @map("created_at")
+  updatedAt                   DateTime                                    @updatedAt @map("updated_at")
+  provenanceMode              PayoutObligationProvenanceMode              @default(LEGACY_UNRECONCILED) @map("provenance_mode")
+  authorityInstructionId      String?                                     @unique(map: "bp_m1_obligation_authority_key") @map("authority_instruction_id")
+  authorityInstruction        CollaborationFinancialAuthorityInstruction? @relation(fields: [authorityInstructionId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  authorityInstructionKind    String?                                     @map("authority_instruction_kind") @db.VarChar(48)
+  authorityInstructionVersion Int?                                        @map("authority_instruction_version")
+  authorityInstructionHash    String?                                     @map("authority_instruction_hash") @db.Char(64)
+  commercialAgreementId       String?                                     @map("commercial_agreement_id")
+  commercialAgreement         CollaborationCommercialAgreement?           @relation(fields: [commercialAgreementId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  agreementVersion            Int?                                        @map("agreement_version")
+  agreementHash               String?                                     @map("agreement_hash") @db.Char(64)
+  intakeRecordedAt            DateTime?                                   @map("intake_recorded_at")
+  settlementEligibleAt        DateTime?                                   @map("settlement_eligible_at")
+  paymentTermSnapshot         UcePayoutTerms?                             @map("payment_term_snapshot")
+  dueRuleVersion              String?                                     @map("due_rule_version") @db.VarChar(40)
+  dueEvidenceRecordedAt       DateTime?                                   @map("due_evidence_recorded_at")
+  lifecycle                   PayoutObligationLifecycle?
+  currentGate                 PayoutObligationGate?                       @map("current_gate")
+  amountSettled               Decimal                                     @default(0) @map("amount_settled") @db.Decimal(15, 4)
+  amountReversed              Decimal                                     @default(0) @map("amount_reversed") @db.Decimal(15, 4)
+  amountOutstanding           Decimal?                                    @map("amount_outstanding") @db.Decimal(15, 4)
+  reconciledReceipts          PayoutReconciledReceipt[]
+  transfers                   RouteTransferAttempt[]
+  fundingAllocations          CreatorPayoutFundingAllocation[]
+
+  @@index([lifecycle, currentGate, paymentDueAt, id], map: "bp_m1_obligation_due_work_idx")
+  @@index([collaborationId, commercialAgreementId, authorityInstructionVersion], map: "bp_m1_obligation_authority_lookup_idx")
   @@index([brandProfileId, status, createdAt])
   @@index([collaborationId, status])
   @@index([creatorProfileId, status])
@@ -5091,34 +5272,50 @@ model CreatorPayoutFundingAllocation {
 }
 
 model RouteTransferAttempt {
-  id                  String                  @id @default(uuid()) @map("transfer_attempt_id")
-  obligationId        String                  @map("obligation_id")
-  obligation          CreatorPayoutObligation @relation(fields: [obligationId], references: [id], onDelete: Restrict)
-  attemptSequence     Int                     @map("attempt_sequence")
-  profileStateVersion Int                     @map("profile_state_version")
-  provider            String                  @default("RAZORPAY_ROUTE") @db.VarChar(40)
-  idempotencyKey      String                  @unique @map("idempotency_key")
-  transferId          String?                 @unique @map("transfer_id")
-  amount              Decimal                 @db.Decimal(15, 4)
-  currency            String                  @db.VarChar(3)
-  state               RouteTransferState      @default(CREATED)
-  settlementState     RouteSettlementState    @default(PENDING) @map("settlement_state")
-  onHold              Boolean                 @default(false) @map("on_hold")
-  onHoldUntil         DateTime?               @map("on_hold_until")
-  providerState       String?                 @map("provider_state") @db.VarChar(100)
-  settlementId        String?                 @unique @map("settlement_id")
-  diagnosticPayload   Json?                   @map("diagnostic_payload")
-  initiatedAt         DateTime                @default(now()) @map("initiated_at")
-  providerAcceptedAt  DateTime?               @map("provider_accepted_at")
-  processedAt         DateTime?               @map("processed_at")
-  releasedAt          DateTime?               @map("released_at")
-  settledAt           DateTime?               @map("settled_at")
-  failedAt            DateTime?               @map("failed_at")
-  createdAt           DateTime                @default(now()) @map("created_at")
-  updatedAt           DateTime                @updatedAt @map("updated_at")
-  reversals           RouteTransferReversal[]
+  id                         String                                   @id @default(uuid()) @map("transfer_attempt_id")
+  obligationId               String                                   @map("obligation_id")
+  obligation                 CreatorPayoutObligation                  @relation(fields: [obligationId], references: [id], onDelete: Restrict)
+  attemptSequence            Int                                      @map("attempt_sequence")
+  profileStateVersion        Int                                      @map("profile_state_version")
+  provider                   String                                   @default("RAZORPAY_ROUTE") @db.VarChar(40)
+  idempotencyKey             String                                   @unique @map("idempotency_key")
+  transferId                 String?                                  @unique @map("transfer_id")
+  amount                     Decimal                                  @db.Decimal(15, 4)
+  currency                   String                                   @db.VarChar(3)
+  state                      RouteTransferState                       @default(CREATED)
+  settlementState            RouteSettlementState                     @default(PENDING) @map("settlement_state")
+  onHold                     Boolean                                  @default(false) @map("on_hold")
+  onHoldUntil                DateTime?                                @map("on_hold_until")
+  providerState              String?                                  @map("provider_state") @db.VarChar(100)
+  settlementId               String?                                  @unique @map("settlement_id")
+  diagnosticPayload          Json?                                    @map("diagnostic_payload")
+  initiatedAt                DateTime                                 @default(now()) @map("initiated_at")
+  providerAcceptedAt         DateTime?                                @map("provider_accepted_at")
+  processedAt                DateTime?                                @map("processed_at")
+  releasedAt                 DateTime?                                @map("released_at")
+  settledAt                  DateTime?                                @map("settled_at")
+  failedAt                   DateTime?                                @map("failed_at")
+  createdAt                  DateTime                                 @default(now()) @map("created_at")
+  updatedAt                  DateTime                                 @updatedAt @map("updated_at")
+  reversals                  RouteTransferReversal[]
+  snapshotMode               PayoutTransferSnapshotMode               @default(LEGACY_UNRECONCILED) @map("snapshot_mode")
+  destinationId              String?                                  @map("destination_id")
+  destination                CreatorPayoutDestination?                @relation(fields: [destinationId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  destinationVersion         Int?                                     @map("destination_version")
+  providerMappingId          String?                                  @map("provider_mapping_id")
+  providerMapping            CreatorPayoutDestinationProviderMapping? @relation(fields: [providerMappingId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  mappingProvider            String?                                  @map("mapping_provider") @db.VarChar(40)
+  queuedAt                   DateTime?                                @map("queued_at")
+  executionStartedAt         DateTime?                                @map("execution_started_at")
+  providerRequestCompletedAt DateTime?                                @map("provider_request_completed_at")
+  failureRecordedAt          DateTime?                                @map("failure_recorded_at")
+  finalDisposition           PayoutTransferDisposition?               @map("final_disposition")
+  finalDispositionRecordedAt DateTime?                                @map("final_disposition_recorded_at")
+  reconciledReceipts         PayoutReconciledReceipt[]
 
   @@unique([obligationId, attemptSequence])
+  @@index([finalDisposition, queuedAt, id], map: "bp_m1_transfer_work_idx")
+  @@index([destinationId, destinationVersion, providerMappingId], map: "bp_m1_transfer_destination_lookup_idx")
   @@index([obligationId, state, createdAt])
   @@map("route_transfer_attempts")
 }
@@ -5144,6 +5341,25 @@ model RouteTransferReversal {
   @@map("route_transfer_reversals")
 }
 
+model PayoutReconciledReceipt {
+  id                 String                  @id @default(uuid())
+  dedupeIdentity     String                  @unique @map("dedupe_identity")
+  entityType         PayoutReceiptEntityType @map("entity_type")
+  eventClass         PayoutReceiptEventClass @map("event_class")
+  transferAttemptId  String                  @map("transfer_attempt_id")
+  transferAttempt    RouteTransferAttempt    @relation(fields: [transferAttemptId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  obligationId       String                  @map("obligation_id")
+  obligation         CreatorPayoutObligation @relation(fields: [obligationId], references: [id], onDelete: Restrict, onUpdate: Restrict)
+  receivedAt         DateTime                @map("received_at")
+  providerOccurredAt DateTime?               @map("provider_occurred_at")
+  reconciledAt       DateTime                @map("reconciled_at")
+  createdAt          DateTime                @default(now()) @map("created_at")
+
+  @@index([transferAttemptId, receivedAt, id], map: "bp_m1_receipt_transfer_time_idx")
+  @@index([obligationId, receivedAt, id], map: "bp_m1_receipt_obligation_time_idx")
+  @@map("payout_reconciled_receipts")
+}
+
 model RouteWebhookReceipt {
   id               String   @id @default(uuid()) @map("webhook_receipt_id")
   eventIdentity    String   @unique @map("event_identity")
