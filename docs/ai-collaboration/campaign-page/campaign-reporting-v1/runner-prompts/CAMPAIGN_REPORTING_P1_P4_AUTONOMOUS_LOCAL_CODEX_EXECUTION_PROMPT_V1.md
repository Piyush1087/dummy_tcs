# CAMPAIGN REPORTING — P1→P4 AUTONOMOUS LOCAL CODEX EXECUTION PROMPT V1

`CAMPAIGN_REPORTING_P1_P4_AUTONOMOUS_LOCAL_CODEX_EXECUTION_V1`

## 0. Assignment

You are the External Local Codex implementation runner for Campaign Reporting V1.

Execute autonomously through:

```text
CONVERGENCE CHECKPOINT
→ P1 ASSETS-FIRST BACKEND VERTICAL SLICE
→ P2 MEDIA METRICS + DETERMINISTIC INTERPRETATION
→ P3 ORCHESTRATION + API + FRONTEND
→ P4 INTEGRATED ACCEPTANCE + CLOSEOUT
```

Do not return for Parent relay between successful packets.

At each packet:

```text
implement
→ run the packet's mandatory gates
→ correct bounded defects inside authorized scope
→ create an immutable normal commit
→ continue when all packet gates pass
```

Stop only for a defined circuit breaker, authority mismatch, unexpected convergence conflict, Product ambiguity, or material scope expansion.

Do not merge to `development` or `production`. Do not deploy. Do not perform live Meta/Graph/model calls or provider mutations.

---

## 1. Technical-SA adjudication

The Campaign Reporting Technical SA has reviewed the published P0 package and adjudicates:

```text
P0 = ACCEPTED

IMPLEMENTATION_BASES_AND_CONVERGENCE_SEQUENCE = ACCEPTED

C04_OBLIGATION_IDENTITY = ACCEPTED

C04_NUMERATOR_MAPPING =
APPROVED | AUTO_APPROVED

MATERIALITY_REGISTRY =
CampaignReportingMaterialityRegistryV1 / cr-mat-1.0
ACCEPTED

REPORT_REFRESH_COOLDOWN =
15_MINUTES
VERSIONED_TECHNICAL_CONFIGURATION

P1_TO_P4_AUTONOMOUS_IMPLEMENTATION = AUTHORIZED
```

This prompt is the exact bounded implementation authority. Do not seek another Parent checkpoint after successful packets.

### 1.1 P0 evidence authority

Repository:

```text
Piyush1087/dummy_tcs
```

Branch:

```text
program/campaign-reporting-v1-authority
```

Exact P0 authority checkpoint:

```text
SHA  = 29488282baf227500db97b319049994f50f0c51a
TREE = db95535ab9eed6aacf08d782b5859fe06aeebad2
```

Required P0 artifact:

```text
docs/ai-collaboration/campaign-page/campaign-reporting-v1/
P0_AUTHORITY_CURRENT_STATE_CAPABILITY_AND_MATERIALITY_PREFLIGHT.md

Git blob = e5527ce154224d8fc8adaff35dfdf0c6c4249886
```

The same checkpoint contains the accepted Product contract, clarification, Plan V3, execution ledger and archived P0 prompt.

Before source mutation, fetch/prune and verify the exact authority SHA/tree and the exact Git blobs below:

```text
CAMPAIGN_REPORTING_PRODUCT_AND_CONSUMER_CONTRACT_V1.md
blob = 7e177f61e7ee973e1851092982e104cbc71f0d91
raw SHA-256 = 897c8c1c81114c1fedec1da3dbeeffa5fa690461dd7905a5ab86905bebefdb65

CAMPAIGN_REPORTING_TECHNICAL_PREFLIGHT_AND_FINITE_IMPLEMENTATION_PLAN_V3.md
blob = d5c6b0d4233a9e249749fa93516888133aa275ee
raw SHA-256 = 446a1ee55d243d80192c9c7581a9a283349972e5d532e6ff44edf67002b947b5

CAMPAIGN_REPORTING_PERFORMANCE_CLARIFICATION_V1.md
blob = 9881c8e7ed35eb677d7b0534dd64a81b90474cad

EXECUTION_LEDGER.md
blob = fa52866c87055fdf1c1d0bf3fef55f45885aa81f
```

If the authority branch has moved, do not silently substitute the newer tip. Confirm the required checkpoint is present and inspect descendants. Stop with `CAMPAIGN_REPORTING_AUTHORITY_STATE_MISMATCH` if a descendant changes Product, P0 conclusions, execution bases or this packet's authority.

---

## 2. Exact source inputs and branches

### 2.1 Backend

Repository:

```text
Piyush1087/creator-commerce-backend-v2-clone
```

Broad Campaign/C04/Settings/shared-runtime base:

```text
SHA  = 129b291ecbca4a1e79451215a81726000cfb5bff
TREE = 9fe6585addf8d8ccee9973b9b55cc0312c6e2b0b
```

Required accepted Instagram/provider/DE lineage:

```text
SHA  = fef32afb0fdef52f00c7c22b3d0a85967a68fded
TREE = 7ef6bd705b55ef82836b3f57f06a2832a867e781
```

Target branch:

```text
program/campaign-reporting-v1-backend
```

### 2.2 Frontend

Repository:

```text
Piyush1087/creator-commerce-frontend-v2-clone
```

Broad Campaign/C04/Settings shell base:

```text
SHA  = 6ea628b09817c4784a248658ddfc0202a13fbb00
TREE = 7de1702cda439c6f0d77000ee0c0c53f40a76c4d
```

Required accepted Instagram workspace/provider lineage:

```text
SHA  = 5866d0ac82f742957f53a8db2144a9a166628b75
TREE = 7c095290ffdef4c84f37382bc594bef536c92a49
```

Target branch:

```text
program/campaign-reporting-v1-frontend
```

### 2.3 Authority

Repository/branch:

```text
Piyush1087/dummy_tcs
program/campaign-reporting-v1-authority
```

Starting checkpoint:

```text
SHA  = 29488282baf227500db97b319049994f50f0c51a
TREE = db95535ab9eed6aacf08d782b5859fe06aeebad2
```

### 2.4 Starting-state gate

Before mutation:

1. Fetch/prune all three repositories.
2. Verify repository identities and exact SHA/tree checkpoints.
3. Verify all required predecessor ancestry.
4. Verify the relevant starting worktrees are clean.
5. Inventory all registered worktrees and preserve unrelated user work.
6. Do not reset, clean, stash, discard, overwrite or reconstruct another user's worktree.
7. Use fresh task-owned worktrees where practical.

If any exact source identity is missing or inconsistent, stop with:

```text
CAMPAIGN_REPORTING_STARTING_STATE_MISMATCH
```

Campaign Reporting must consume the accepted objective vocabulary:

```text
AWARENESS | TRUST | ASSETS | ACTION
```

Do not use legacy `PULSE | PROOF | PRODUCTION | PUSH` Reporting vocabulary as canonical truth. If the converged Campaign projection still exposes legacy values, reuse only an already accepted exact Campaign mapping. Do not bulk-rewrite Campaign state or invent a mapping inside Reporting. If no exact canonical projection/mapping exists, stop with `CAMPAIGN_OBJECTIVE_AUTHORITY_GAP`.

---

## 3. Required convergence checkpoint

Create the backend target from the exact backend broad base and perform a normal non-force, non-squash merge of the exact Instagram backend lineage.

P0 found expected overlap only in:

```text
package.json
prisma/schema.prisma
src/app.module.ts
```

Preserve both accepted lineages. Never choose either side wholesale.

After convergence, prove:

```text
both exact parents are ancestors
98 distinct predecessor migration directories
no same-path/different-blob migration collision
all predecessor migration files byte-identical
Prisma schema validates
package/lockfile consistency
Campaign/C04/Settings/shared-runtime modules preserved
Instagram/provider/DE modules preserved
```

Create the frontend target from the exact frontend broad base and perform a normal non-force, non-squash merge of the exact Instagram frontend lineage. Preserve Campaign Page, C04/Settings and Instagram workspace/provider behavior.

Freeze and record the resulting convergence SHA/tree/migration inventory before P1 changes.

Run focused convergence builds/tests/static checks. Publish the convergence checkpoints normally and independently fetch back exact SHA/tree equality.

Stop with:

```text
CAMPAIGN_REPORTING_CONVERGENCE_SCOPE_EXPANSION_REQUIRED
```

if:

- unexpected semantic conflicts occur outside the P0-reviewed overlap;
- the migration union is not exactly 98;
- an accepted predecessor must be dropped or rewritten;
- dependency reconciliation requires an unapproved major upgrade;
- Campaign/C04/Settings/Instagram behavior cannot coexist without architecture redesign.

Ordinary merge mechanics and type errors inside the reviewed overlap are bounded implementation work.

---

## 4. Frozen architecture and ownership

### 4.1 Reuse

Reuse the accepted shared Intelligence runtime:

```text
execution identity
processor execution and attempts
leases / retry / backoff
dependency and Evidence manifests
immutable Object/component generations
Evidence references
transition / CAS current
failure-current preservation
```

Add a real `CAMPAIGN` Intelligence subject with exact Campaign and owning-Brand fencing. Never synthesize a Brand or Offering subject.

Implement one atomic Reporting root generation/current boundary. Every applicable window component for one calculation must share:

```text
Campaign version
objective
asOf
input manifest/hash
metric contract version
materiality contract version
source observation set
```

Publish the complete root with one serializable CAS. Partial children must never become current. Component currents, if required by the shared runtime, are projections of the accepted root and must not independently advance.

### 4.2 Ownership boundaries

```text
Campaign = Campaign identity, lifecycle, objective, accepted page projection
C04 = Collaboration, Deliverable obligation and execution-state truth
Settings/provider = connection, account, authorization generation and provider lifecycle
DE/Evidence = admitted provider facts and provenance
Campaign Reporting = deterministic calculations and Reporting Intelligence
```

Do not mutate Campaign/C04/provider truth to make Reporting easier.

### 4.3 Explicit non-dependencies

Do not consume or depend on:

```text
Creator Portfolio
Creator Media Kit
Creator Audience
Creator Brand
Creator Rate Card
Creator Work Preferences
```

Canonical creator/content identifiers may be projected for display only. Their Product outputs never enter a Reporting metric.

### 4.4 Prohibited parallel architecture

Do not create:

```text
second Intelligence runtime
second scheduler
second Evidence store
second current store
direct current-row writes
Reporting-owned provider connection
raw provider payload persistence
frontend metric calculation
GET-triggered execution
```

---

## 5. Frozen C04 ASSETS semantics

Canonical denominator:

```text
all CollaborationDeliverableExecution obligations
for all canonical Campaign-linked Collaborations in scope
```

Identity:

```text
CollaborationDeliverableExecution.id
unique(collaborationId, sourceBriefDeliverableId)
```

Canonical numerator states:

```text
APPROVED
AUTO_APPROVED
```

Exclude as numerator authority:

```text
REVISION_REQUESTED
HARD_STOP
publishing compliance states
aggregate Collaboration lifecycle COMPLETED
submission-version history
```

For ACTION Report eligibility only, use the exact accepted C04 publication state/record that unambiguously establishes a published Campaign Deliverable or media item. P0 identifies `CollaborationPublishingState.COMPLIANCE_VERIFIED` as publication-compliance truth, separate from ASSETS completion. Do not use publication compliance as the ASSETS numerator. If current executable C04 truth cannot establish ACTION publication eligibility without interpreting free text, stop with `C04_PUBLISHED_ELIGIBILITY_AUTHORITY_GAP`; do not fabricate eligibility.

Canonical metric ID:

```text
DELIVERABLE_EXECUTION_COMPLETION_RATE
```

`ASSET_COMPLETION_RATE` is only a compatibility alias when a proven existing public caller requires it. Never use it as the new canonical ID.

Required cardinality proof:

```text
2 Brief Deliverable definitions
× 5 Campaign-linked Collaborations
= 10 canonical C04 obligations

8 APPROVED/AUTO_APPROVED
→ 8 / 10 = 80%
```

The Brief template count is provenance, not the multi-Collaboration denominator.

---

## 6. Accepted materiality registry

Freeze:

```text
CampaignReportingMaterialityRegistryV1
version = cr-mat-1.0
```

### 6.1 Count metrics

Applicable families:

```text
reach
impressions
plays
views
likes
comments
saves
shares
```

Subject/dimension:

```text
CREATOR | MEDIA | DELIVERABLE
```

Comparator:

```text
median of other items
same format
same window
same metric contract
minimum 3 comparable peers
```

Thresholds:

```text
absolute floor = 1 count
relative floor = 20%
coverage gate = 70%
zero baseline = above-signal only when subject >= 1
```

### 6.2 Trust interaction rate

```text
metric = TRUST_INTERACTION_RATE
subject = CREATOR | MEDIA | DELIVERABLE
comparator = median of other items
cohort = exact same-media-set format/window/contract
minimum peers = 3
absolute floor = 1 percentage point
relative floor = 20%
coverage gate = 70%
zero baseline = above-signal only when subject >= 1 percentage point
no below-zero signal
```

### 6.3 Result-only metrics

The following remain Results only in V1:

```text
Campaign-level unique reach
Campaign-level aggregated reach
DELIVERABLE_EXECUTION_COMPLETION_RATE
Campaign-level counts
attributed outcomes
```

No registry entry means no Signal, Pattern or Learning.

### 6.4 Recurrence

Apply the accepted deterministic recurrence gates from Product/Plan:

```text
Pattern:
>= 3 Deliverables
>= 2 Creators
>= 70% coverage

Learning:
>= 5 Deliverables
>= 3 Creators
>= 80% coverage
or two compatible non-overlapping cohorts where Product permits
```

No causal language. No single-post Pattern or Learning. Preserve sample sizes, coverage, comparator basis and inconclusive states.

---

## 7. Provider/media boundary

P0 proved that the accepted first-party Instagram path has strong provider identity and Evidence fencing, but C04 publishing evidence does not yet provide an exact typed, immutable join to creator-published provider media.

Therefore:

```text
REPORTING-PV-01 = NOT_PROVEN_FOR_CREATOR_PUBLISHED_COLLABORATION_MEDIA
REPORTING-PV-02 = PROVEN_ONLY_FOR_ACCEPTED_FIRST_PARTY_ACCOUNT_PATH
REPORTING-PV-03 = INCOMPLETE_FAIL_CLOSED
```

Do not infer media identity from:

```text
caption
handle
creator name
timestamp
permalink text
free-form evidenceRef
unfenced JSON metadata
```

Do not add a C04/provider identity contract, provider permission or Settings workflow unless an already accepted exact source contract is discovered during convergence and can be used without reinterpretation.

If no exact typed C04→provider media identity exists, production behavior must be:

```text
AWARENESS/TRUST provider metrics = capability-gated unavailable
ACTION primary attributed outcome = unavailable
ASSETS = fully operational
```

This is an accepted capability outcome, not a circuit breaker.

You may implement and test deterministic calculators using bounded provider-neutral fixture inputs, but production admission must require exact fenced Evidence. Fixture capability must never masquerade as production availability.

No live Graph call is authorized. No live model call is authorized. No model is needed for Campaign Reporting V1.

---

## 8. P1 — ASSETS-first backend vertical slice

Implement:

```text
Campaign + canonical C04 obligations
→ immutable input manifest
→ DELIVERABLE_EXECUTION_COMPLETION_RATE
→ Result
→ atomic shared generation/current
→ strict Campaign Reporting consumer
```

### 8.1 Required scope

- Add `CAMPAIGN` subject support with exact same-Brand Campaign fencing.
- Register `campaign_reporting_v1` and its strict root/output contracts.
- Add only the bounded shared root generation/current adaptation proven necessary by P0.
- Prefer one ordered additive migration after the exact 98-migration convergence base.
- Preserve PostgreSQL enum-transaction safety. Never add `CAMPAIGN` to a PostgreSQL enum and then unsafely reference that newly added enum value in a same-transaction constraint/default/data statement.
- If the accepted invariant cannot be implemented safely in one migration, two tightly coupled ordered migrations are authorized: first the enum addition, then the dependent schema/constraint change. Record the reason, exact checksums and clean/upgrade proof. This safety split is not scope expansion.
- Do not add a parallel Reporting persistence stack.
- Persist immutable calculation, manifest identity, contract versions, provenance, history and accepted current.
- Calculate ASSETS Campaign-to-date only.
- Emit approved/published/remaining counts only when semantically supported.
- Preserve missing/unavailable as `null`, never zero.
- Implement exact execution identity and deterministic canonical hashing.
- Reject stale Campaign version, C04 manifest and cross-Brand/Campaign substitutions.
- Exact replay must reuse the same accepted result without new generations/current rows.
- A failed changed execution must preserve the prior valid current.
- Owner, Campaign Manager and Finance Admin may read; no frontend is required in P1.

### 8.2 PostgreSQL proof

Use disposable task-owned PostgreSQL 17.

Prove:

```text
clean 99-migration deployment
populated 98→99 upgrade
all predecessor migration digests/counts preserved
Campaign subject same-Brand FK/check
10-obligation / 8-approved = 80%
multi-Collaboration rate never exceeds 100%
manifest → generation → current lineage
replay/idempotency
failure-current preservation
tenant/Campaign/Collaboration/Deliverable isolation
C04 source truth unchanged
```

If implementation can safely reuse existing tables without a new migration, document exact proof and do not force a migration merely to meet the expected count. More than two new migrations, or any material schema family beyond the accepted Campaign-subject/root-current adaptation, requires `CAMPAIGN_REPORTING_SCHEMA_SCOPE_EXPANSION_REQUIRED`.

### 8.3 P1 checkpoint

Run focused contract, service, repository, PostgreSQL, migration, shared-runtime and predecessor tests; build; scoped lint/format; diff integrity; secret scan.

Commit and normally publish the accepted P1 checkpoint. Fetch back exact SHA/tree equality. Update the single authority ledger and publish P1 evidence before continuing.

---

## 9. P2 — media metrics and deterministic interpretation

Implement the complete provider-neutral calculation/interpretation layer, while preserving the production capability boundary in Section 7.

### 9.1 Objective and window semantics

```text
AWARENESS
= published Campaign media
+ usable admitted provider-performance Evidence

TRUST
= published Campaign media
+ enough compatible admitted Evidence for at least one Trust result

ASSETS
= canonical C04 obligations and execution states
provider Evidence not required

ACTION
= canonical published Campaign Deliverable/media
attribution not required for Report existence
primary outcome unavailable until authoritative attribution exists
```

Windows:

```text
CAMPAIGN_TO_DATE = all objectives
LAST_7D_CONTENT = media publication cohort only
LAST_30D_CONTENT = media publication cohort only
ASSETS = CAMPAIGN_TO_DATE only
ACTION primary attribution = CAMPAIGN_TO_DATE only in V1
```

A 7D/30D content cohort is never described as outcomes generated during that interval unless an authoritative attribution contract proves it.

### 9.2 Metrics

- Unique Campaign reach only from an authoritative deduplicated source.
- Otherwise label aggregated media reach explicitly non-unique.
- Trust rate: `(comments + saves + shares) / reach`; likes excluded.
- Trust numerator and denominator must use the exact same eligible media set.
- Preserve observed zero separately from missing, unsupported, suppressed and provider failure.
- Enforce metric/media capability allowlists.
- Use latest admitted observation at immutable `asOf`; never silently mix observations across incompatible cutoffs.
- Every metric records availability, unit, numerator/denominator, eligible/included counts, coverage, observed-through, contract version and limitations.
- Action primary remains unavailable without authoritative attribution.

### 9.3 Interpretation

- Emit Results whenever the calculation is valid.
- Emit Signals only through `cr-mat-1.0`.
- Emit Patterns/Learnings only after recurrence/coverage gates.
- Deterministic statements only; no model calls or causal claims.
- Preserve comparator, sample size, coverage and Evidence refs.
- Prove permutation/hash stability and duplicate collapse.
- If materiality is inapplicable, emit Result only.

### 9.4 Production degradation

When the C04/provider lineage is not exact:

```text
do not persist fabricated metric Evidence
do not infer provider truth
do not block ASSETS
return stable provider/media-lineage unavailable limitations
```

Commit, normally publish, fetch back and record the P2 evidence checkpoint before continuing.

---

## 10. P3 — orchestration, API and frontend

### 10.1 Execution and triggers

Use shared request/worker/lease/retry/backoff primitives.

Authorized requests:

```text
first objective-specific eligibility
authoritative admitted Evidence change
canonical C04 obligation/state/publication change
Campaign completion
explicit REPORT_REFRESH
definition/version change
explicit missing-applicable-window execution
```

Campaign Page and Reporting GETs trigger zero execution, provider calls or recalculation.

Execution identity must include exact Campaign/version/objective, applicable root window set, immutable `asOf`, source/C04 manifest and all contract versions.

Manual refresh:

```text
server-authorized REPORT_REFRESH command
Owner = allowed when availableActions contains it
Campaign Manager = allowed when availableActions contains it
Finance Admin = denied
ARCHIVED = denied
idempotent
15-minute versioned technical cooldown
ALREADY_RUNNING and COOLDOWN are explicit outcomes
```

Source refresh, when applicable, is requested only through the owning provider port. Reporting never retrieves provider data directly.

ARCHIVED behavior:

```text
accepted current/history readable
no manual refresh
no automatic late-Evidence recalculation in V1
Campaign lifecycle remains separate from Report finality
no ARCHIVED_CURRENT enum
```

### 10.2 Backend consumer

Implement strict authenticated Campaign-scoped surfaces consistent with existing route conventions. Preferred semantics:

```text
GET  /api/v1/brand-uce/campaigns/:campaignId/reporting
POST /api/v1/brand-uce/campaigns/:campaignId/reporting/refresh
```

If exact existing route conventions require a bounded variant, document it and preserve the semantic ownership.

Server resolves Brand/Campaign membership. No caller-selected Brand scope.

Response must include:

```text
contract version
Campaign context/objective/lifecycle/version
requested window
state
availableActions
current calculation or null
latest attempt
limitations
freshness/coverage/provenance-safe summaries
```

State rules:

```text
initial running + no current = PROCESSING
running + valid current = retain current state/current; latestAttempt RUNNING
failed + valid current = FAILED_CURRENT_PRESERVED
failed + no current = FAILED_NO_CURRENT
```

Never expose raw provider payloads, credentials, signed locators, prompts, leases, private runtime internals or unrelated domains.

### 10.3 Campaign Attention layer

Campaign Attention Reporting summary is always Campaign-to-date.

It shows truthful objective-specific primary/supporting outcome, readiness/current/freshness/limitations and `View Full Report` when appropriate. It must not make unavailable metrics appear as zero.

Keep exactly three Campaign operational workspaces:

```text
Discovery
Applicants
Collaborations
```

Reporting is not a fourth tab/workspace.

### 10.4 Full Reporting page

Implement directly addressable nested route:

```text
/brand/uce/campaigns/:id/reporting
```

Window is request/URL state:

```text
CAMPAIGN_TO_DATE
LAST_7D_CONTENT
LAST_30D_CONTENT
```

Default is Campaign-to-date. Window changes do not mutate Campaign, create Campaign versions or persist a Campaign preference.

The responsive page includes only truthfully available:

```text
Campaign/report context
objective outcome
window selector
primary/supporting metrics
coverage/freshness/limitations
Results
Signals
Patterns
Learnings
creator/content breakdowns
current/attempt degradation
clear return to parent Campaign
```

Frontend consumes strict server DTOs and `availableActions`; it performs no metric, materiality, eligibility or role calculation.

Required states:

```text
loading
not eligible
processing without current
current
partial/unavailable provider capability
failed with preserved current
failed without current
archived read-only
window not applicable
API error
```

Owner/Manager show Refresh only when supplied by `availableActions`. Finance and Archived never show it.

Preserve existing Campaign header, Attention layout, Discovery/Applicants/Collaborations workspaces and all Campaign/C04 authorization behavior.

Commit, normally publish, fetch back and record the P3 evidence checkpoint before continuing.

---

## 11. P4 — integrated acceptance and closeout

Run the complete changed-surface and cross-boundary acceptance suite.

### 11.1 PostgreSQL/runtime

- Fresh PostgreSQL 17 clean deployment.
- Populated predecessor upgrade from exact converged 98-migration state.
- Migration status current and exact inventory/digests.
- Production backend build and boot.
- `/health/live` HTTP 200.
- `/health` HTTP 200 with PostgreSQL connectivity.
- Real fixture lineage from Campaign/C04/Evidence through generation/current/API/UI.
- Exact replay with zero duplicate generation/current rows.
- Failed changed execution preserves valid current.
- Cross-Brand/Campaign/Collaboration/Deliverable/media/Evidence substitutions rejected.
- C04, Campaign, Settings/provider and admitted Evidence truth remain unchanged.

### 11.2 Required fixtures

Run every Product/Plan fixture:

```text
RPT-01 through RPT-31
```

This includes, without weakening:

```text
objective-specific eligibility
all valid/inapplicable window semantics
8/10 multi-Collaboration ASSETS proof
ASSETS without provider connection
ACTION Report with unavailable attribution
GET causes zero execution/provider calls
Owner/Manager refresh
Finance denial
refresh replay/cooldown/already-running
ARCHIVED read/no-refresh/no-auto-recalculation
lifecycle/finality separation
URL window changes cause no Campaign mutation
provider-unavailable truthful degradation
failed-current preservation
materiality-absent Result-only behavior
permutation/hash stability
tenant/source isolation
```

### 11.3 Backend regressions

At minimum:

```text
Campaign Page/query/authorization
Discovery/Applicants/Collaborations
C03/C04 handoff and Deliverable state
Brand Settings/provider authorization fencing
shared DE/Evidence lifecycle
shared Intelligence execution/generation/current/CAS
accepted Instagram provider-neutral paths
Campaign objective/lifecycle contracts
```

Do not run unrelated full suites unless shared-surface changes require them. Any materially changed shared runtime requires its complete accepted regression family.

### 11.4 Frontend/browser/accessibility

Run production API→database→UI browser proof for:

```text
390
767
768
desktop 1440
```

Prove:

```text
authenticated Owner read/refresh
authenticated Campaign Manager read/refresh
authenticated Finance Admin read/no refresh
inactive/unauthorized denial
anonymous API 401
Campaign Attention summary
nested full page and parent return
URL-backed window selection
existing three workspaces unchanged
processing/current/partial/unavailable/failure-preserved/archived states
keyboard and visible focus
no horizontal overflow
Axe serious = 0
Axe critical = 0
console errors = 0
uncaught page errors = 0
no external provider/model traffic
```

### 11.5 Static/integrity/security

- Immutable dependency install.
- Prisma generate/validate.
- Backend/frontend production builds.
- TypeScript checks.
- Scoped ESLint and formatting.
- `git diff --check`.
- Package/lockfile/schema/migration identity accounting.
- Secret/private-key/token/signed-locator/raw-media/provider-payload/runtime-artifact scan.
- No `.env` or browser state tracked.
- No raw provider media persisted.

---

## 12. Authority/evidence artifacts

Use the existing single directory and ledger:

```text
docs/ai-collaboration/campaign-page/campaign-reporting-v1/
```

Before backend/frontend mutation, publish:

```text
P0_TECHNICAL_SA_ACCEPTANCE_AND_P1_P4_AUTHORIZATION.md
```

It must copy the exact adjudication in Section 1 without reinterpretation.

For each packet publish one evidence artifact and update the single `EXECUTION_LEDGER.md`:

```text
P1_ASSETS_BACKEND_VERTICAL_SLICE.md
P2_MEDIA_METRICS_AND_DETERMINISTIC_INTERPRETATION.md
P3_ORCHESTRATION_API_AND_FRONTEND.md
P4_INTEGRATED_ACCEPTANCE_AND_CLOSEOUT.md
```

At final closeout also publish:

```text
CAMPAIGN_REPORTING_V1_DEVELOPER_HANDOFF.md
CAMPAIGN_REPORTING_V1_FINAL_ACCEPTANCE_AND_PRODUCT_LEARNING_HANDOFF_V1.md
```

Archive this exact runner prompt under:

```text
docs/ai-collaboration/campaign-page/campaign-reporting-v1/runner-prompts/
CAMPAIGN_REPORTING_P1_P4_AUTONOMOUS_LOCAL_CODEX_EXECUTION_PROMPT_V1.md
```

Prove transferred prompt/archive raw or normalized-content equality and record SHA-256.

Do not create a second ledger or another Campaign Reporting program.

---

## 13. Commit, publication and cleanup discipline

Maintain ordered immutable checkpoints:

```text
authority P0 acceptance
backend/frontend convergence
P1
P2
P3
P4/final handoff
```

Normal commits only. Normal non-force pushes only. No history rewrite, rebase of accepted published checkpoints or force operation.

After each published checkpoint:

```text
independent fetch/prune
local/fetched SHA equality
local/fetched tree equality
required predecessor ancestry
clean task worktree
```

Clean up only exact task-owned processes, browsers, temporary media, databases, containers, volumes, logs and ignored build outputs. Never delete or alter unrelated resources. If policy blocks deletion of a verified task-owned ignored artifact, report it precisely; do not bypass policy or fail accepted source work solely for harmless unpublished residue.

---

## 14. Circuit breakers

Stop and return an immutable report if any occurs:

```text
CAMPAIGN_REPORTING_STARTING_STATE_MISMATCH
CAMPAIGN_REPORTING_AUTHORITY_STATE_MISMATCH
CAMPAIGN_REPORTING_CONVERGENCE_SCOPE_EXPANSION_REQUIRED
CAMPAIGN_REPORTING_SCHEMA_SCOPE_EXPANSION_REQUIRED
CAMPAIGN_REPORTING_SHARED_RUNTIME_SCOPE_EXPANSION_REQUIRED
CAMPAIGN_REPORTING_PRODUCT_CONFLICT
C04_STATE_MAPPING_AUTHORITY_GAP
C04_PUBLISHED_ELIGIBILITY_AUTHORITY_GAP
CAMPAIGN_OBJECTIVE_AUTHORITY_GAP
CAMPAIGN_PROVIDER_MEDIA_IDENTITY_AUTHORITY_CONFLICT
CAMPAIGN_REPORTING_TENANT_ISOLATION_CONFLICT
CAMPAIGN_REPORTING_ATOMIC_CURRENT_UNSAFE
```

Do not treat the following as circuit breakers:

```text
creator-published provider metrics unavailable
live credentials unavailable
live Graph/model calls prohibited
ordinary DTO/mapper/test/frontend defects
bounded merge/type corrections inside reviewed convergence overlap
bounded fixture/harness defects
```

If stopped after an accepted packet, preserve and publish that packet's exact checkpoint/evidence when safe. Do not silently consume a later packet or correction cycle.

---

## 15. Required final immutable return

After successful P4, return exactly:

```text
CAMPAIGN_REPORTING_V1_FINAL_ACCEPTANCE_AND_PRODUCT_LEARNING_HANDOFF_V1
```

Include:

```text
A. exact starting authority/backend/frontend SHA/tree/worktree proof
B. standards/Product/P0 reconciliation
C. authority acceptance publication
D. backend/frontend convergence commits and both-parent ancestry
E. exact changed-file inventories and statistics by packet
F. schema/migration identity and populated-upgrade proof
G. Campaign subject and atomic root/current architecture
H. C04 obligation identity and numerator mapping
I. 8/10 ASSETS calculation proof
J. objective eligibility and window matrix
K. provider/media capability and fail-closed production boundary
L. metric registry and missing-not-zero proof
M. materiality registry, Signals/Patterns/Learnings proof
N. execution identity, replay and permutation/hash stability
O. failure-current and stale-manifest preservation
P. trigger/manual-refresh/cooldown/archived behavior
Q. authenticated API/roles/availableActions proof
R. Attention Layer and full-page frontend proof
S. PostgreSQL lineage and row counts
T. tenant/Campaign/C04/media/Evidence isolation
U. Campaign/C04/Settings/provider non-mutation
V. RPT-01 through RPT-31 matrix
W. backend/frontend regression results
X. browser/accessibility matrix
Y. build/typecheck/lint/format/diff results
Z. dependency and secret/forbidden-artifact scan
AA. live-call/provider-mutation proof
AB. backend publication/fetch-back SHA/tree equality
AC. frontend publication/fetch-back SHA/tree equality
AD. authority publication/fetch-back SHA/tree equality
AE. prompt archive digest equality
AF. final cleanup/worktree/runtime state
AG. primary-run/correction-cycle accounting by packet
AH. implementation learning and deferred capability debt
AI. exact blocker, if any
```

Success footer:

```text
RESULT =
READY_FOR_CAMPAIGN_REPORTING_TECHNICAL_SA_FINAL_REVIEW

P0 = ACCEPTED
P1 = ACCEPTED
P2 = ACCEPTED
P3 = ACCEPTED
P4_EVIDENCE_READY = YES

CAMPAIGN_REPORTING_V1_TECHNICAL_CLOSEOUT_READY = YES

AWARENESS_TRUST_PROVIDER_METRICS =
AVAILABLE_ONLY_IF_EXACT_TYPED_C04_PROVIDER_MEDIA_LINEAGE_PROVED
OTHERWISE_TRUTHFULLY_UNAVAILABLE

LIVE_GRAPH_CALLS = NONE
LIVE_MODEL_CALLS = NONE
META_PROVIDER_MUTATIONS = NONE

DEVELOPMENT_MERGE = NOT_PERFORMED
DEPLOYMENT = NOT_PERFORMED

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CAMPAIGN REPORTING TECHNICAL SA
FOR FINAL REVIEW
```

---

## 16. Final instruction

Begin with exact authority/source verification and the required normal convergence checkpoints.

Continue autonomously through P1→P4 when each packet passes. Preserve provider truth and missing-not-zero semantics. Do not weaken C04, tenant, Evidence, current or authorization invariants to manufacture a complete-looking Report.

The objective is a production-shaped, truthful Campaign Reporting V1 whose ASSETS path is fully operational and whose provider-dependent paths are operational only to the exact capability proven by admitted, fenced Evidence.
