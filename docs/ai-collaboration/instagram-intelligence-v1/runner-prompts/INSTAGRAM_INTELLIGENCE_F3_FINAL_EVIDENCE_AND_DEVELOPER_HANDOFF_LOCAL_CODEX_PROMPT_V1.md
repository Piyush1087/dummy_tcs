# INSTAGRAM INTELLIGENCE — F3 FINAL EVIDENCE + DEVELOPER HANDOFF — EXTERNAL LOCAL CODEX EXECUTION V1

`INSTAGRAM_INTELLIGENCE_F3_FINAL_EVIDENCE_AND_DEVELOPER_HANDOFF_LOCAL_CODEX_EXECUTION_V1`

You are the authorized **External Local Codex runner** for the final bounded packet of the Creator Shop Instagram Intelligence V1 program.

This attached Markdown file is the complete execution instruction. Do not reinterpret Product, UX, provider scope, architecture or accepted implementation.

Preserve this exact prompt file in the authority repository at:

```text
docs/ai-collaboration/instagram-intelligence-v1/runner-prompts/INSTAGRAM_INTELLIGENCE_F3_FINAL_EVIDENCE_AND_DEVELOPER_HANDOFF_LOCAL_CODEX_PROMPT_V1.md
```

Include it in the final authority publication only when the F3 documentation checkpoint is evidence-ready. If execution stops before publication, leave the repositories unchanged and report that the prompt was not published.

---

# 1. ACCEPTED AUTHORITY

The Child-SA / Program Orchestrator has accepted:

```text
A1 / A2 / A3 = ACCEPTED
B1 / B2 / B3A / B4 / B3B = ACCEPTED
C2 / C3 / C4 / C1 = ACCEPTED
D_COMBINED = ACCEPTED
HIDDEN_BRAND_LANE = ACCEPTED_FOR_V1
E1 / E2_E3_COMBINED / E4 = ACCEPTED
F1_INTERNAL_GATE = ACCEPTED
F2_INTERNAL_GATE = ACCEPTED
F1_F2_COMBINED = ACCEPTED
```

F1/F2 acceptance is based on the immutable combined runner evidence published at the starting authority checkpoint below.

Current authorization:

```text
F3 = AUTHORIZED
IMPLEMENTATION = NOT AUTHORIZED
PRODUCT CHANGE = NOT AUTHORIZED
ARCHITECTURE CHANGE = NOT AUTHORIZED
UX CHANGE = NOT AUTHORIZED
SCHEMA / MIGRATION CHANGE = NOT AUTHORIZED
DEPENDENCY CHANGE = NOT AUTHORIZED
DEPLOYMENT / MERGE TO DEVELOPMENT = NOT AUTHORIZED
```

F3 is a small, documentation-led final evidence and developer-handoff packet. It is not another implementation or full-acceptance run.

The runner must not self-accept F3 or the overall program. It produces immutable closeout evidence for Child-SA / Program-Orchestrator and Parent review.

---

# 2. EXACT STARTING CHECKPOINTS

Use the existing local repositories and established Instagram program branches.

## Backend

```text
REPOSITORY = Piyush1087/creator-commerce-backend-v2-clone
BRANCH = program/instagram-intelligence-v1-backend
SHA = d5b9de557ed983a9c3af26b1edc305ba8b77d6f3
TREE = 67d2642871899fda9e7bd9248b8d56892c3d6e6d
MIGRATION_COUNT = 90
```

## Frontend

```text
REPOSITORY = Piyush1087/creator-commerce-frontend-v2-clone
BRANCH = program/instagram-intelligence-v1-frontend
SHA = 5866d0ac82f742957f53a8db2144a9a166628b75
TREE = 7c095290ffdef4c84f37382bc594bef536c92a49
```

## Program authority

```text
REPOSITORY = Piyush1087/dummy_tcs
BRANCH = program/instagram-intelligence-v1-authority
SHA = 9356269285d0ee87d85f17dd4e1894233526c1d3
TREE = f2361bf27d062a64a7acf04ea654ec2a6a47ce5c
```

Before any documentation mutation:

1. Resolve the three exact local repository paths.
2. Run normal `fetch --prune`.
3. Prove local SHA/tree equals fetched remote SHA/tree for all repositories.
4. Prove accepted predecessor ancestry.
5. Prove all worktrees are clean with no staged, unstaged, untracked or overlapping user work.
6. Verify the backend still contains exactly 90 accepted migrations.
7. Verify the F1/F2 authority artifact, prompt and execution-ledger update exist at the authority checkpoint.

If any identity, ancestry, migration inventory, authority artifact or clean-worktree gate differs, stop without mutation and return:

```text
F3_STARTING_STATE_MISMATCH
```

Do not reset, clean, stash, discard, force checkout or rewrite history.

---

# 3. REQUIRED RECONCILIATION

Read the applicable current repository instructions and accepted authority, including:

```text
all applicable AGENTS.md files
AI_ENGINEERING_STANDARD.md
accepted Instagram V1 definitive architecture and plan
V1.1 canonicalization
accepted post-B2 optimized execution plan
single Instagram execution ledger
accepted checkpoint artifacts from A1 through F1/F2
current contract registry and consumer contracts
current Settings Instagram integration authority
current Brand Centre navigation/workspace authority
current deferred-debt records, if present
```

Treat committed accepted authority and the exact starting trees as authoritative. Preserve committed development terminology for Campaign vocabulary.

Do not reopen accepted decisions or independently infer new requirements.

---

# 4. F3 PURPOSE

Produce the durable closeout record needed by a developer to understand, integrate, operate and continue the accepted Instagram Intelligence V1 without replaying the full execution history.

F3 contains only:

```text
final evidence canonicalization
developer handoff
deferred-debt canonicalization
optional bounded live Graph read smoke
single execution-ledger closeout
normal authority publication and fetch-back
```

Do not repeat F1/F2 runtime suites. Their accepted immutable evidence is the authority. Run only small read-only checks required to verify documentation claims and repository identities.

---

# 5. FINAL EVIDENCE ARTIFACT

Create or update the canonical authority artifact at:

```text
docs/ai-collaboration/instagram-intelligence-v1/F3_FINAL_EVIDENCE_AND_DEVELOPER_HANDOFF.md
```

If the accepted authority already defines a different canonical F3 filename, preserve that existing path instead of creating a duplicate and report the resolved path.

The final evidence section must include:

```text
program/module identity and scope
accepted Product, UX, provider and architecture boundaries
complete accepted packet/checkpoint sequence
exact final backend/frontend/authority branches, SHAs and trees
backend migration count = 90
provider-neutral acceptance classification
F1/F2 integrated acceptance summary and evidence link/path
test, PostgreSQL, browser, role, isolation, delete-data,
failure-current, accessibility and security gate summary
live Graph/model/provider-mutation truth
known non-blocking inherited diagnostics
normal publication/fetch-back status
what has not been deployed or merged
```

Do not copy thousands of individual test assertions into the handoff. Link to accepted immutable evidence and summarize the developer-relevant result.

---

# 6. DEVELOPER HANDOFF CONTENT

Create or update one canonical developer handoff at:

```text
docs/ai-collaboration/instagram-intelligence-v1/INSTAGRAM_INTELLIGENCE_V1_DEVELOPER_HANDOFF.md
```

If an accepted canonical handoff already exists, update it rather than creating a parallel document.

The handoff must be concise but operationally complete.

## 6.1 What was built

Cover:

```text
Settings-owned Instagram connection projection
provider-neutral Instagram Graph adapter and truth semantics
Instagram DE Resources/Captures/Artifacts/Evidence
30-day eligible-media corpus
bounded deep corpus of up to 24 posts
V1 image/carousel-cover/video-cover inspection behavior
C2 deterministic foundations
C3 per-media semantic observations and likely_collab
C4 three Instagram Intelligence Objects and complete consumer
C1 refresh automation and manual refresh
hidden Instagram → Brand source-native lane
six-workspace Brand Centre navigation
singular Instagram workspace
media-detail drawer/mobile sheet
```

## 6.2 Runtime and source map

Identify the principal backend/frontend modules, public/internal routes, execution entry points, schedulers/workers, processor bundles, consumer schemas, frontend routes and key tests by repository-relative path.

Include the accepted API routes:

```text
GET /api/v1/brand-centre/instagram
GET /api/v1/brand-centre/instagram/media/:mediaId
```

List other accepted refresh/internal routes only after verifying their exact committed names. Do not invent route names.

## 6.3 Runtime behavior

Record:

```text
window = fixed 30 days for V1
initial connection = immediate
profile/media/performance = daily
audience = weekly
dispatcher = hourly with due gates + jitter
manual refresh cooldown = 15 minutes
Brand Owner = refresh allowed
Campaign Manager = refresh allowed
Finance Admin = read-only / refresh denied
```

Explain current preservation during refresh/failure, readiness/freshness/partial behavior and unavailable-versus-zero semantics.

## 6.4 Ownership boundaries

State explicitly:

```text
Settings owns connect/reconnect/account conflict/disconnect/delete-data
Instagram owns source-native observations and three Instagram Objects
hidden Instagram-derived Brand generations do not directly mutate canonical/manual Brand truth
Offering links obey exact same-Brand source grounding and do not mutate Offering truth
likely_collab is an inference, not canonical Collaboration truth
Instagram does not own canonical Creator identity
Instagram does not own Campaign configuration or future Campaign Performance evaluation
```

Campaign objective authority is:

```text
AWARENESS / TRUST / ASSETS / ACTION
```

`PULSE / PROOF / PRODUCTION / PUSH` is legacy only.

## 6.5 Data, security and deletion

Document:

```text
tenant/account/authorization-generation fencing
Capture → completed capturedAt → Evidence ordering
MODEL_DERIVATION lineage
SSRF/redirect/hostname defenses
65,536-byte streamed locator response limit
temporary media containment and cleanup
no raw media persistence
Settings-owned delete-data graph and preservation boundary
credential ownership and containment
```

List only existing environment-variable **names** that a developer needs, and only after locating them in committed code/configuration. Never copy or infer values. Never commit `.env` files.

## 6.6 Local verification and integration

Provide the smallest reliable commands for:

```text
dependency install
Prisma generation/validation
90-migration apply/status
backend/frontend build
focused Instagram test entry points
provider-neutral local runtime
authenticated browser proof where needed
```

Use exact repository scripts. Do not invent commands.

State that the program branches have not been merged into `development` and that merge/integration/deployment remains the developer/Parent responsibility.

## 6.7 Known limitations and deferred work

Reconcile the existing debt register. At minimum ensure it truthfully records:

```text
7-day and 14-day window toggles are post-V1; V1 is fixed at 30 days
multi-frame Reel/video analysis deferred
full carousel-child visual sweep deferred
audio analysis and transcription deferred
video pacing/temporal interpretation deferred
video-specific creative-structure conclusions deferred
semantic editing/manual override is a future shared capability
likely_collab does not become canonical Collaboration truth
Creator enrichment/follower-tier ownership remains outside Instagram V1
optional live Graph read may remain unexecuted without invalidating provider-neutral acceptance
existing repository-wide dependency/lint diagnostics are not silently reclassified as Instagram defects
accepted lower-severity workspace landmark findings remain recorded if still applicable
```

Do not invent new V2 Product scope. Distinguish explicit deferrals from operational follow-ups and inherited repository debt.

---

# 7. OPTIONAL LIVE GRAPH READ SMOKE

This lane is:

```text
OPTIONAL
READ-ONLY
NON-BLOCKING WHEN CREDENTIALS OR SUITABLE DATA ARE UNAVAILABLE
```

Attempt it only if existing Settings-managed local runtime credentials already work without any OAuth, permission, application configuration or provider mutation.

Do not request or emit credential values. Do not create, exchange, refresh or expose tokens solely for this smoke. Do not change Meta application settings, permissions, access levels, products, redirect URIs, webhooks, deauthorization or data-deletion configuration.

If safely available, perform at most:

```text
one profile read
one media-page read
one naturally available supported insight read
one carousel read only if naturally present
```

Do not download raw media and do not invoke a live model.

Record only sanitized contract-level evidence such as request category, HTTP/result class, pagination presence, field presence/absence, capability availability and failure category. Do not include handles, captions, media IDs, provider account IDs, tokens, signed URLs, raw payloads or user data.

Disposition must be exactly one of:

```text
PASS
NOT_RUN_CREDENTIALS_UNAVAILABLE_NON_BLOCKING
NOT_RUN_SUITABLE_DATA_UNAVAILABLE_NON_BLOCKING
FAILED_ENVIRONMENTAL_NON_BLOCKING
LIVE_PROVIDER_CONTRADICTION_REQUIRES_PARENT_REVIEW
```

If an executed read materially contradicts the accepted provider contract, do not reinterpret or repair the provider architecture. Stop with the contradiction disposition and preserve exact sanitized evidence for Parent review.

The absence of live credentials or suitable media is not a V1 blocker because provider-neutral acceptance is complete.

---

# 8. PROHIBITED WORK

Do not:

```text
change backend or frontend source
change Prisma schema or migrations
change dependencies or lockfiles
run a new broad/full acceptance suite
change Product, UX, provider scope or architecture
add OAuth, permissions, webhooks, publishing, DMs,
Business Discovery or Creator Marketplace behavior
change Settings lifecycle ownership
change the 30-day V1 window
defer or remove the accepted hidden Brand lane
expose hidden Brand results in Instagram
create canonical Creator or Collaboration truth
change Offering or Campaign truth
merge to development
deploy to AWS or production
force push or rewrite history
create a second execution plan or ledger
```

If a code/runtime correction is required, stop and return:

```text
F3_SCOPE_EXPANSION_REQUIRED
```

with exact reasoning. Do not implement the correction.

---

# 9. LEDGER AND PUBLICATION

Update the single existing execution ledger to record:

```text
E4 = ACCEPTED
F1 = ACCEPTED
F2 = ACCEPTED
F1_F2_COMBINED = ACCEPTED
F3 = EVIDENCE_READY_AWAITING_CHILD_SA_PROGRAM_ORCHESTRATOR_ACCEPTANCE
F3_ACCEPTED = NO
INSTAGRAM_INTELLIGENCE_V1 = TECHNICAL_CLOSEOUT_AWAITING_FINAL_REVIEW
```

Do not self-record final acceptance.

The only expected repository mutation is authority documentation:

```text
F3 final evidence artifact
developer handoff
existing debt register update, if applicable
single ledger update
exact runner-prompt file
```

Do not modify backend/frontend files.

After completing and validating the documentation:

1. Run `git diff --check` and documentation/path/link integrity checks.
2. Run a secret and forbidden-artifact scan over planned authority changes.
3. Commit on the established authority branch.
4. Push normally without force.
5. Fetch/prune independently.
6. Prove local/fetched SHA and tree equality.
7. Prove the starting authority checkpoint is an ancestor.
8. Prove backend/frontend remain byte-identical to their starting checkpoints.
9. Prove all worktrees are clean.

Do not merge any program branch into `development`.

---

# 10. REQUIRED IMMUTABLE RETURN CONTRACT

Return:

# `INSTAGRAM_INTELLIGENCE_F3_EXTERNAL_LOCAL_CODEX_REPORT_V1`

with:

```text
A. exact starting SHA/tree/worktree verification
B. standards and accepted-authority reconciliation
C. F1/F2 acceptance preservation
D. changed-file inventory and diff statistics
E. exact final-evidence artifact path and summary
F. exact developer-handoff artifact path and summary
G. runtime/source-map completeness
H. roles, cadence, state and ownership documentation
I. security, data, deletion and environment-name documentation
J. local verification/integration commands
K. deferred-debt register disposition
L. optional live Graph read disposition and sanitized evidence
M. provider-neutral versus live-provider readiness classification
N. backend/frontend unchanged proof
O. schema/migration/dependency identity
P. documentation validation, diff and secret scan
Q. authority publication/fetch-back equality
R. final worktree/runtime cleanup
S. exact blocker, if any
```

End with:

```text
RESULT =
READY_FOR_CHILD_SA_PROGRAM_ORCHESTRATOR_FINAL_REVIEW
|
F3_CORRECTION_REQUIRED
|
F3_SCOPE_EXPANSION_REQUIRED
|
F3_STARTING_STATE_MISMATCH
|
F3_LIVE_PROVIDER_CONTRADICTION

F1 =
ACCEPTED

F2 =
ACCEPTED

F3_ACCEPTED =
NO — AWAITING CHILD-SA / PROGRAM-ORCHESTRATOR REVIEW

F3_EVIDENCE_READY =
YES | NO

DEVELOPER_HANDOFF_READY =
YES | NO

INSTAGRAM_INTELLIGENCE_V1_TECHNICAL_CLOSEOUT_READY =
YES | NO

OPTIONAL_LIVE_GRAPH_READ =
PASS
|
NOT_RUN_CREDENTIALS_UNAVAILABLE_NON_BLOCKING
|
NOT_RUN_SUITABLE_DATA_UNAVAILABLE_NON_BLOCKING
|
FAILED_ENVIRONMENTAL_NON_BLOCKING
|
LIVE_PROVIDER_CONTRADICTION_REQUIRES_PARENT_REVIEW

LIVE_MODEL_CALLS =
NONE

META_PROVIDER_MUTATIONS =
NONE

RAW_MEDIA_PERSISTED =
NO

BACKEND_CHANGED =
NO

FRONTEND_CHANGED =
NO

NEW_MIGRATIONS =
NONE

DEVELOPMENT_MERGE =
NOT_PERFORMED

DEPLOYMENT =
NOT_PERFORMED

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CHILD SA / PROGRAM ORCHESTRATOR
FOR FINAL REVIEW
```

Then stop.

---

# 11. F3 ACCEPTANCE CRITERIA

F3 is evidence-ready only if:

```text
exact starting checkpoints and clean worktrees match
F1/F2 accepted evidence remains intact
final evidence is accurate and compact
developer handoff is operationally sufficient
exact source paths/routes/scripts are repository-verified
all ownership boundaries are preserved
all explicit V1.1/V2 deferrals are recorded without invention
optional live-read outcome is classified truthfully
no secret, provider payload, raw media or user data is published
backend/frontend/schema/migrations/dependencies remain unchanged
authority documentation passes integrity checks
normal non-force publication and independent fetch-back succeed
all worktrees are clean
no merge or deployment occurs
F3 and overall program remain awaiting external acceptance
```

Then return the immutable report and wait.
