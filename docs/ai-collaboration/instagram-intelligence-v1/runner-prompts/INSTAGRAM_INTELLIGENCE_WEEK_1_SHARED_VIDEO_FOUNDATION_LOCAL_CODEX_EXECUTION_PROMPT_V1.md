# Instagram Intelligence — Week 1 Shared Video Foundation + Selected Brand Reel Analysis — External Local Codex Execution V1

`INSTAGRAM_INTELLIGENCE_WEEK_1_SHARED_VIDEO_FOUNDATION_LOCAL_CODEX_EXECUTION_V1`

You are the authorized **External Local Codex runner** for Week 1 of Creator
Shop's post-V1 Instagram Intelligence incremental Reel capability program.

This file is the complete execution instruction. It is delivered from a
docs-only prompt branch for transport. That delivery branch is not Product or
execution authority and must not become an implementation base.

When and only when the run reaches an evidence-ready checkpoint, preserve this
exact prompt in the authority repository at:

```text
docs/ai-collaboration/instagram-intelligence-v1/runner-prompts/INSTAGRAM_INTELLIGENCE_WEEK_1_SHARED_VIDEO_FOUNDATION_LOCAL_CODEX_EXECUTION_PROMPT_V1.md
```

Do not start Week 2. Do not merge to `development`. Do not deploy.

---

## 1. Execution authority

The Parent has accepted:

```text
INSTAGRAM_INTELLIGENCE_V1 = TECHNICALLY_ACCEPTED
F1 = ACCEPTED
F2 = ACCEPTED
F3 = ACCEPTED
INCREMENTAL_REEL_RELEASE_CHARTER = ACCEPTED
WEEK_1_SHARED_VIDEO_FOUNDATION = AUTHORIZED
WEEKS_2_TO_8 = PLANNED_NOT_AUTHORIZED
```

Canonical Week 1 authority is:

```text
INSTAGRAM_INTELLIGENCE_INCREMENTAL_REEL_CAPABILITY_RELEASE_CHARTER_V1
```

This run may implement only:

```text
W1_SHARED_VIDEO_FOUNDATION_AND_SELECTED_BRAND_REEL_ANALYSIS
```

The runner does not self-accept Week 1. It produces immutable evidence for the
Child SA / Program Orchestrator.

---

## 2. Exact starting checkpoints

Use the existing local repositories and established program branches.

### Backend

```text
REPOSITORY = Piyush1087/creator-commerce-backend-v2-clone
BRANCH = program/instagram-intelligence-v1-backend
SHA = d5b9de557ed983a9c3af26b1edc305ba8b77d6f3
TREE = 67d2642871899fda9e7bd9248b8d56892c3d6e6d
EXPECTED_MIGRATION_COUNT = 90
```

### Frontend

```text
REPOSITORY = Piyush1087/creator-commerce-frontend-v2-clone
BRANCH = program/instagram-intelligence-v1-frontend
SHA = 5866d0ac82f742957f53a8db2144a9a166628b75
TREE = 7c095290ffdef4c84f37382bc594bef536c92a49
```

### Program authority

```text
REPOSITORY = Piyush1087/dummy_tcs
BRANCH = program/instagram-intelligence-v1-authority
SHA = 55efc544234758b2c694b3d5fbd3bdcf8b8877da
TREE = 37775870c7e91c21c44621097d032fb004d9f27c
```

Before mutation:

1. Resolve the exact three local repository paths.
2. Run normal `fetch --prune`.
3. Prove local SHA/tree equals fetched remote SHA/tree for every repository.
4. Prove accepted predecessor ancestry.
5. Prove all three worktrees are clean with no staged, unstaged, untracked, or
   overlapping user work.
6. Verify exactly 90 backend migrations and record the latest migration name
   and hash.
7. Verify that the exact charter file at the authority SHA is present and
   byte-readable.

If any starting identity, ancestry, migration inventory, or clean-worktree gate
differs, stop without mutation and return:

```text
W1_STARTING_STATE_MISMATCH
```

Do not reset, clean, stash, discard, force checkout, rewrite history, or operate
from the prompt-delivery branch.

---

## 3. Required authority and source reconciliation

Read only the minimum sufficient applicable authority before implementation:

```text
all applicable repository AGENTS.md files
AI_ENGINEERING_STANDARD.md
backend/database/validation/AI-integration standards
INSTAGRAM_INTELLIGENCE_INCREMENTAL_REEL_CAPABILITY_RELEASE_CHARTER_V1.md
INSTAGRAM_INTELLIGENCE_V1_DEVELOPER_HANDOFF.md
F3_FINAL_EVIDENCE_AND_DEVELOPER_HANDOFF.md
POST_B2_OPTIMIZED_EXECUTION_PLAN_V1.md
single EXECUTION_LEDGER.md
B2_PROVIDER_TRUTH_AND_PAGINATION.md
B3A_MINIMUM_IMAGE_MEDIA_PATH.md
B3B_THINNER_V1_MEDIA_COMPLETION.md
C2_DETERMINISTIC_FOUNDATIONS.md
C3_MEDIA_OBSERVATIONS_AND_LIKELY_COLLAB.md
C4_THREE_OBJECTS_AND_COMPLETE_CONSUMER.md
C1_SYNC_COORDINATOR.md
F1_F2_COMBINED_INTEGRATED_ACCEPTANCE.md
```

Inspect the actual current implementation, including:

```text
Instagram provider client/types/config
Settings-authorized Instagram reads
locator response bounding
secure image downloader and temporary store
B3A image pipeline and low-level visual model port
B3B selector and media-completion pipeline
Instagram Capture/Evidence writer and purge path
C2 deterministic foundations
C3 semantic input/finalization/provenance
C4 three-Object generation/current and media-detail consumer
C1 sync execution identity/cadence
hidden Brand lane input assumptions
backend production build/container/deployment packaging
frontend aggregate/media-detail schemas and mounted views
```

Preserve committed Product authority. Do not independently reinterpret Campaign,
Collaboration, Creator, Offering, Settings, Brand, or Instagram ownership.

---

## 4. Week 1 objective

Implement the first reusable Reel/video foundation and use it only for already
deep-selected Brand Reel/video media.

Required path:

```text
Settings-authorized provider fixture/replay
-> bounded video-source locator
-> secure temporary video acquisition
-> container/duration/size validation
-> deterministic timestamp selection
-> temporary frame extraction
-> per-frame low-level visual observation
-> completed Capture and exact Evidence
-> C3 per-media semantics using exact frame Evidence subsets
-> existing C4 three-Object generation/current
-> existing consumer/media-detail projection where necessary
```

This is a new modality depth for selected Reel/video media, not a new
Intelligence engine or Object family.

---

## 5. Corpus and selection boundary

Preserve exactly:

```text
WINDOW = 30 DAYS
ELIGIBLE_MEDIA_CAP = 500
DEEP_SELECTED_CORPUS_CAP = 24 POSTS ACROSS FORMATS
```

All eligible posts continue to receive metadata, caption, metrics, and coverage.

Only already deep-selected media normalized as:

```text
REEL
REELS
VIDEO
```

receive Week 1 multi-frame inspection.

Unselected video remains:

```text
VISUAL_INSPECTION = NOT_INSPECTED
VISUAL_SEMANTIC_RESULT = UNKNOWN
```

Week 1 must not change selected IMAGE or CAROUSEL behavior. Full carousel-child
inspection belongs to Week 2.

---

## 6. Frozen technical profile

Use versioned constants:

```text
VIDEO_ANALYSIS_PROFILE = selected-reel-frames-v1
MAX_VIDEO_DOWNLOAD_BYTES = 104857600
MAX_VIDEO_DURATION_SECONDS = 180
MAX_SELECTED_FRAMES_PER_VIDEO = 6
FRAME_LONG_EDGE_MAX_PIXELS = 1280
FRAME_ENCODED_MAX_BYTES = 4194304
RAW_VIDEO_RETENTION = NONE_AFTER_EXECUTION
RAW_FRAME_RETENTION = NONE_AFTER_EXECUTION
```

The implementation may choose stricter limits only when an accepted repository,
runtime, provider, security, or deployment constraint requires them. Record the
exact reason and resulting value. Do not silently increase any limit.

Construct candidate timestamps from:

```text
0 seconds
1.5 seconds
3 seconds
50 percent of verified duration
80 percent of verified duration
max(verified duration - 0.25 seconds, 0)
```

Then remove out-of-range timestamps, normalize to stable milliseconds,
deduplicate, sort ascending, and extract no more than six frames.

Short videos may produce fewer than six unique frames. This is truthful bounded
coverage, not a failure and not permission to invent replacement timestamps.

Do not add adaptive scene-change selection in Week 1.

---

## 7. Provider locator and secure video acquisition

Extend the established provider boundary only as required to obtain the selected
media's source video locator through the already-authorized Instagram Login
read contract.

The provider adapter must:

- request only the minimum verified fields;
- validate returned media identity and media type;
- preserve Settings credential ownership;
- never return or persist a token/locator in consumer-visible or Evidence data;
- use the existing bounded locator JSON reader, including streamed enforcement;
- expose typed AVAILABLE/PARTIAL/UNAVAILABLE/provider-failure truth; and
- retain authorization-generation fencing before and after acquisition where
  the accepted runtime requires it.

The video downloader must preserve or strengthen B3A/B3B controls:

```text
HTTPS only
approved hostname policy
DNS lookup and private/reserved IP rejection
redirect validation on every hop
bounded redirect count
connection/read/overall timeout
Content-Length precheck
streamed byte enforcement independent of Content-Length
dishonest Content-Length rejection
container/signature/MIME validation
atomic temporary file creation
unique isolation scope
cleanup after success/failure/abort/timeout
```

Never expose credentials, signed locators, raw response bodies, raw bytes,
temporary paths, or provider payloads in logs, errors, database rows, reports,
fixtures, DTOs, or Git.

Do not hold a database transaction open during provider download, decode, frame
extraction, or model execution.

---

## 8. Video probing and frame extraction

Before source mutation, inspect the backend's production runtime packaging and
establish a decoder/extractor route that is valid both locally and in the
production-built runtime.

Allowed:

- existing local/system decoder for the runner's tests when already present;
- the smallest justified repository-scoped dependency when it is production-
  deployable, license-compatible, and recorded; or
- an already-established repository/container primitive.

Prohibited:

- silent system-wide installation or reconfiguration;
- a path that works only on the Local Codex machine;
- unpinned downloads during application execution;
- shell construction using untrusted media IDs, paths, locators, or timestamps;
- unbounded child process output/runtime; and
- persisting raw video or extracted frames.

Use argument-vector process execution with bounded stdout/stderr, timeout,
termination, and exit-code handling. Treat probe/decoder output as untrusted and
validate it.

If no deployable bounded decoder path exists without material scope expansion,
stop before broad mutation and return:

```text
VIDEO_DECODER_DEPLOYMENT_PREREQUISITE_REQUIRED
```

The video foundation must return typed, provider-neutral results and must not
depend on Brand Object, Creator Object, Campaign, Collaboration, or frontend
types.

---

## 9. Per-frame observation contract

Reuse the existing fail-closed low-level visual model abstraction wherever
compatible. Add a new versioned video-frame observation profile rather than
silently changing the meaning of the accepted B3A image profile.

Every successful frame observation must bind:

```text
provider media ID
verified video fingerprint
video-analysis profile version
frame ordinal
requested timestamp
actual decoded timestamp when supported
frame width/height/byte length/hash
observation contract version
prompt/profile version
provider/model identity
Capture and Evidence identity
```

Low-level frame output remains descriptive. It must not itself output:

```text
Campaign recommendation
brief instructions
Creator identity/tier
canonical Collaboration
canonical Offering mutation
performance superiority
causal claim
cross-Reel pattern or learning
```

Use deterministic fixture acquisition, probe/extraction, and model adapters for
acceptance. The default unconfigured production visual adapter must remain
fail-closed unless an already-authorized live adapter exists.

---

## 10. Capture, Evidence and persistence

Preserve the accepted lifecycle:

```text
create Capture
-> acquire/persist bounded material references
-> mark Capture COMPLETED with authoritative capturedAt
-> normalize/insert Evidence
```

Persist structured frame metadata and low-level observations only. Do not
persist raw video, raw frame bytes, base64, signed locators, temporary paths, or
decoder logs.

Prefer existing `instagram.media_visual_observations` ownership and existing
JSON/structured Evidence capacity. A new Prisma migration is not expected.

If correct frame lineage requires a new generalized media table, capability,
cross-source persistence design, or material migration redesign, stop and
return:

```text
W1_PERSISTENCE_SCOPE_EXPANSION_REQUIRED
```

Evidence keys and manifests must be deterministic. Exact replay must preserve
stable valid row counts and references.

Settings-owned delete-data must remove the target Brand's new Instagram-derived
frame Evidence/observations/generations while preserving every other tenant and
unrelated source/canonical domain.

---

## 11. Brand semantic integration

Feed ordered, timestamped frame evidence into C3 through the smallest versioned
input/profile change compatible with the existing public contracts.

Positive semantic values may cite one or more exact frame Evidence refs.
Field-level refs must remain subsets of capability-owned parent Evidence.

For sampled Reel/video media:

```text
UNOBSERVED IN SAMPLED FRAMES
!=
ABSENT FROM THE VIDEO
```

Therefore sampled frames alone must not publish `NO_CREATOR`, `NO_OFFERING`,
`NO_COLLAB_SIGNAL`, or equivalent complete-video negatives. Use `UNKNOWN` /
`INSUFFICIENT_EVIDENCE` for unsupported absence.

Week 1 may derive evidence-grounded labels within existing semantic fields, for
example:

```text
product-led opening
person-led opening
product visible in sampled opening
product-use demonstration observed
brand/logo visible
visual CTA observed
```

Use the exact early timestamps to support opening claims. Do not infer spoken
hooks, exact on-screen text, scene changes, pacing, or complete sequence.

Reuse the existing three Objects. Enrich only existing content-behavior
components where truthfully supported, including:

```text
theme_patterns
creative_structure_patterns
offering_presence_patterns
creator_presence_patterns
representative_media_refs
bounded_learnings
coverage
```

Preserve existing C2/C4 sample-size, coverage, cohort, readiness, confidence,
and inconclusive-state behavior. One Reel is not a pattern or learning.

Existing post-level metrics may be associated with Reel-level semantic cohorts.
Do not attribute performance to an individual frame and do not make causal
claims.

Do not add or fabricate a three-second-view rate. Existing `views`, `reach`, or
`total_interactions` are not proxies for that metric.

---

## 12. Execution identity, reuse and failure behavior

Bind execution identity to the applicable exact set:

```text
tenant/Brand subject
provider account
authorization generation
provider media ID
source Capture/Evidence manifest
verified video fingerprint
video acquisition profile
frame-selection profile
frame observation contract/profile/model identity
C3 semantic processor/profile version
window/cutoff identity
```

Unchanged media with an exact valid successful identity must not repeat
expensive video/frame/model work on every daily refresh.

A changed profile/version may create a bounded new execution. If it fails:

```text
prior valid current remains visible
no empty replacement is published
processing/freshness degradation is projected separately
no metric is replaced with zero
```

Partial frame success must be explicit. It may support positive facts from
successful frames but cannot support complete-video negatives.

---

## 13. Creator reuse boundary

The low-level video foundation must be reusable by a future Creator Instagram
adapter without importing Brand Objects or Brand workspace contracts.

Do not implement Creator Intelligence, Creator connection lifecycle, Creator
Objects, Creator consumer APIs, or Creator workspace behavior in this packet.

Provide a compact Creator reuse manifest at:

```text
docs/ai-collaboration/instagram-intelligence-v1/W1_SHARED_VIDEO_FOUNDATION_CREATOR_REUSE_MANIFEST.md
```

It must record:

```text
backend predecessor and resulting checkpoint
shared modules/files and exported ports
input/output and error contracts
configuration names without values
safety constants/profile versions
decoder/runtime prerequisite
Evidence and cleanup semantics
new dependency identity if any
Brand-bound adapters that Creator must not reuse
Creator integration seams
focused compatibility tests
```

Prove at the type/architecture/test level that the low-level video foundation
does not require a Brand Object or frontend type. Do not generalize the entire
Brand-subject Intelligence runtime merely for future reuse.

---

## 14. Consumer and frontend boundary

Prefer unchanged routes:

```text
GET /api/v1/brand-centre/instagram
GET /api/v1/brand-centre/instagram/media/:mediaId
```

If existing schemas already project inspected frame count and truthful coverage,
do not change the frontend merely to create visible churn.

An additive backend/frontend change is authorized only when necessary to show:

- selected Reel multi-frame inspection;
- inspected-frame count and partial/unknown limitations;
- existing content-behavior conclusions supported by frame Evidence; and
- preserved-current degradation after video-analysis failure.

Any DTO evolution must be versioned/additive, strictly parsed, tenant-safe, and
must expose no raw frame, locator, timestamp manifest, model internals, prompt,
provider payload, or temporary path.

Do not change navigation, workspace hierarchy, Settings mutations, actions, or
responsive composition beyond the smallest compatibility work. If frontend
changes, test 390, 767, 768, and desktop plus keyboard/focus and Axe for the
changed surface. If frontend does not change, prove exact unchanged SHA/tree.

---

## 15. Rollout and observability boundary

Reuse an established rollout/configuration mechanism if present. Otherwise add
only one narrow non-secret environment/config kill switch for selected-video
frame processing, defaulting fail-closed, and document its name without a value.

Do not create a generalized feature-flag platform.

Record non-sensitive operational outcomes for:

```text
acquisition/probe/extraction category
frames requested/extracted/observed
duration/size/codec rejection category
processing latency/timeout
model invocation count/profile
cleanup success/failure
UNKNOWN/NOT_INSPECTED/UNAVAILABLE outcome
replay/reprocessing outcome
current-preservation event
```

Do not log captions, frame descriptions, locators, tokens, raw provider bodies,
raw bytes, or temporary paths merely for observability.

Production backfill orchestration, rollout percentages, AWS infrastructure,
deployment, and monitoring configuration are not authorized in Week 1.

---

## 16. Required deterministic and adversarial test matrix

### 16.1 Technical profile

Prove:

```text
six canonical timestamps for a normal-duration video
deduplicated bounded timestamps for short videos
stable order and millisecond normalization
duration boundary at and above 180 seconds
size boundary at and above 104857600 bytes
maximum six extracted frames
frame dimension and encoded-size enforcement
profile/version included in identity
```

### 16.2 Transport/security

Prove:

```text
non-HTTPS rejection
disallowed host rejection
private/reserved IP rejection
redirect revalidation
redirect-to-private rejection
oversized declared Content-Length rejection
oversized chunked response rejection
dishonest Content-Length rejection
timeout and abort
corrupt/unsupported container rejection
probe/decoder bounded stdout/stderr and timeout
no shell injection through IDs/paths/timestamps
cleanup on every terminal path
```

### 16.3 Media behavior

Prove:

```text
selected REEL success
selected VIDEO success
partial frame extraction
unselected video bypass
IMAGE unchanged
CAROUSEL unchanged
model unavailable/failure
no complete-video negative from sampled frames
exact positive support refs
```

### 16.4 PostgreSQL lineage and preservation

Using a uniquely named disposable PostgreSQL 17 route, prove:

```text
all 90 accepted migrations apply cleanly and status is current
no new migration unless separately escalated and authorized
Resource -> Capture -> completed capturedAt -> frame Evidence
frame Evidence -> C3 derived Evidence/Observation
C3 -> C4 generation/current
exact replay leaves stable counts and refs
changed-profile failure preserves prior valid current
tenant/account/generation/media substitutions fail atomically
Settings target delete removes new Instagram rows
other tenant and website/canonical rows survive
```

### 16.5 Regression/build

Run changed-surface suites plus focused regressions for:

```text
B2 provider truth/locator bounding
B3A image acquisition and cleanup
B3B selection/image/carousel/video-cover behavior
C2 deterministic foundations used by Reel cohorts
C3 Evidence grounding and likely_collab
C4 Object generation/current/consumer
C1 execution identity and failure-current preservation
hidden Brand lane input compatibility
Settings delete-data
```

Also run:

```text
npm ci
Prisma generate and validate
backend production build
scoped ESLint
git diff --check
secret/raw-media/forbidden-artifact scan
```

Run frontend typecheck/build/scoped tests/browser only if frontend changed.

Prove the selected decoder is available and executable in the production-built
runtime/container shape. Local-host availability alone is insufficient.

---

## 17. Optional live read boundary

Provider-neutral deterministic evidence is sufficient for Week 1 acceptance.

If and only if existing working Settings-managed local credentials are already
available without OAuth, reconnect, permission, app, or provider mutation, the
runner may perform one bounded read-only sanity check:

```text
one connected Brand account
one naturally available selected Reel/video
minimum source locator read
bounded download/probe/frame extraction
no live model call
guaranteed cleanup
```

Do not request credentials from the Parent. Do not print or persist their
values. Absence of credentials is non-blocking and must be reported as:

```text
OPTIONAL_LIVE_VIDEO_READ = NOT_RUN_CREDENTIALS_UNAVAILABLE_NON_BLOCKING
```

No Meta/OAuth/provider mutation is authorized.

---

## 18. Prohibited work

Do not implement:

```text
Week 2 through Week 8
full carousel-child sweep
OCR
audio extraction/analysis
transcription
adaptive scene segmentation
full temporal interpretation or precise pacing
all-post video inspection
7-day or 14-day windows
new provider permissions
Facebook Login or Business Discovery
Creator Marketplace Discovery
three-second-view proxy
new Instagram Object/component family
Creator Intelligence or Creator workspace
Campaign Recommendation or brief creation/mutation
Collaboration submission/compliance workflow
canonical Creator/Collaboration/Offering/Campaign mutation
manual semantic editing/override
new generalized media/scheduler/research/statistics/feature-flag platform
development merge
AWS/production deployment or migration
```

Do not weaken:

```text
tenant/account/authorization-generation fencing
Capture/Evidence completion ordering
MODEL_DERIVATION provenance
raw-media containment
Settings deletion ownership
failure-current preservation
provider truth semantics
explicit UNKNOWN/UNAVAILABLE/NOT_INSPECTED behavior
```

---

## 19. Circuit breakers and correction policy

Stop and return a circuit-breaker candidate if resolution requires:

```text
new Product/UX/provider ownership decision
fourth Instagram Object or new top-level component family
new provider permission/OAuth behavior
new general Intelligence current/candidate framework
material schema/migration redesign
weakening security, lifecycle, isolation, deletion, or truth semantics
durable raw-video/frame persistence
system-wide decoder installation
local-only non-deployable decoder path
parallel incompatible Creator changes in shared surfaces
Campaign/Collaboration/Creator/Offering canonical mutation
```

Ordinary DTO, mapper, profile-version, process-wrapper, fixture, test,
integration, and bounded existing-UI defects inside this charter are normal
Week 1 correction work.

Use one primary execution run. Resolve ordinary bounded defects inside that run
without weakening gates. If a defect remains, preserve the exact bounded state
and return `W1_CORRECTION_REQUIRED`. Do not silently consume future formal
correction cycles.

---

## 20. Publication and authority update

Only after all mandatory Week 1 gates pass:

1. Commit the complete backend checkpoint to
   `program/instagram-intelligence-v1-backend`.
2. Commit a frontend checkpoint only if the bounded consumer projection required
   frontend changes; otherwise prove frontend unchanged.
3. Copy this exact prompt into its canonical authority path.
4. Create:

```text
docs/ai-collaboration/instagram-intelligence-v1/W1_SHARED_VIDEO_FOUNDATION_AND_SELECTED_BRAND_REEL_ANALYSIS.md
docs/ai-collaboration/instagram-intelligence-v1/W1_SHARED_VIDEO_FOUNDATION_CREATOR_REUSE_MANIFEST.md
```

5. Update the single `EXECUTION_LEDGER.md` to record:

```text
W1 = EVIDENCE_READY_AWAITING_CHILD_SA_PROGRAM_ORCHESTRATOR_ACCEPTANCE
W1_ACCEPTED = NO
W2 = PROHIBITED
```

6. Do not rewrite prior immutable evidence.
7. Use normal non-force pushes only.
8. Fetch/prune independently after publication.
9. Prove local/fetched SHA and tree equality and predecessor ancestry for every
   changed repository.
10. Prove all final worktrees clean.

Do not label Week 1 accepted. Acceptance belongs to the Child SA / Program
Orchestrator after reviewing the immutable report.

If a mandatory gate fails, do not publish a false acceptance artifact.

---

## 21. Cleanup

Stop only task-owned backend/frontend/browser/decoder processes. Remove only
exactly identified task-owned PostgreSQL containers, volumes, temporary videos,
frames, fixtures, logs, and runtime artifacts.

Prove:

```text
temporary video count = 0
temporary frame count = 0
task-owned database/container/volume count = 0
task-owned process count = 0
all repository worktrees have the reported exact state
```

Do not remove unrelated resources.

---

## 22. Required immutable return contract

Return:

# `INSTAGRAM_INTELLIGENCE_WEEK_1_EXTERNAL_LOCAL_CODEX_REPORT_V1`

with:

```text
A. exact starting repository SHA/tree/worktree verification
B. standards, charter and predecessor reconciliation
C. shared-foundation reuse decision and architecture placement
D. production decoder/extractor route and deployability proof
E. exact changed-file inventory and diff statistics
F. package/dependency/configuration changes, if any
G. provider video-locator contract and truth-state proof
H. secure video transport/SSRF/redirect/size/timeout evidence
I. video container/probe/duration/codec validation
J. deterministic timestamp and short-video deduplication proof
K. frame extraction, dimension/size/hash and cleanup proof
L. per-frame low-level observation contract and fail-closed model behavior
M. Capture/Evidence/frame lineage and lifecycle proof
N. selected/unselected and IMAGE/CAROUSEL regression behavior
O. multi-frame C3 semantic grounding and no-false-negative proof
P. C2/C4 cohort, Object, current and bounded-learning proof
Q. post-level performance association and no-causal/no-3-second-proxy proof
R. execution identity, replay and expensive-work reuse proof
S. partial/failure-current preservation proof
T. tenant/account/generation/media isolation proof
U. Settings delete-data and cross-tenant/source preservation proof
V. consumer/API and frontend change-or-unchanged proof
W. rollout kill-switch and non-sensitive observability proof
X. Creator reuse manifest and domain-neutral foundation proof
Y. PostgreSQL environment, migrations, row counts and exact lineage
Z. focused regression matrix
AA. backend build/lint/diff results
AB. frontend tests/build/browser/Axe results if changed
AC. schema/migration/dependency identity
AD. secret/raw-media/locator/forbidden-artifact scan
AE. optional live-read disposition and live-call counts
AF. backend publication/fetch-back equality
AG. frontend publication/fetch-back or unchanged equality
AH. authority prompt/evidence/manifest/ledger publication/fetch-back
AI. final runtime and temporary-media cleanup
AJ. primary-run/correction accounting
AK. exact blocker, if any
```

End exactly with:

```text
RESULT =
READY_FOR_CHILD_SA_PROGRAM_ORCHESTRATOR_REVIEW
|
W1_CORRECTION_REQUIRED
|
W1_TRUE_CIRCUIT_BREAKER_CANDIDATE
|
W1_STARTING_STATE_MISMATCH
|
VIDEO_DECODER_DEPLOYMENT_PREREQUISITE_REQUIRED
|
W1_PERSISTENCE_SCOPE_EXPANSION_REQUIRED

W1_ACCEPTED =
NO — AWAITING CHILD-SA / PROGRAM-ORCHESTRATOR REVIEW

W1_EVIDENCE_READY =
YES | NO

W1_PRIMARY_RUNS_USED =
<COUNT>

W1_CORRECTION_CYCLES_USED =
<COUNT>

W2 =
PROHIBITED

OPTIONAL_LIVE_VIDEO_READ =
NOT_RUN_CREDENTIALS_UNAVAILABLE_NON_BLOCKING
|
NOT_RUN_BY_CHOICE
|
<SANITIZED READ-ONLY RESULT>

LIVE_GRAPH_CALLS =
NONE | <SANITIZED COUNT>

LIVE_MODEL_CALLS =
NONE | <SANITIZED COUNT>

META_PROVIDER_MUTATIONS =
NONE | <SANITIZED DESCRIPTION>

RAW_VIDEO_PERSISTED =
NO | YES

RAW_FRAMES_PERSISTED =
NO | YES

NEW_MIGRATIONS =
NONE | <EXACT LIST>

DEVELOPMENT_MERGE =
NOT_PERFORMED

DEPLOYMENT =
NOT_PERFORMED

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CHILD SA / PROGRAM ORCHESTRATOR
```

---

## 23. Week 1 evidence-ready criteria

Week 1 is evidence-ready only when:

```text
exact starting identities/trees/ancestry/worktrees pass
accepted 90-migration history remains valid
production-deployable bounded decoder/extractor is proven
provider video locator is typed, bounded and credential-safe
video transport resists SSRF/redirect/size/timeout abuse
video/container/duration/frame limits are enforced
temporary videos and frames always clean up
deterministic six-or-fewer timestamp profile passes
only deep-selected Reel/video receives multi-frame work
IMAGE/CAROUSEL and unselected-media behavior remains unchanged
each frame has exact technical/model/Capture/Evidence lineage
sampled absence never becomes complete-video negative
C3/C4 consume exact supported frame subsets
one Reel does not become a pattern/learning
post metrics remain Reel-level and no causal/3-second proxy claim appears
replay avoids duplicate rows and unnecessary expensive work
failed changed-profile execution preserves prior current
tenant/account/generation/media isolation passes
Settings delete-data removes target-only new rows
Creator reuse manifest is complete and low-level foundation is domain-neutral
consumer/frontend remains compatible and truthful
no secret, locator, raw media, temporary path or forbidden artifact is published
no unauthorized live call/provider mutation occurs
normal publication and independent fetch-back pass
final worktrees/runtime/temp media are clean
Week 2 remains unstarted
development merge and deployment remain unperformed
```

Then stop and wait for Child-SA / Program-Orchestrator review.
