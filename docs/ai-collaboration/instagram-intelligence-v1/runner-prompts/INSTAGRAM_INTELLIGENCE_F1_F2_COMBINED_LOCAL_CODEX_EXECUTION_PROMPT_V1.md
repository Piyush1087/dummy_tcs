# INSTAGRAM INTELLIGENCE — F1 + F2 COMBINED INTEGRATED ACCEPTANCE — EXTERNAL LOCAL CODEX EXECUTION V1

`INSTAGRAM_INTELLIGENCE_F1_F2_COMBINED_LOCAL_CODEX_EXECUTION_V1`

You are the authorized **External Local Codex runner** for the Creator Shop Instagram Intelligence V1 program.

This file is the complete execution instruction. Do not reinterpret Product, UX, provider scope, or architecture. Do not start F3.

The Parent has supplied this prompt file rather than pasting the prompt into chat. Preserve this exact file in the authority repository at:

```text
docs/ai-collaboration/instagram-intelligence-v1/runner-prompts/INSTAGRAM_INTELLIGENCE_F1_F2_COMBINED_LOCAL_CODEX_EXECUTION_PROMPT_V1.md
```

Include it in the final authority publication if and only if this run reaches an evidence-ready checkpoint. If the run stops before publication, preserve the Parent-supplied file outside the repositories and report that it was not published.

---

# 1. EXECUTION AUTHORITY

The Program Orchestrator has accepted:

```text
A1 / A2 / A3 = ACCEPTED
B1 / B2 / B3A / B4 / B3B = ACCEPTED
C2 / C3 / C4 / C1 = ACCEPTED
D_COMBINED = ACCEPTED
HIDDEN_BRAND_LANE = ACCEPTED_FOR_V1
E1 = ACCEPTED
E2_E3_COMBINED = ACCEPTED
E4 = ACCEPTED
```

Wave F is optimized as:

```text
F1 + F2 = ONE COMBINED LOCAL EXECUTION
           WITH TWO SEPARATELY REPORTED INTERNAL GATES

F3 = SEPARATE FINAL EVIDENCE / OPTIONAL LIVE READ / DEVELOPER HANDOFF
```

This consolidation reduces repeated environment setup. It does **not** merge or weaken F1 and F2 acceptance criteria.

Current authorization:

```text
F1_F2_COMBINED = AUTHORIZED
F3 = PROHIBITED_PENDING_F1_F2_ACCEPTANCE
```

The runner may not self-accept F1/F2. It produces immutable evidence for Child-SA / Program-Orchestrator review.

---

# 2. EXACT STARTING CHECKPOINTS

Use the existing local repositories and their established program branches.

## Backend

```text
REPOSITORY = Piyush1087/creator-commerce-backend-v2-clone
BRANCH = program/instagram-intelligence-v1-backend
SHA = d5b9de557ed983a9c3af26b1edc305ba8b77d6f3
TREE = 67d2642871899fda9e7bd9248b8d56892c3d6e6d
EXPECTED_MIGRATION_COUNT = 90
```

## Frontend

```text
REPOSITORY = Piyush1087/creator-commerce-frontend-v2-clone
BRANCH = program/instagram-intelligence-v1-frontend
SHA = 5866d0ac82f742957f53a8db2144a9a166628b75
TREE = 7c095290ffdef4c84f37382bc594bef536c92a49
```

## Program authority

```text
REPOSITORY = Piyush1087/dummy_tcs
BRANCH = program/instagram-intelligence-v1-authority
SHA = f19fdb8ae80bea84036df874fb2b1736fa831155
TREE = 4559f0234085d726ee054c6148c963cf7444bf4e
```

Before mutation:

1. Resolve the three exact local repository paths.
2. Run normal `fetch --prune`.
3. Prove local SHA/tree equals fetched remote SHA/tree for every repository.
4. Prove the expected accepted predecessor ancestry.
5. Prove every worktree is clean, with no staged, unstaged, untracked, or overlapping user work.
6. Verify the backend migration inventory is exactly the accepted 90-migration history before any test database is created.

If any identity, ancestry, migration inventory, or clean-worktree requirement differs, stop without mutation and return:

```text
F1_F2_STARTING_STATE_MISMATCH
```

Do not reset, clean, stash, discard, force checkout, or rewrite history.

---

# 3. REQUIRED AUTHORITY RECONCILIATION

Before execution, read and reconcile the applicable current files from the three repositories, including:

```text
all applicable AGENTS.md files
AI_ENGINEERING_STANDARD.md
backend/database/validation/AI integration standards
frontend/layout/design/accessibility standards
accepted Instagram Intelligence V1 definitive plan and V1.1 canonicalization
accepted post-B2 optimized execution plan
single Instagram execution ledger
accepted A1 through E4 checkpoint artifacts
installed Instagram DE, Evidence, semantic, Object and consumer contracts
Settings Instagram integration contracts
Brand Centre mounted navigation/workspace contracts
```

Treat committed accepted authority and the exact starting trees as authoritative. Preserve committed development terminology for Campaign vocabulary.

Do not independently reinterpret frozen decisions.

---

# 4. PURPOSE AND TEST MODEL

Purpose:

```text
PROVE THE COMPLETE PROVIDER-NEUTRAL INSTAGRAM V1
AS ONE INTEGRATED SYSTEM

AND

PROVE THE COMPLETE AUTHENTICATED USER EXPERIENCE
ACROSS ROLES, CONNECTION STATES, RESPONSIVE BOUNDARIES,
RECOVERY, ISOLATION AND DELETE-DATA
```

This is assurance breadth over accepted functionality, not authorization for new scope.

Execute as two explicit internal gates:

```text
INTERNAL_GATE_F1 = BACKEND + CROSS-PACKET INTEGRATED ACCEPTANCE
INTERNAL_GATE_F2 = AUTHENTICATED BROWSER + ROLE + STATE ACCEPTANCE

OVERALL_PASS = F1 PASS AND F2 PASS
```

F2 may start only after F1 establishes a healthy migrated database and runtime foundation. If F1 has a mandatory blocker, stop before F2 except for read-only diagnosis needed to classify the blocker.

---

# 5. INTERNAL GATE F1 — BACKEND AND CROSS-PACKET ACCEPTANCE

## 5.1 PostgreSQL and migration integrity

Use a uniquely named, disposable PostgreSQL 17 route.

Prove:

```text
all 90 migrations apply from a clean database
Prisma migration status is current
required extensions/connectivity are available
schema and migration history are byte-stable unless an authorized bounded defect requires a change
representative pre-Instagram website/shared rows survive the accepted upgrade route where applicable
no destructive or unrelated schema behavior exists
```

Do not create a migration merely for test convenience. Any material schema or migration redesign is a circuit breaker.

## 5.2 Settings-to-provider truth

Using deterministic provider fixtures and the accepted Settings-owned integration state, prove:

```text
active integration identity
provider-account identity
authorization-generation fencing
exact accepted provider capability semantics
AVAILABLE / PARTIAL / UNAVAILABLE truth
pagination and bounded acquisition
transient provider failure handling
same-account reconnect continuity
different-account isolation
no token or authorization secret leakage
```

Settings remains the sole owner of connect, reconnect, account-conflict resolution, disconnect and delete-data mutations.

## 5.3 DE, Capture, Artifact, Evidence and media safety

Prove the accepted path and ordering:

```text
Resource
→ Capture begins
→ bounded provider/artifact material persists
→ Capture completes with authoritative capturedAt
→ Evidence persists
→ observations/derivations retain exact lineage
```

Verify:

```text
stable account/media identity
idempotent request/execution identity
Capture/Evidence ordering
same-capability support invariants
MODEL_DERIVATION provenance and parent Evidence lineage
artifact-reference validity
temporary media containment and cleanup
SSRF defenses
redirect and hostname validation
streamed 65,536-byte locator response limit,
including chunked and dishonest Content-Length responses
no raw media persistence
```

Preserve the accepted V1 media-depth policy:

```text
ALL ELIGIBLE 30-DAY POSTS = metadata + caption + metrics + coverage
DEEP-SELECTED CORPUS = up to 24 posts
IMAGE = selected image inspection
CAROUSEL = deterministic representative-child inspection
REEL / VIDEO = cover or thumbnail inspection only
NON-SELECTION = NOT_INSPECTED / UNKNOWN
```

Never turn missing or uninspected modalities into negative semantic claims.

## 5.4 Deterministic and semantic Intelligence

Prove the integrated flow for the accepted 30-day corpus:

```text
Instagram DE / Evidence
→ deterministic C2 foundations
→ C3 per-media semantics and likely_collab
→ C4 three Instagram Objects
→ shared generation/current
→ complete Instagram consumer
```

The three Objects are:

```text
instagram_content_behavior
instagram_audience_profile
instagram_organic_performance_profile
```

Verify every accepted component contract and processor-owned path through the verified runtime bundle. Prove deterministic identity/hash/replay, current projection and exact Evidence support.

Preserve:

```text
RESULT → SIGNAL / PATTERN → LEARNING
sample size and coverage
deterministic arithmetic
inconclusive states
no causal claims
no single-post learning
confidence/state contracts
```

For `likely_collab`, prove the accepted inference matrix, modality/source grounding, deterministic mention parsing, phrase boundaries, cue deduplication/conflict rejection, and provider-relation non-fabrication.

Do not create or mutate canonical Creator or Collaboration truth. Do not invent creator followers/tier.

Offering inference may only link an exact active same-Brand Offering under the accepted source-grounding rules. Do not mutate canonical Offering truth, identity, price or currency.

## 5.5 Shared current and independent failure preservation

After successful publication, execute bounded later failures independently across relevant capability/Object paths and prove:

```text
valid current remains unchanged
failed execution cannot publish empty replacement
freshness/processing degradation is projected separately
unavailable metrics are not converted to zero
successful peer Objects/media remain current
retry/replay cannot duplicate generations or current pointers
```

Prove transaction/concurrency behavior cannot expose invalid partial publication.

## 5.6 C1 execution lifecycle and cadence

Prove the accepted V1 lifecycle and cadence with deterministic time control:

```text
initial connection = immediate
profile/media/performance = daily
audience = weekly
dispatcher = hourly with due gates + jitter
manual refresh = 15-minute cooldown
```

Verify reuse of shared worker/lease/retry/backoff primitives, idempotent execution, lease/reclaim behavior where accepted, due-gate selection, retry/backoff, authorization-generation fence, RUNNING/SUCCESS/FAILURE states, and failure-current preservation.

Do not add a second generalized Instagram scheduling platform.

Preserve the role matrix:

```text
Brand Owner = refresh allowed
Campaign Manager = refresh allowed
Finance Admin = refresh denied / read-only
```

## 5.7 Hidden Brand lane

The hidden Brand lane is required for V1 and must not be deferred.

Prove:

```text
Instagram Evidence
→ source-aware admission/profile
→ reuse of existing Brand processing semantics
→ generation-only INSTAGRAM_OWNED Brand output
```

Verify source isolation, source-native authority, merge/ignore/negative-mark boundary, current/candidate protection behavior where applicable, and absence from the visible Instagram workspace.

Do not create parallel Brand processors or a new canonical Brand state system. Do not directly mutate canonical/manual Brand truth. Do not display hidden Brand semantics in Instagram.

## 5.8 Consumer and canonical-domain boundaries

Prove the complete authenticated consumer routes, including:

```text
GET /api/v1/brand-centre/instagram
GET /api/v1/brand-centre/instagram/media/:mediaId
```

Verify strict versioned DTOs, server-resolved Brand scope, active integration/account/generation fencing, bounded response sizes, correct readiness/freshness/coverage, and absence of raw provider payloads, credentials, signed locators, internal IDs, prompts, leases, candidates or pipeline internals.

Prove Instagram does not override or mutate:

```text
canonical Brand/manual state
canonical Offering identity/facts/price/currency
Campaign Objective/Offering/target/deliverable/commercial truth
canonical Creator identity
canonical Collaboration identity/agreement/deliverables/publishing evidence
Settings integration lifecycle
future Campaign Performance or recommendation authority
```

Campaign objective authority is exactly:

```text
AWARENESS / TRUST / ASSETS / ACTION
```

`PULSE / PROOF / PRODUCTION / PUSH` is legacy only.

## 5.9 Isolation, deletion and retention

In PostgreSQL-backed proofs, verify:

```text
Brand/tenant isolation
provider-account isolation
authorization-generation isolation
media identity isolation
subject/execution isolation
cross-Brand non-disclosure
Settings-owned delete Instagram data removes the complete target-Brand Instagram-owned graph
other-Brand data survives
unrelated website/shared Intelligence survives
no orphan current pointer, Evidence support, artifact or observation remains
accepted retention boundaries remain unchanged
```

## 5.10 Required F1 test evidence

Run:

```text
clean PostgreSQL migration apply/status
relevant upgrade/preservation proof
cross-packet provider/DE/Evidence/media tests
C2/C3/C4/C1 integrated tests
hidden Brand lane tests
consumer and media-detail integration tests
tenant/account/generation isolation tests
delete-data tests
failure-current and concurrency/idempotency tests
backend production build
scoped lint for changed surfaces, or the accepted final backend lint gate
git diff integrity
dependency identity
secret and forbidden-artifact scan
```

Use focused grouping/serial database execution when needed to avoid disposable-database teardown contention. Do not weaken assertions to make the suite pass.

---

# 6. INTERNAL GATE F2 — AUTHENTICATED BROWSER, ROLES AND STATES

Start the real local backend/frontend against the migrated disposable database. Use authenticated, server-resolved Brand membership and deterministic provider/model fixtures. Successful Product-path evidence must be database-backed; fixture-only browser states must be explicitly identified.

## 6.1 Mounted Brand Centre navigation

Verify the shared peer-workspace composition:

```text
Overview
Brand
Offerings
Instagram
Market
Recommendations
```

Required boundary:

```text
390 and 767 = mobile workspace selector/dropdown
768 and desktop = horizontal peer navigation
```

Verify active-state semantics, direct routes, browser history, Brand and Offerings migration compatibility, no duplicate inner Instagram tabs, and no horizontal overflow.

## 6.2 Singular Instagram workspace

Verify the accepted fixed 30-day workspace hierarchy and all accepted states without changing Product or UX:

```text
connection/context and status
account-level performance near the top
primary understanding
semantic sections
what changed / needs attention where contracted
representative posts as supporting evidence
domain-owned actions only
```

Preserve conclusion-first presentation, progressive disclosure, truthful partial/stale/unavailable states, current preservation, supporting metrics, hidden technical detail, no semantic editing, and no hidden Brand output display.

## 6.3 Connection and capability state matrix

Prove truthful workspace projection for the accepted Settings-owned lifecycle states:

```text
NOT CONNECTED
CONNECTING / AUTH FLOW where observable
CONNECTED
PARTIAL CAPABILITY
UNKNOWN / UNVERIFIED CAPABILITY
REAUTH REQUIRED
TOKEN / AUTHORIZATION DEGRADATION
SAME-ACCOUNT RECONNECT
DIFFERENT-ACCOUNT IDENTITY CONFLICT
SYNC HEALTH
TRANSIENT PROVIDER FAILURE
DISCONNECTED
INSTAGRAM DATA DELETED / NO CURRENT
```

Workspace actions may deep-link to Settings. Instagram must not duplicate connect, reconnect, conflict resolution, disconnect or delete-data mutations.

## 6.4 Role matrix and refresh

At minimum prove authenticated reads and action authority for:

```text
Brand Owner
Campaign Manager
Finance Admin
inactive Brand member
non-member
unauthenticated actor
```

Verify:

```text
Brand Owner = read + refresh
Campaign Manager = read + refresh
Finance Admin = read-only; refresh denied
inactive/non-member = non-disclosing denial
unauthenticated = authentication denial
manual refresh 15-minute cooldown
refresh running/success/failure states
successful current preserved during failed refresh
Settings actions remain role-restricted by Settings authority
```

## 6.5 Responsive and accessibility matrix

Run authenticated browser proof at exactly:

```text
390px
767px
768px
desktop (use 1440px unless repository authority specifies another accepted width)
```

At every width verify layout priority, navigation mode, workspace readability, no horizontal overflow, safe long content and controls, and correct loading/partial/stale/unavailable/error treatment.

Run keyboard/focus and Axe proof for the representative state matrix required by the accepted frontend authority. Report all impact levels. Critical and serious findings must be zero; any allowed lower-severity exception must already be explicitly accepted in authority. Do not silently waive findings.

## 6.6 Media detail drawer/sheet

Verify:

```text
route = /brand-centre/instagram/media/:mediaId
390 / 767 = full-screen modal sheet
768 / desktop = right-side modal drawer
route is sole open-detail authority
workspace remains mounted behind detail
Back/Forward/close/Escape behavior
initial focus, focus trap and deterministic focus return
internal scrolling and body-lock restoration
stale route response suppression and request cancellation
safe Instagram permalink handling
no raw media embed/fetch
no internal ID or Evidence-reference display
likely_collab non-canonical boundary copy
```

Test loading, ready, malformed/invalid response, unauthorized, forbidden, not found, transient error, stale/changed generation, removed-after-refresh and retry behavior.

## 6.7 End-to-end isolation, deletion and recovery

Through the authenticated UI and real backend prove:

```text
caller cannot select another Brand/account/generation
no previous-tenant content flashes
second-tenant media is non-disclosing
delete-data is initiated only in Settings
post-deletion Instagram workspace/detail shows truthful no-current state
other tenant and unrelated canonical data survive
reauth/degradation preserves valid current while freshness/capability degrades
unavailable metrics are never rendered as zero
```

## 6.8 Browser/runtime evidence

Record:

```text
browser/version
Node/npm versions
PostgreSQL version
backend/frontend health
authenticated routes and HTTP statuses
viewport-specific screenshots or deterministic harness evidence as permitted by repository standards
Axe results by viewport/state
console errors
page errors
failed/unexpected network requests
provider/model call counts
horizontal overflow measurements
```

Run frontend typecheck, production build, relevant tests, scoped lint, diff integrity, dependency identity, and secret/forbidden-artifact scans.

---

# 7. OPTIONAL LIVE PROVIDER BOUNDARY

For this F1/F2 run:

```text
LIVE_GRAPH_READ_SMOKE = NOT REQUIRED
LIVE_META/OAUTH MUTATION = PROHIBITED
LIVE_MODEL CALL = PROHIBITED
```

Do not ask the Parent for Meta secrets. Use accepted deterministic fixtures/provider DI.

The optional bounded live Graph read belongs to F3 and remains non-blocking when provider-neutral evidence is sufficient.

---

# 8. CHANGE AND CORRECTION POLICY

The expected work is integrated acceptance and evidence. New feature work is not expected.

If an ordinary implementation, test-harness, selector, fixture, or accessibility defect is found inside already accepted Product/UX/architecture contracts, you may make the smallest bounded correction necessary and rerun affected gates.

Record correction-cycle accounting explicitly. Do not weaken tests or truth semantics.

Stop and return a circuit-breaker candidate if resolution requires any of:

```text
new Product behavior
UX redesign
provider permission or OAuth change
new canonical-domain ownership
new generalized Intelligence/DE/scheduler platform
material schema or migration redesign
weakening tenant/account/generation isolation
weakening Capture/Evidence lifecycle
weakening delete-data or retention behavior
weakening failure-current preservation
exposing secrets, provider payloads, signed locators or raw media
deferring the accepted hidden Brand lane
starting F3
```

Do not modify unrelated files. Preserve user work. Do not reset, clean, stash, discard or rewrite history.

---

# 9. PUBLICATION AND AUTHORITY UPDATE

Only after both internal gates pass:

1. Commit any bounded backend/frontend corrections to their established branches.
2. Copy this exact prompt into the authority path specified at the beginning.
3. Create one final F1/F2 evidence artifact in the existing Instagram authority directory.
4. Update the single execution ledger.
5. Record:

```text
E4 = ACCEPTED
F1_F2_COMBINED = EVIDENCE_READY_AWAITING_CHILD_SA_PROGRAM_ORCHESTRATOR_ACCEPTANCE
F1_ACCEPTED = NO
F2_ACCEPTED = NO
F3 = NOT_STARTED / PROHIBITED
```

6. Use normal non-force push only.
7. Fetch/prune independently.
8. Prove local/fetched SHA and tree equality for every changed repository.
9. Prove accepted predecessor ancestry.
10. Prove final worktrees clean.

Do not label F1 or F2 accepted. Acceptance belongs to the Child SA / Program Orchestrator after reviewing the immutable report.

If either internal gate fails, do not publish a false acceptance artifact. Preserve bounded changes and report exact state according to repository policy.

---

# 10. CLEANUP

Stop task-owned backend/frontend/browser processes. Remove only exactly identified task-owned PostgreSQL containers, volumes, temporary fixtures, browser state, logs and runtime artifacts. Verify relevant task ports are no longer listening.

Do not remove pre-existing or unrelated resources.

---

# 11. REQUIRED IMMUTABLE RETURN CONTRACT

Return:

# `INSTAGRAM_INTELLIGENCE_F1_F2_COMBINED_EXTERNAL_LOCAL_CODEX_REPORT_V1`

with these sections:

```text
A. exact starting repository SHA/tree/worktree verification
B. standards and authority reconciliation
C. changed-file inventory and diff statistics
D. Internal Gate F1 overall result
E. PostgreSQL environment, clean migration and upgrade/preservation evidence
F. Settings/provider truth and capability matrix
G. DE/Capture/Artifact/Evidence lifecycle and lineage
H. media acquisition, SSRF, size-limit and containment proof
I. 30-day corpus and modality/coverage proof
J. C2 deterministic foundation proof
K. C3 semantic and likely_collab proof
L. C4 three-Object, component and shared-current proof
M. C1 lifecycle, cadence, lease/retry and cooldown proof
N. independent failure-current preservation and replay/concurrency proof
O. hidden Brand lane proof
P. consumer/API contract and canonical-domain non-mutation proof
Q. tenant/account/generation/media isolation
R. Settings-owned delete-data and retention proof
S. F1 backend test/build/lint/diff results
T. Internal Gate F2 overall result
U. authenticated runtime and role matrix
V. Brand Centre peer navigation and route proof
W. workspace connection/capability/state matrix
X. refresh, degradation and recovery browser proof
Y. 390px browser proof
Z. 767px browser proof
AA. 768px browser proof
AB. desktop browser proof
AC. media-detail drawer/sheet, routing and safe permalink proof
AD. keyboard/focus and Axe results
AE. console/page/network/overflow evidence
AF. real database-backed versus fixture-only evidence classification
AG. predecessor and cross-packet regressions
AH. schema/migration/dependency identity
AI. secret/raw-media/forbidden-artifact scan
AJ. live-call proof
AK. backend publication/fetch-back or unchanged proof
AL. frontend publication/fetch-back or unchanged proof
AM. authority prompt/evidence/ledger publication and fetch-back
AN. final worktree/runtime cleanup
AO. correction-cycle accounting
AP. exact blocker, if any
```

End exactly with:

```text
RESULT =
READY_FOR_CHILD_SA_REVIEW
|
F1_F2_CORRECTION_REQUIRED
|
F1_F2_TRUE_CIRCUIT_BREAKER_CANDIDATE
|
F1_F2_STARTING_STATE_MISMATCH

F1_INTERNAL_GATE =
PASS | FAIL | NOT_RUN

F2_INTERNAL_GATE =
PASS | FAIL | NOT_RUN

F1_ACCEPTED =
NO — AWAITING CHILD-SA / PROGRAM-ORCHESTRATOR REVIEW

F2_ACCEPTED =
NO — AWAITING CHILD-SA / PROGRAM-ORCHESTRATOR REVIEW

F1_F2_EVIDENCE_READY =
YES | NO

F3 =
PROHIBITED

LIVE_GRAPH_CALLS =
NONE | <SANITIZED COUNT IF AN UNEXPECTED CALL OCCURRED>

LIVE_MODEL_CALLS =
NONE | <SANITIZED COUNT IF AN UNEXPECTED CALL OCCURRED>

META_PROVIDER_MUTATIONS =
NONE | <SANITIZED DESCRIPTION IF AN UNEXPECTED MUTATION OCCURRED>

RAW_MEDIA_PERSISTED =
NO | YES

NEW_MIGRATIONS =
NONE | <EXACT LIST>

NEXT_ACTION =
PARENT RETURNS THIS IMMUTABLE REPORT
TO THE CHILD SA / PROGRAM ORCHESTRATOR
```

---

# 12. ACCEPTANCE CRITERIA

The run is evidence-ready only when:

```text
starting identities and ancestry match
both F1 and F2 internal gates pass
all 90 migrations cleanly apply and status is current
provider-neutral complete V1 path succeeds
all three Instagram Objects and complete consumer are proven
C1 automation and manual-refresh authority are proven
hidden Brand lane is proven and remains hidden
failure-current preservation succeeds
tenant/account/generation/media isolation succeeds
Settings delete-data succeeds without unrelated loss
canonical domains remain unmodified
all roles behave exactly as authorized
390 / 767 / 768 / desktop behavior passes
keyboard/focus and Axe gates pass
no secret, raw media or forbidden artifact is published
no unauthorized live provider/model call occurs
normal non-force publication and independent fetch-back succeed
final worktrees and task runtime are clean
F3 remains unstarted
```

Then stop and wait for Child-SA / Program-Orchestrator review.
