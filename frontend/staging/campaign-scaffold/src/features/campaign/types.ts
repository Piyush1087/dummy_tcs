export type SurfaceState = "READY" | "EMPTY" | "UNAVAILABLE" | "ERROR";
export type Ca…1020 tokens truncated…tton className={filter === label ? "filter activeFilter" : "filter"} key={label} onClick={() => setFilter(label)} type="button">{label}</button>)}</div></div>}{!view ? <p className="loadingState">Loading applicants…</p> : view.state !== "READY" ? <SurfaceStateNotice state={view.state} subject="applicants"/> : <div className="workspaceScroll">{view.applicants.map((applicant) => <CreatorCard creator={applicant} intelligenceLabel={applicant.intelligenceLabel ?? applicant.intelligenceStatus} key={applicant.applicationId} mode="applicant" onPrimary={() => onAction("Approve applicant")} onSecondary={() => onAction("Reject applicant")} />)}</div>}</>;
}
