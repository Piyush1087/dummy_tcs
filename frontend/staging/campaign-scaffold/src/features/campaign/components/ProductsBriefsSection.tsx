import type { ProductsBriefsSummary } from "../types";

export function ProductsBriefsSection({ summary, expanded, onToggle, onAction }: { summary: ProductsBriefsSummary; expanded: boolean; onToggle: () => void; onAction: (action: string) => void }) {
  return <section className="productsSection"><div className="sectionHeading"><div><h2>Products &amp; Briefs</h2><small>{summary.products.length} Products · {summary.products.reduce((count, product) => count + product.briefs.length, 0)} Briefs</small></div><button aria-expanded={expanded} className="textAction" onClick={onToggle} type="button">{expanded ? "Collapse" : "Expand"}</button></div>{expanded && <div className="productRows">{summary.products.map((product) => <div className="productRow" key={product.campaignAssetId}><button className="productName" onClick={() => onAction("Product Details")} type="button">{product.name}</button><div>{product.briefs.map((brief) => <button className="briefName" key={brief.briefId} onClick={() => onAction("Brief Details")} type="button">{brief.name}</button>)}</div><button className="textAction" onClick={() => onAction("Add Brief")} type="button">Add Brief</button></div>)}<button className="secondaryAction" onClick={() => onAction("Add Product")} type="button">Add Product</button></div>}</section>;
}
